{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://logweir.dev/schemas/logweir-drill-scorecard-1.0.0.json",
  "title": "Scorecard",
  "type": "object",
  "required": [
    "approval",
    "engine",
    "evidence",
    "format_version",
    "integrity",
    "last_phase_completed",
    "measured",
    "objectives",
    "outcome",
    "phases",
    "requested_at",
    "run_id",
    "sample",
    "source",
    "target",
    "target_diff",
    "topic_parity"
  ],
  "properties": {
    "approval": {
      "$ref": "#/definitions/ApprovalInfo"
    },
    "approval_validated_at": {
      "default": null,
      "type": "string",
      "format": "date-time",
      "nullable": true
    },
    "engine": {
      "$ref": "#/definitions/EngineInfo"
    },
    "engine_subreport": {
      "default": null,
      "allOf": [
        {
          "$ref": "#/definitions/EngineSubreport"
        }
      ],
      "nullable": true
    },
    "evidence": {
      "$ref": "#/definitions/EvidenceInfo"
    },
    "format_version": {
      "description": "Semver of the document format, and the field every reader checks FIRST: Global Constraint 12 — readers ignore unknown fields and refuse a higher major.\n\nThe schema PINS the major with a pattern rather than leaving the field an unconstrained string. Without it, a document reading `\"9.9.9\"` validated cleanly against the file named `logweir-drill-scorecard-1.0.0.json`, so a schema-only validator — the one route that does not go through `Scorecard::refuse_unreadable_major` — accepted exactly the document GC12 exists to refuse. The pattern allows any `1.x.y`, because a MINOR bump adds optional fields only and a 1.0.0 reader must still read it.",
      "type": "string",
      "pattern": "^1\\.[0-9]+\\.[0-9]+$"
    },
    "integrity": {
      "$ref": "#/definitions/Integrity"
    },
    "last_phase_completed": {
      "description": "Highest phase INDEX completed. The DOMAIN is eleven phase slots, -1 through 9 (Global Constraint 18, panel decision D1, 2026-09-03); -1 is the source-side `--from-cluster` capture phase, whose code lands in a follow-up, so v0.1.0 never emits it.\n\nWHAT A v0.1.0 SIGNED DOCUMENT CAN ACTUALLY CARRY IS 5, 6 or 7, and the reason is structural rather than incidental: `phase8_score::run` signs a frozen clone, so phase 8's own record — and phase 9's, which happens after the put — are pushed onto the in-memory document AFTER the bytes were signed. A drill that completed teardown therefore reads 7 here and is NOT a drill whose teardown was skipped; the teardown is attested in its own separately signed document. `drill run`'s console line quotes this same value so the two cannot disagree.\n\nA previous version of this comment added \"a value below -1 is impossible because the phase-0 admission guard refusing yields exit code 3 with last_phase_completed = -1\", which implied such a document exists. It does not: exit 3 writes NO scorecard at all. The lower bound is a domain rule, enforced by `validate_invariants`, not a description of anything v0.1 emits.",
      "type": "integer",
      "format": "int8"
    },
    "measured": {
      "$ref": "#/definitions/Measured"
    },
    "objectives": {
      "$ref": "#/definitions/Objectives"
    },
    "outcome": {
      "$ref": "#/definitions/Outcome"
    },
    "phases": {
      "type": "array",
      "items": {
        "$ref": "#/definitions/PhaseRecord"
      }
    },
    "redactions": {
      "default": [],
      "type": "array",
      "items": {
        "$ref": "#/definitions/Redaction"
      }
    },
    "requested_at": {
      "type": "string",
      "format": "date-time"
    },
    "run_id": {
      "type": "string"
    },
    "sample": {
      "$ref": "#/definitions/SampleInfo"
    },
    "source": {
      "$ref": "#/definitions/SourceInfo"
    },
    "target": {
      "$ref": "#/definitions/TargetInfo"
    },
    "target_diff": {
      "$ref": "#/definitions/TargetDiffSummary"
    },
    "topic_parity": {
      "$ref": "#/definitions/TopicParity"
    },
    "triggered_by": {
      "default": null,
      "type": "string",
      "nullable": true
    }
  },
  "definitions": {
    "ApprovalInfo": {
      "type": "object",
      "required": [
        "approved_at",
        "approver",
        "key_id",
        "plan_hash",
        "self_attested",
        "ticket"
      ],
      "properties": {
        "approved_at": {
          "description": "The human's OUT-OF-BAND timestamp. Explicitly NOT an input to any measured field in v0.1 (spec §9.3 phase 8).",
          "type": "string",
          "format": "date-time"
        },
        "approver": {
          "type": "string"
        },
        "key_id": {
          "type": "string"
        },
        "plan_hash": {
          "type": "string"
        },
        "self_attested": {
          "description": "true when the approval key equals the signing key. Labelled, never refused (spec §10).",
          "type": "boolean"
        },
        "ticket": {
          "type": "string"
        }
      }
    },
    "EngineInfo": {
      "type": "object",
      "required": [
        "digest",
        "execution",
        "id",
        "levers",
        "matrix_verdict",
        "version"
      ],
      "properties": {
        "digest": {
          "type": "string"
        },
        "execution": {
          "type": "string"
        },
        "id": {
          "type": "string"
        },
        "levers": {
          "$ref": "#/definitions/Levers"
        },
        "matrix_verdict": {
          "$ref": "#/definitions/MatrixVerdict"
        },
        "matrix_verdict_reason": {
          "description": "REQUIRED when `matrix_verdict` is `fail`; null otherwise. Enforced by `validate_invariants`.",
          "default": null,
          "type": "string",
          "nullable": true
        },
        "version": {
          "type": "string"
        }
      }
    },
    "EngineSubreport": {
      "type": "object",
      "required": [
        "body_b64",
        "body_sha256",
        "caveat",
        "retained_verbatim",
        "retrieved_from"
      ],
      "properties": {
        "body_b64": {
          "description": "The engine's evidence report as the EXACT bytes read from the bucket, base64 (RFC 4648 standard alphabet, padded).\n\nNOT a parsed `serde_json::Value`. OSO's envelope \"covers the exact, complete stored report bytes. No canonicalization is performed at verification time\" and \"no JSON parsing, re-serialization, or canonicalization influences the digest or signature check\" [VERIFIED U/kafka-backup/crates/kafka-backup-core/src/evidence/envelope.rs:5,254]. Re-emitting a parsed value through `to_deterministic_json`'s two-space `PrettyFormatter` would change whitespace, escaping and number formatting, so the digest OSO signed would no longer match and the SP1c exit criterion (\"the embedded engine sub-report round-trips through OSO's own `validation evidence-verify`\") could never be ticked.",
          "type": "string"
        },
        "body_sha256": {
          "description": "`sha256:<hex>` of the DECODED bytes, so an auditor can re-check the binding without base64-decoding anything.",
          "type": "string"
        },
        "caveat": {
          "type": "string"
        },
        "retained_verbatim": {
          "type": "boolean"
        },
        "retrieved_from": {
          "type": "string"
        }
      }
    },
    "EvidenceInfo": {
      "description": "WHAT THIS BLOCK IS NOT: it is not evidence about the upload, and it is not a statement about the storage backend's capabilities.\n\nAll four fields describe the upload of THIS scorecard — an event that has not happened when the scorecard is signed, and cannot happen before it, because a signature covers bytes and the bytes must exist first. The scorecard is never re-serialised afterwards (that would invalidate the signature). So `crate::drill::phase8_score::run` ZEROES all four immediately before serialising, whatever the caller supplied, and every scorecard Logweir emits in v0.1 carries `{version_id: null, retain_until: null, immutable: false, create_only_enforced: false}`.\n\nRead that as **\"no proof was obtainable at signing time\"** — never as a finding about the object or the backend. The block deliberately UNDER-claims; what it buys is the guarantee that a valid Logweir signature can never cover an unsubstantiated WORM or create-only assertion.\n\nThe real post-upload readback IS published — in a second, separately signed document written after the put: `logweir/drills/<run_id>.receipt.json` plus its `.receipt.sig` sidecar (`crate::drill::phase8_score::PutReceipt`, Task 21a, discharging Task 20's carried obligation). It carries the observed `create_only_enforced`, `version_id`, `immutable` and `retain_until`, bound to the scorecard by the sha256 of the exact SIGNED BYTES. Read the two documents together: the scorecard is the measurement and under-claims about storage; the receipt is the storage evidence. `docs/stability.md` carries the adopter-facing version of this note.",
      "type": "object",
      "required": [
        "create_only_enforced",
        "immutable"
      ],
      "properties": {
        "create_only_enforced": {
          "description": "Always false in v0.1, on EVERY backend, including those whose conditional put was in fact used: whether the put was conditional is only known after it happens, and this document is signed first. `false` is NOT evidence that the backend lacks conditional put (spec §11) and NOT evidence that the object could have been overwritten — the two cases are indistinguishable in the signed document by design.",
          "type": "boolean"
        },
        "immutable": {
          "description": "Spec §6 C3 permits `true` ONLY after a provider readback. Always false in v0.1 — the readback happens after signing, and `object_store` 0.14 exposes no Object Lock / WORM API on any backend it can build, so there is nothing to read back. `false` means \"not proven here\"; it is NOT a claim that the object is mutable or unprotected.",
          "type": "boolean"
        },
        "retain_until": {
          "description": "Always null in v0.1: a retention date can only come from a provider readback performed after the put. NOT a claim that no retention applies.",
          "default": null,
          "type": "string",
          "format": "date-time",
          "nullable": true
        },
        "version_id": {
          "description": "Always null in v0.1: the store's version id for this object is only known after the put, and the put follows the signature. NOT a claim that the bucket is unversioned.",
          "default": null,
          "type": "string",
          "nullable": true
        }
      }
    },
    "Integrity": {
      "type": "object",
      "required": [
        "level",
        "mismatches",
        "records_sampled",
        "records_sampled_matching",
        "result"
      ],
      "properties": {
        "level": {
          "$ref": "#/definitions/IntegrityLevel"
        },
        "mismatches": {
          "type": "integer",
          "format": "uint64",
          "minimum": 0.0
        },
        "partial_reason": {
          "default": null,
          "type": "string",
          "nullable": true
        },
        "pass_rate_measured": {
          "description": "MEASURED `records_sampled_matching / records_sampled`. The REQUESTED rate stays in `objectives.pass_rate`, so an auditor can read both the ask and the result off one document.\n\nNull in THREE cases, not one — a `byte-fingerprint` document with a null rate here is well-formed, not malformed: 1. `level` is not `byte-fingerprint` (also enforced by `validate_invariants`). 2. Not every selection's record lane reached a conclusion. A ratio over part of the sample, published as if it were the whole, is misleading even when every figure in it is true. 3. `records_sampled` is 0 — a zero denominator is withheld rather than published as NaN.\n\nDecided in one place, `crate::drill::phase7_verify::roll_up`.",
          "default": null,
          "type": "number",
          "format": "double",
          "nullable": true
        },
        "records_sampled": {
          "type": "integer",
          "format": "uint64",
          "minimum": 0.0
        },
        "records_sampled_matching": {
          "type": "integer",
          "format": "uint64",
          "minimum": 0.0
        },
        "restoredPrincipalCouldConsume": {
          "description": "SP3 only; null, never false, when not attempted. The wire name is camelCase because spec §6.1 and §14 SP3 both cite the path `integrity.restoredPrincipalCouldConsume`, and `format_version` freezes at 1.0.0 in this task — renaming later would be a MAJOR bump.",
          "default": null,
          "type": "boolean",
          "nullable": true
        },
        "result": {
          "$ref": "#/definitions/IntegrityResult"
        }
      }
    },
    "IntegrityLevel": {
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "not-attempted"
          ]
        },
        {
          "description": "sha256(key‖value‖headers‖timestamp) reconciled per record.",
          "type": "string",
          "enum": [
            "byte-fingerprint"
          ]
        },
        {
          "description": "The KBAK decoder returned Err(Unsupported); records were consumed but not reconciled against archive bytes (spec §11).",
          "type": "string",
          "enum": [
            "consume-only"
          ]
        }
      ]
    },
    "IntegrityResult": {
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "pass",
            "fail"
          ]
        },
        {
          "description": "A selection whose evidence was inconclusive — something in the sample was not examined, so the drill cannot claim it. NOT a pass.\n\nA COMPACTED TOPIC DOES NOT REACH THIS in v0.1: it is reported as `fail`, through the same path a real mismatch takes. See `docs/stability.md`.",
          "type": "string",
          "enum": [
            "partial"
          ]
        }
      ]
    },
    "LeverState": {
      "description": "Per-run readback of an engine lever (spec §9.3 phase 5, §7.2(a)).",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "honoured",
            "ignored"
          ]
        },
        {
          "description": "`dry_run_check_segments` is not observable from DryRunReport; its only per-run readback is the absence of an unknown-key warning.",
          "type": "string",
          "enum": [
            "unknown-not-observable"
          ]
        }
      ]
    },
    "Levers": {
      "type": "object",
      "required": [
        "dry_run_check_segments",
        "header_preflight"
      ],
      "properties": {
        "dry_run_check_segments": {
          "$ref": "#/definitions/LeverState"
        },
        "header_preflight": {
          "$ref": "#/definitions/LeverState"
        },
        "unknown_key_warnings": {
          "description": "Every path the engine logged as `Ignoring unknown config key <path>`. A match on a Logweir-rendered key aborts the run (spec §7.2(a)).",
          "default": [],
          "type": "array",
          "items": {
            "type": "string"
          }
        }
      }
    },
    "MatrixVerdict": {
      "description": "The engine-matrix row THIS RUN established, decided once — in `logweir::drill::phase8_score::matrix_verdict_for` — immediately before the document is validated and signed.\n\nThis comment used to read \"copied from this engine tag's engine-matrix row; never re-derived per run\". That was never true of the shipped code: phase 5 raised the field to `Pass` the moment the `header_preflight` lever was honoured, and nothing lowered it afterwards, so a drill that blocked at preflight, restored nothing, failed its reconciliation or missed its RTO signed `matrix_verdict: \"pass\"` beside its own contradicting `outcome`. `docs/support-matrix.md` defines the five values; the decision function's doc comment carries the table.\n\nEvery variant serialises as a FLAT kebab-case string, so the field's schema is a plain enum and the auditor's ~20-line Python verifier never has to handle `string | object`. The reason for a `fail` lives in `EngineInfo.matrix_verdict_reason`, mirroring the `integrity.result`/`partial_reason` pair.",
      "type": "string",
      "enum": [
        "pass",
        "pass-degraded",
        "fail",
        "fail-lever-not-honoured",
        "unsupported-lever-absent"
      ]
    },
    "Measured": {
      "type": "object",
      "properties": {
        "rpo_seconds": {
          "description": "ARCHIVE COVERAGE GAP at the requested recovery point — NOT source-relative data loss.\n\n`requested_point_in_time - newest_restored_record`, in that order, and **never negative**: it is how far SHORT of the requested recovery point the newest restored record falls. A record at or beyond the requested point means no gap there, which is 0. A negative gap is not a smaller gap, it is a meaningless one, and a reader meeting `-90` here would most likely take it for \"no data loss\". Enforced by `validate_invariants`, so the rule holds for every writer, not only for today's single call site in `crate::drill::phase8_score`.",
          "default": null,
          "type": "integer",
          "format": "int64",
          "nullable": true
        },
        "rpo_source_relative_seconds": {
          "description": "Source-relative data loss: how far behind the live source the restored data is. Null in v0.1 (the source is never contacted); becomes a number only under `--from-cluster`. **Never negative**, for the same reason as `rpo_seconds` and enforced by the same `validate_invariants` rule — a rule deliberately put in place BEFORE the first writer exists, so the `--from-cluster` implementation inherits it rather than has to invent it.",
          "default": null,
          "type": "integer",
          "format": "int64",
          "nullable": true
        },
        "rpo_source_relative_unmeasured_reason": {
          "default": null,
          "type": "string",
          "nullable": true
        },
        "rto_excluding_preflight_seconds": {
          "description": "THE value compared against objectives.rto_seconds (spec §9.3 phase 8).",
          "default": null,
          "type": "integer",
          "format": "uint64",
          "minimum": 0.0,
          "nullable": true
        },
        "rto_requested_to_verified_seconds": {
          "default": null,
          "type": "integer",
          "format": "uint64",
          "minimum": 0.0,
          "nullable": true
        },
        "rto_restore_only_seconds": {
          "default": null,
          "type": "integer",
          "format": "uint64",
          "minimum": 0.0,
          "nullable": true
        },
        "rto_seconds": {
          "default": null,
          "type": "integer",
          "format": "uint64",
          "minimum": 0.0,
          "nullable": true
        }
      }
    },
    "Objectives": {
      "type": "object",
      "properties": {
        "met": {
          "description": "The aggregate verdict over whichever objectives above are non-null.\n\n- `true`  — every non-null objective was met. - `false` — at least one non-null objective was missed. - `null`  — a `pass_rate` objective WAS requested but could not be measured (`integrity.pass_rate_measured` is null), so the aggregate is unmeasurable rather than satisfied.\n\nThe null condition is about the MEASURED rate, not this block's `pass_rate`. The earlier wording (\"null when pass_rate is null\") read the other way round and was wrong in the direction that matters: when `objectives.pass_rate` is null, no rate was asked for, nothing is unmeasurable, and `met` is a plain true/false over the remaining objectives. Decided in exactly one place, `crate::drill::phase8_score::decide`.",
          "default": null,
          "type": "boolean",
          "nullable": true
        },
        "pass_rate": {
          "default": null,
          "type": "number",
          "format": "double",
          "nullable": true
        },
        "rpo_seconds": {
          "description": "The REQUESTED maximum archive-coverage gap, copied verbatim from the adopter's spec. **Never negative** — a negative allowed gap is not a stricter objective, it is an unsatisfiable one (`measured.rpo_seconds` is itself non-negative, so `got <= want` could never hold), and a signed document asserting it would be incoherent. Enforced by `validate_invariants`; nothing else validates this value, since it travels straight from YAML into the signed document.",
          "default": null,
          "type": "integer",
          "format": "int64",
          "nullable": true
        },
        "rto_seconds": {
          "default": null,
          "type": "integer",
          "format": "uint64",
          "minimum": 0.0,
          "nullable": true
        }
      }
    },
    "Outcome": {
      "description": "Spec §6.1. `drift` is deliberately NOT a v0.1 value — v0.1 collects no metadata. It returns as a minor bump when SP2 ships.",
      "type": "string",
      "enum": [
        "pass",
        "fail-objective",
        "fail-integrity",
        "preflight-failed"
      ]
    },
    "PhaseRecord": {
      "type": "object",
      "required": [
        "at",
        "duration_ms",
        "name",
        "outcome",
        "phase"
      ],
      "properties": {
        "at": {
          "type": "string",
          "format": "date-time"
        },
        "duration_ms": {
          "type": "integer",
          "format": "uint64",
          "minimum": 0.0
        },
        "name": {
          "type": "string"
        },
        "notes": {
          "description": "Free-form evidence attached to this phase record. Two producers today, and the list is corrected here rather than left stale (Task 20 fix round 2): - phase 5 (`crate::drill::phase5_preflight`) carries each `Finding`'s tag/detail here so a `preflight-failed` scorecard states *why* the drill was refused, not just that it was — without this field the adjudication's reasoning had nowhere in the signed document to land. Note that phase 5 defines WHAT it contributes; the `PhaseRecord` itself is not constructed until the orchestrator lands (Task 21a). - phase 8 (`crate::drill::phase8_score`) writes onto the phase-8 record when the engine-validation prefix is empty, and when it holds more than one object (naming the key retained and that others were dropped). Both are warnings that must not be silent: an absent or ambiguous engine sub-report is a fact about the evidence, not a failure.\n\n`skip_serializing_if` (matching `TargetDiffSummary::absent`, Task 16 fix round 1) so a document signed before this field existed keeps round-tripping byte-for-byte when it has nothing to report.\n\nThis reverses `task-3-addendum.md` A2 (\"do not add a `notes` field to `PhaseRecord`\"), on controller authority, in Task 17 fix round 1: that ruling predates `task-21a-brief.md:259`, which writes `p.notes = …` against this exact struct, so leaving the field out only delayed the same `E0560` to Task 21a.",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "outcome": {
          "type": "string"
        },
        "phase": {
          "type": "integer",
          "format": "int8"
        }
      }
    },
    "Redaction": {
      "type": "object",
      "required": [
        "path",
        "present",
        "reason"
      ],
      "properties": {
        "path": {
          "description": "A JSON pointer into THIS scorecard — never a missing field.",
          "type": "string"
        },
        "present": {
          "type": "boolean"
        },
        "reason": {
          "type": "string"
        }
      }
    },
    "SampleInfo": {
      "type": "object",
      "required": [
        "anchor",
        "coverage_note",
        "partitions",
        "records_expected",
        "records_restored",
        "topics",
        "window_end",
        "window_start"
      ],
      "properties": {
        "anchor": {
          "type": "string"
        },
        "coverage_note": {
          "type": "string"
        },
        "partitions": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0.0
        },
        "records_expected": {
          "description": "THE CANARY SIZE: how many records this drill set out to reconcile — the sum of `sample.records_per_partition` over the partitions actually selected. `integrity.records_sampled` is measured against THIS figure.\n\nIt is NOT `crate::drill::phase4_sample::Selection::records_expected` (in `crates/logweir`), which shares the name and answers a different question: how many records the manifest says the whole sampled window holds. The two differ by orders of magnitude on any real archive — 25 against 500 on a single partition of this repository's own fixtures — and substituting one for the other would make a signed document overstate or understate what was verified. Both definition sites carry this note deliberately (Task 16's parked item, discharged in Task 21a); the field is not renamed because a scorecard field name is a Global Constraint 12 question and the format is frozen at 1.0.0.",
          "type": "integer",
          "format": "uint64",
          "minimum": 0.0
        },
        "records_restored": {
          "type": "integer",
          "format": "uint64",
          "minimum": 0.0
        },
        "topics": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0.0
        },
        "window_end": {
          "type": "string",
          "format": "date-time"
        },
        "window_start": {
          "type": "string",
          "format": "date-time"
        }
      }
    },
    "SourceInfo": {
      "type": "object",
      "required": [
        "backup_id",
        "captured_by_logweir",
        "manifest_sha256"
      ],
      "properties": {
        "backup_id": {
          "type": "string"
        },
        "captured_by_logweir": {
          "description": "true ONLY under `--from-cluster`. `--from-cluster` is in v0.1 scope (Global Constraint 18 / docs/adr/0007-from-cluster-in-v0.1.md); its execution path lands in a follow-up task, so nothing in the main task line yet sets this true. The invariant below is live regardless.",
          "type": "boolean"
        },
        "manifest_sha256": {
          "type": "string"
        },
        "manifest_version_id": {
          "default": null,
          "type": "string",
          "nullable": true
        }
      }
    },
    "TargetDiffSummary": {
      "description": "The published sink for phase 3 — the phase no shipped artifact performs. Without this block the diff would be computed and discarded, and the §4 positioning claim would rest on a value that reaches no reader.",
      "type": "object",
      "required": [
        "level"
      ],
      "properties": {
        "absent": {
          "description": "Mapped target topics that do NOT exist on the target — the normal case on a scratch cluster. Every entry here has a corresponding `would_create` entry at the partition count the restore will build. `skip_serializing_if` (unlike its siblings here) so a document signed before this field existed keeps round-tripping byte-for-byte when it has nothing to report — added in Task 16 fix round 1, after `TargetDiff.absent` was found computed and never read.",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "collisions": {
          "description": "Mapped target topics that already EXIST on the target — regardless of whether they currently hold any records. Existence alone is what makes writing into them a collision worth flagging (Task 16 fix round 1, MINOR-2): an existing-but-empty topic can still carry configuration that differs from what the restore expects, and creating a topic fresh is a different operational fact from reusing one someone else already created — even an empty one. The prior wording here (\"already existed AND already held records\") described a narrower rule than the code implements; the code is right (refusing to write into an existing empty topic costs at most a false alarm, while the narrower rule would let a restore silently write into a topic someone else created), so this text was corrected to match it.",
          "default": [],
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "level": {
          "description": "\"full\" in v0.1. Becomes \"shallow\" only if spec §15 cut 0d is ever taken.",
          "type": "string"
        },
        "would_create": {
          "description": "(mapped target topic, partition count the restore created).",
          "default": [],
          "type": "array",
          "items": {
            "type": "array",
            "items": [
              {
                "type": "string"
              },
              {
                "type": "integer",
                "format": "int32"
              }
            ],
            "maxItems": 2,
            "minItems": 2
          }
        }
      }
    },
    "TargetInfo": {
      "type": "object",
      "required": [
        "cluster_id",
        "marker_topic",
        "topic_mapping_entries",
        "topic_mapping_prefix",
        "topic_mapping_sha256"
      ],
      "properties": {
        "cluster_id": {
          "type": "string"
        },
        "marker_topic": {
          "description": "The v0.1 segregation proof, verified over the logweir-kafka client: cluster_id ∈ allowedClusterIds AND this topic exists (spec §9.3 phase 0).",
          "type": "string"
        },
        "topic_mapping_entries": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0.0
        },
        "topic_mapping_prefix": {
          "type": "string"
        },
        "topic_mapping_sha256": {
          "description": "sha256 over the rendered restore.yaml topic_mapping block, so an auditor can re-derive exactly what was written.",
          "type": "string"
        }
      }
    },
    "TopicParity": {
      "type": "object",
      "required": [
        "intentionally_deviated",
        "unexpected_divergence"
      ],
      "properties": {
        "intentionally_deviated": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "unexpected_divergence": {
          "type": "array",
          "items": {
            "type": "string"
          }
        }
      }
    }
  }
}
