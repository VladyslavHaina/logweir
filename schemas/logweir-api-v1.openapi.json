{
  "openapi": "3.0.3",
  "info": {
    "title": "Logweir product API",
    "version": "1.0.0-alpha.1",
    "description": "The bounded shared-console product API (PLAT-17.1). Not a Kubernetes proxy: no generic resource path, no Secret, Pod, log, exec or Job access, no delete, and no arbitrary patch. Every error is application/problem+json. Unsafe methods require Content-Type: application/json and an Origin equal to the configured public origin. Durable creates require Idempotency-Key. Capabilities without a route are advertised false in /api/v1/session."
  },
  "paths": {
    "/healthz": {
      "get": {
        "operationId": "getHealthz",
        "summary": "Process liveness.",
        "responses": {
          "200": {
            "description": "Alive.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HealthStatus"
                }
              }
            }
          },
          "default": {
            "description": "An application/problem+json error; the possible codes are listed in x-logweir-problem-codes.",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          }
        },
        "x-logweir-problem-codes": [
          "header_not_allowed",
          "misdirected_request"
        ]
      }
    },
    "/readyz": {
      "get": {
        "operationId": "getReadyz",
        "summary": "Readiness: configuration valid and Kubernetes reachable. No detail is returned.",
        "responses": {
          "200": {
            "description": "Ready.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HealthStatus"
                }
              }
            }
          },
          "default": {
            "description": "An application/problem+json error; the possible codes are listed in x-logweir-problem-codes.",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          }
        },
        "x-logweir-problem-codes": [
          "header_not_allowed",
          "kubernetes_unavailable",
          "misdirected_request"
        ]
      }
    },
    "/api/v1/session": {
      "get": {
        "operationId": "getSession",
        "summary": "The actor, its roles and explicit namespace grants, the synchronizer CSRF token and the capability flags. In shared mode the CSRF token must be sent in X-CSRF-Token on every unsafe method.",
        "responses": {
          "200": {
            "description": "The session.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/SessionResponse"
                }
              }
            }
          },
          "default": {
            "description": "An application/problem+json error; the possible codes are listed in x-logweir-problem-codes.",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          }
        },
        "x-logweir-problem-codes": [
          "header_not_allowed",
          "internal_error",
          "method_not_allowed",
          "misdirected_request",
          "session_expired",
          "unauthenticated"
        ]
      }
    },
    "/api/v1/session/logout": {
      "post": {
        "operationId": "logout",
        "summary": "Clear the session cookie. Shared mode only; it is an unsafe method, so it requires the exact Origin, application/json and the synchronizer token like every other mutation.",
        "parameters": [
          {
            "name": "Origin",
            "in": "header",
            "required": true,
            "description": "Must equal the configured public origin exactly.",
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "204": {
            "description": "The session cookie is cleared."
          },
          "default": {
            "description": "An application/problem+json error; the possible codes are listed in x-logweir-problem-codes.",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          }
        },
        "x-logweir-problem-codes": [
          "header_not_allowed",
          "internal_error",
          "method_not_allowed",
          "misdirected_request",
          "not_found",
          "origin_mismatch",
          "session_expired",
          "unauthenticated",
          "unsupported_media_type"
        ]
      }
    },
    "/auth/login": {
      "get": {
        "operationId": "authLogin",
        "summary": "Begin an OpenID Connect Authorization Code + PKCE S256 sign-in. Shared mode only; answers 303 to the provider's authorization endpoint and sets the short-lived __Host-logweir_login cookie holding state, nonce and the PKCE verifier.",
        "parameters": [
          {
            "name": "next",
            "in": "query",
            "required": false,
            "description": "Where to land after signing in. Only a path under /ui/ on this origin; anything else is ignored.",
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "303": {
            "description": "Redirect to the identity provider."
          },
          "default": {
            "description": "An application/problem+json error; the possible codes are listed in x-logweir-problem-codes.",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          }
        },
        "x-logweir-problem-codes": [
          "header_not_allowed",
          "kubernetes_unavailable",
          "misdirected_request",
          "not_found",
          "rate_limited"
        ]
      }
    },
    "/auth/callback": {
      "get": {
        "operationId": "authCallback",
        "summary": "Finish a sign-in. Shared mode only; validates state, exchanges the code with PKCE, validates the ID token (exact issuer, audience, allowed algorithm, JWKS signature, exp, iat, nonce) and sets __Host-logweir_session. The browser never receives a provider token.",
        "parameters": [
          {
            "name": "code",
            "in": "query",
            "required": false,
            "description": "The authorization code.",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "state",
            "in": "query",
            "required": false,
            "description": "The state this service issued.",
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "303": {
            "description": "Redirect to the console."
          },
          "default": {
            "description": "An application/problem+json error; the possible codes are listed in x-logweir-problem-codes.",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          }
        },
        "x-logweir-problem-codes": [
          "header_not_allowed",
          "misdirected_request",
          "not_found",
          "rate_limited",
          "unauthenticated"
        ]
      }
    },
    "/api/v1/namespaces": {
      "get": {
        "operationId": "listNamespaces",
        "summary": "The configured namespace grants. Core Namespace objects are never listed.",
        "responses": {
          "200": {
            "description": "The grants.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/NamespaceListResponse"
                }
              }
            }
          },
          "default": {
            "description": "An application/problem+json error; the possible codes are listed in x-logweir-problem-codes.",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          }
        },
        "x-logweir-problem-codes": [
          "header_not_allowed",
          "internal_error",
          "method_not_allowed",
          "misdirected_request",
          "session_expired",
          "unauthenticated"
        ]
      }
    },
    "/api/v1/namespaces/{ns}/connections": {
      "get": {
        "operationId": "listConnections",
        "summary": "KafkaCluster projections. Secret reference names only.",
        "parameters": [
          {
            "name": "ns",
            "in": "path",
            "required": true,
            "description": "An explicitly granted namespace. An ungranted namespace is namespace_forbidden whether or not it exists.",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "limit",
            "in": "query",
            "required": false,
            "description": "Page size, 1 to 200. Default 50.",
            "schema": {
              "type": "integer",
              "minimum": 1,
              "maximum": 200,
              "default": 50
            }
          },
          {
            "name": "cursor",
            "in": "query",
            "required": false,
            "description": "The opaque nextCursor of the previous page. Bound to actor, route, namespace and filters; expires after 15 minutes.",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "labelSelector",
            "in": "query",
            "required": false,
            "description": "Equality-only label selector, key=value terms separated by commas.",
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "One page.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ConnectionList"
                }
              }
            }
          },
          "default": {
            "description": "An application/problem+json error; the possible codes are listed in x-logweir-problem-codes.",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          }
        },
        "x-logweir-problem-codes": [
          "cursor_expired",
          "cursor_invalid",
          "forbidden",
          "header_not_allowed",
          "internal_error",
          "kubernetes_unavailable",
          "malformed_request",
          "method_not_allowed",
          "misdirected_request",
          "namespace_forbidden",
          "rate_limited",
          "session_expired",
          "unauthenticated",
          "upstream_timeout",
          "validation_failed"
        ]
      },
      "post": {
        "operationId": "createConnection",
        "summary": "Create a KafkaCluster that references an existing credential Secret by name.",
        "parameters": [
          {
            "name": "ns",
            "in": "path",
            "required": true,
            "description": "An explicitly granted namespace. An ungranted namespace is namespace_forbidden whether or not it exists.",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "Idempotency-Key",
            "in": "header",
            "required": true,
            "description": "8 to 128 visible ASCII characters. The object name is derived from (actor, namespace, route, key); a replay of the same request returns 200 with the same object, a different request with the same key is idempotency_conflict.",
            "schema": {
              "type": "string",
              "minLength": 8,
              "maxLength": 128
            }
          },
          {
            "name": "Origin",
            "in": "header",
            "required": true,
            "description": "Must equal the configured public origin exactly.",
            "schema": {
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CreateConnectionRequest"
              }
            }
          }
        },
        "responses": {
          "201": {
            "description": "Created.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ConnectionResponse"
                }
              }
            }
          },
          "200": {
            "description": "Replayed an identical earlier request; the same object.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ConnectionResponse"
                }
              }
            }
          },
          "default": {
            "description": "An application/problem+json error; the possible codes are listed in x-logweir-problem-codes.",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          }
        },
        "x-logweir-problem-codes": [
          "forbidden",
          "header_not_allowed",
          "idempotency_conflict",
          "idempotency_key_invalid",
          "idempotency_key_required",
          "internal_error",
          "kubernetes_unavailable",
          "malformed_request",
          "method_not_allowed",
          "misdirected_request",
          "namespace_forbidden",
          "origin_mismatch",
          "payload_too_large",
          "rate_limited",
          "session_expired",
          "state_conflict",
          "unauthenticated",
          "unsupported_media_type",
          "upstream_timeout",
          "validation_failed"
        ]
      }
    },
    "/api/v1/namespaces/{ns}/connections/{name}": {
      "get": {
        "operationId": "getConnection",
        "summary": "One KafkaCluster projection.",
        "parameters": [
          {
            "name": "ns",
            "in": "path",
            "required": true,
            "description": "An explicitly granted namespace. An ungranted namespace is namespace_forbidden whether or not it exists.",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "name",
            "in": "path",
            "required": true,
            "description": "The object name.",
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "The object.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ConnectionResponse"
                }
              }
            }
          },
          "default": {
            "description": "An application/problem+json error; the possible codes are listed in x-logweir-problem-codes.",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          }
        },
        "x-logweir-problem-codes": [
          "forbidden",
          "header_not_allowed",
          "internal_error",
          "kubernetes_unavailable",
          "malformed_request",
          "method_not_allowed",
          "misdirected_request",
          "namespace_forbidden",
          "not_found",
          "rate_limited",
          "session_expired",
          "unauthenticated",
          "upstream_timeout"
        ]
      }
    },
    "/api/v1/cadence-previews": {
      "get": {
        "operationId": "previewCadence",
        "summary": "What a cron expression, or a preset, will actually do in a time zone: the next runs as UTC instants with their local wall times and DST markers. Computed by the same module the scheduler uses; the browser never evaluates cron. It reads nothing from Kubernetes and takes no namespace, so it needs permission to read schedules in at least one granted namespace.",
        "parameters": [
          {
            "name": "schedule",
            "in": "query",
            "required": false,
            "description": "A five-field cron expression or @hourly/@daily/@weekly. Exactly one of schedule or preset.",
            "schema": {
              "type": "string",
              "maxLength": 128
            }
          },
          {
            "name": "preset",
            "in": "query",
            "required": false,
            "description": "hourly, everyNHours, daily, weekly or monthly, with that preset's parameters. The response carries the canonical expression it compiled to.",
            "schema": {
              "type": "string",
              "enum": [
                "hourly",
                "everyNHours",
                "daily",
                "weekly",
                "monthly"
              ]
            }
          },
          {
            "name": "minute",
            "in": "query",
            "required": false,
            "description": "0 to 59. A parameter of every preset.",
            "schema": {
              "type": "integer",
              "minimum": 0,
              "maximum": 59
            }
          },
          {
            "name": "hour",
            "in": "query",
            "required": false,
            "description": "0 to 23. daily, weekly and monthly.",
            "schema": {
              "type": "integer",
              "minimum": 0,
              "maximum": 23
            }
          },
          {
            "name": "dayOfWeek",
            "in": "query",
            "required": false,
            "description": "0 to 6, 0 = Sunday. weekly only.",
            "schema": {
              "type": "integer",
              "minimum": 0,
              "maximum": 6
            }
          },
          {
            "name": "dayOfMonth",
            "in": "query",
            "required": false,
            "description": "1 to 28. monthly only; 29 to 31 are not a monthly cadence.",
            "schema": {
              "type": "integer",
              "minimum": 1,
              "maximum": 28
            }
          },
          {
            "name": "n",
            "in": "query",
            "required": false,
            "description": "2, 3, 4, 6, 8 or 12. everyNHours only: */n matches local wall-clock hours divisible by n, not n hours after creation.",
            "schema": {
              "type": "integer",
              "enum": [
                2,
                3,
                4,
                6,
                8,
                12
              ]
            }
          },
          {
            "name": "timeZone",
            "in": "query",
            "required": false,
            "description": "An IANA zone name. Absent means UTC and reproduces the pre-PLAT-04.2 slots exactly.",
            "schema": {
              "type": "string",
              "maxLength": 64
            }
          },
          {
            "name": "count",
            "in": "query",
            "required": false,
            "description": "1 to 20. Default 10. A shorter answer is real: an expression can run out of firings.",
            "schema": {
              "type": "integer",
              "minimum": 1,
              "maximum": 20,
              "default": 10
            }
          },
          {
            "name": "after",
            "in": "query",
            "required": false,
            "description": "An RFC 3339 instant to walk strictly after. Default: the server's now.",
            "schema": {
              "type": "string",
              "format": "date-time"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "The canonical expression, the effective zone, the tz database that resolved it, and the firings.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CadencePreviewResponse"
                }
              }
            }
          },
          "default": {
            "description": "An application/problem+json error; the possible codes are listed in x-logweir-problem-codes.",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          }
        },
        "x-logweir-problem-codes": [
          "forbidden",
          "header_not_allowed",
          "internal_error",
          "malformed_request",
          "method_not_allowed",
          "misdirected_request",
          "session_expired",
          "unauthenticated",
          "validation_failed"
        ]
      }
    },
    "/api/v1/namespaces/{ns}/schedules": {
      "get": {
        "operationId": "listSchedules",
        "summary": "BackupSchedule projections.",
        "parameters": [
          {
            "name": "ns",
            "in": "path",
            "required": true,
            "description": "An explicitly granted namespace. An ungranted namespace is namespace_forbidden whether or not it exists.",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "limit",
            "in": "query",
            "required": false,
            "description": "Page size, 1 to 200. Default 50.",
            "schema": {
              "type": "integer",
              "minimum": 1,
              "maximum": 200,
              "default": 50
            }
          },
          {
            "name": "cursor",
            "in": "query",
            "required": false,
            "description": "The opaque nextCursor of the previous page. Bound to actor, route, namespace and filters; expires after 15 minutes.",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "labelSelector",
            "in": "query",
            "required": false,
            "description": "Equality-only label selector, key=value terms separated by commas.",
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "One page.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ScheduleList"
                }
              }
            }
          },
          "default": {
            "description": "An application/problem+json error; the possible codes are listed in x-logweir-problem-codes.",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          }
        },
        "x-logweir-problem-codes": [
          "cursor_expired",
          "cursor_invalid",
          "forbidden",
          "header_not_allowed",
          "internal_error",
          "kubernetes_unavailable",
          "malformed_request",
          "method_not_allowed",
          "misdirected_request",
          "namespace_forbidden",
          "rate_limited",
          "session_expired",
          "unauthenticated",
          "upstream_timeout",
          "validation_failed"
        ]
      },
      "post": {
        "operationId": "createSchedule",
        "summary": "Create a BackupSchedule.",
        "parameters": [
          {
            "name": "ns",
            "in": "path",
            "required": true,
            "description": "An explicitly granted namespace. An ungranted namespace is namespace_forbidden whether or not it exists.",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "Idempotency-Key",
            "in": "header",
            "required": true,
            "description": "8 to 128 visible ASCII characters. The object name is derived from (actor, namespace, route, key); a replay of the same request returns 200 with the same object, a different request with the same key is idempotency_conflict.",
            "schema": {
              "type": "string",
              "minLength": 8,
              "maxLength": 128
            }
          },
          {
            "name": "Origin",
            "in": "header",
            "required": true,
            "description": "Must equal the configured public origin exactly.",
            "schema": {
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CreateScheduleRequest"
              }
            }
          }
        },
        "responses": {
          "201": {
            "description": "Created.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ScheduleResponse"
                }
              }
            }
          },
          "200": {
            "description": "Replayed an identical earlier request; the same object.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ScheduleResponse"
                }
              }
            }
          },
          "default": {
            "description": "An application/problem+json error; the possible codes are listed in x-logweir-problem-codes.",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          }
        },
        "x-logweir-problem-codes": [
          "forbidden",
          "header_not_allowed",
          "idempotency_conflict",
          "idempotency_key_invalid",
          "idempotency_key_required",
          "internal_error",
          "kubernetes_unavailable",
          "malformed_request",
          "method_not_allowed",
          "misdirected_request",
          "namespace_forbidden",
          "origin_mismatch",
          "payload_too_large",
          "rate_limited",
          "session_expired",
          "state_conflict",
          "unauthenticated",
          "unsupported_media_type",
          "upstream_timeout",
          "validation_failed"
        ]
      }
    },
    "/api/v1/namespaces/{ns}/schedules/{name}": {
      "get": {
        "operationId": "getSchedule",
        "summary": "One BackupSchedule projection.",
        "parameters": [
          {
            "name": "ns",
            "in": "path",
            "required": true,
            "description": "An explicitly granted namespace. An ungranted namespace is namespace_forbidden whether or not it exists.",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "name",
            "in": "path",
            "required": true,
            "description": "The object name.",
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "The object.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ScheduleResponse"
                }
              }
            }
          },
          "default": {
            "description": "An application/problem+json error; the possible codes are listed in x-logweir-problem-codes.",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          }
        },
        "x-logweir-problem-codes": [
          "forbidden",
          "header_not_allowed",
          "internal_error",
          "kubernetes_unavailable",
          "malformed_request",
          "method_not_allowed",
          "misdirected_request",
          "namespace_forbidden",
          "not_found",
          "rate_limited",
          "session_expired",
          "unauthenticated",
          "upstream_timeout"
        ]
      },
      "put": {
        "operationId": "updateSchedulePolicy",
        "summary": "Replace a schedule's FUTURE policy under expectedGeneration. The whole policy is sent and a field omitted is removed. spec.sourceRef is not on the request: a different cluster is a different schedule, and naming it is 422 sourceRef: field_immutable. It never touches a Backup that already exists, its frozen inputs or its Job. Idempotency-Key is refused: the precondition is the idempotence.",
        "parameters": [
          {
            "name": "ns",
            "in": "path",
            "required": true,
            "description": "An explicitly granted namespace. An ungranted namespace is namespace_forbidden whether or not it exists.",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "name",
            "in": "path",
            "required": true,
            "description": "The object name.",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "Origin",
            "in": "header",
            "required": true,
            "description": "Must equal the configured public origin exactly.",
            "schema": {
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/UpdateSchedulePolicyRequest"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "The edited schedule, with its new generation.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ScheduleResponse"
                }
              }
            }
          },
          "default": {
            "description": "An application/problem+json error; the possible codes are listed in x-logweir-problem-codes.",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          }
        },
        "x-logweir-problem-codes": [
          "forbidden",
          "header_not_allowed",
          "idempotency_key_invalid",
          "internal_error",
          "kubernetes_unavailable",
          "malformed_request",
          "method_not_allowed",
          "misdirected_request",
          "namespace_forbidden",
          "not_found",
          "origin_mismatch",
          "payload_too_large",
          "precondition_failed",
          "rate_limited",
          "session_expired",
          "unauthenticated",
          "unsupported_media_type",
          "upstream_timeout",
          "validation_failed"
        ]
      }
    },
    "/api/v1/namespaces/{ns}/schedules/{name}:set-suspension": {
      "post": {
        "operationId": "setScheduleSuspension",
        "summary": "Set spec.suspend, the schedule's one mutable field, if expectedResourceVersion is current. Idempotency-Key is refused.",
        "parameters": [
          {
            "name": "ns",
            "in": "path",
            "required": true,
            "description": "An explicitly granted namespace. An ungranted namespace is namespace_forbidden whether or not it exists.",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "name",
            "in": "path",
            "required": true,
            "description": "The object name.",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "Origin",
            "in": "header",
            "required": true,
            "description": "Must equal the configured public origin exactly.",
            "schema": {
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/SetSuspensionRequest"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "The updated schedule.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ScheduleResponse"
                }
              }
            }
          },
          "default": {
            "description": "An application/problem+json error; the possible codes are listed in x-logweir-problem-codes.",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          }
        },
        "x-logweir-problem-codes": [
          "forbidden",
          "header_not_allowed",
          "idempotency_key_invalid",
          "internal_error",
          "kubernetes_unavailable",
          "malformed_request",
          "method_not_allowed",
          "misdirected_request",
          "namespace_forbidden",
          "not_found",
          "origin_mismatch",
          "payload_too_large",
          "precondition_failed",
          "rate_limited",
          "session_expired",
          "unauthenticated",
          "unsupported_media_type",
          "upstream_timeout",
          "validation_failed"
        ]
      }
    },
    "/api/v1/namespaces/{ns}/backups": {
      "get": {
        "operationId": "listBackups",
        "summary": "Backup projections with a status summary.",
        "parameters": [
          {
            "name": "ns",
            "in": "path",
            "required": true,
            "description": "An explicitly granted namespace. An ungranted namespace is namespace_forbidden whether or not it exists.",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "limit",
            "in": "query",
            "required": false,
            "description": "Page size, 1 to 200. Default 50.",
            "schema": {
              "type": "integer",
              "minimum": 1,
              "maximum": 200,
              "default": 50
            }
          },
          {
            "name": "cursor",
            "in": "query",
            "required": false,
            "description": "The opaque nextCursor of the previous page. Bound to actor, route, namespace and filters; expires after 15 minutes.",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "labelSelector",
            "in": "query",
            "required": false,
            "description": "Equality-only label selector, key=value terms separated by commas.",
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "One page.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/BackupList"
                }
              }
            }
          },
          "default": {
            "description": "An application/problem+json error; the possible codes are listed in x-logweir-problem-codes.",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          }
        },
        "x-logweir-problem-codes": [
          "cursor_expired",
          "cursor_invalid",
          "forbidden",
          "header_not_allowed",
          "internal_error",
          "kubernetes_unavailable",
          "malformed_request",
          "method_not_allowed",
          "misdirected_request",
          "namespace_forbidden",
          "rate_limited",
          "session_expired",
          "unauthenticated",
          "upstream_timeout",
          "validation_failed"
        ]
      },
      "post": {
        "operationId": "createBackup",
        "summary": "Back up now. With scheduleRef the schedule's current revision is copied and recorded (uid, generation, runPolicySha256) and a policy field beside it is 422; without it the body is the policy, for a cluster with no schedule yet. A suspended schedule, an invalid cadence, an active Forbid run and a schedule deleted a second later none of them block the run. Readiness is recorded, never obeyed.",
        "parameters": [
          {
            "name": "ns",
            "in": "path",
            "required": true,
            "description": "An explicitly granted namespace. An ungranted namespace is namespace_forbidden whether or not it exists.",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "Idempotency-Key",
            "in": "header",
            "required": true,
            "description": "8 to 128 visible ASCII characters. The object name is derived from (actor, namespace, route, key); a replay of the same request returns 200 with the same object, a different request with the same key is idempotency_conflict.",
            "schema": {
              "type": "string",
              "minLength": 8,
              "maxLength": 128
            }
          },
          {
            "name": "Origin",
            "in": "header",
            "required": true,
            "description": "Must equal the configured public origin exactly.",
            "schema": {
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CreateBackupRequest"
              }
            }
          }
        },
        "responses": {
          "201": {
            "description": "Created; the run and the schedule it came from.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ManualBackupResponse"
                }
              }
            }
          },
          "200": {
            "description": "Replayed an identical earlier request; the same run and the same UID.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ManualBackupResponse"
                }
              }
            }
          },
          "default": {
            "description": "An application/problem+json error; the possible codes are listed in x-logweir-problem-codes.",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          }
        },
        "x-logweir-problem-codes": [
          "forbidden",
          "header_not_allowed",
          "idempotency_conflict",
          "idempotency_key_invalid",
          "idempotency_key_required",
          "internal_error",
          "kubernetes_unavailable",
          "malformed_request",
          "method_not_allowed",
          "misdirected_request",
          "namespace_forbidden",
          "not_found",
          "origin_mismatch",
          "payload_too_large",
          "policy_changed",
          "rate_limited",
          "session_expired",
          "state_conflict",
          "unauthenticated",
          "unsupported_media_type",
          "upstream_timeout",
          "validation_failed"
        ]
      }
    },
    "/api/v1/namespaces/{ns}/backups/{name}": {
      "get": {
        "operationId": "getBackup",
        "summary": "One Backup projection.",
        "parameters": [
          {
            "name": "ns",
            "in": "path",
            "required": true,
            "description": "An explicitly granted namespace. An ungranted namespace is namespace_forbidden whether or not it exists.",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "name",
            "in": "path",
            "required": true,
            "description": "The object name.",
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "The object.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/BackupResponse"
                }
              }
            }
          },
          "default": {
            "description": "An application/problem+json error; the possible codes are listed in x-logweir-problem-codes.",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          }
        },
        "x-logweir-problem-codes": [
          "forbidden",
          "header_not_allowed",
          "internal_error",
          "kubernetes_unavailable",
          "malformed_request",
          "method_not_allowed",
          "misdirected_request",
          "namespace_forbidden",
          "not_found",
          "rate_limited",
          "session_expired",
          "unauthenticated",
          "upstream_timeout"
        ]
      }
    },
    "/api/v1/namespaces/{ns}/restores": {
      "get": {
        "operationId": "listRestores",
        "summary": "Restore projections with a status summary; planBytes omitted.",
        "parameters": [
          {
            "name": "ns",
            "in": "path",
            "required": true,
            "description": "An explicitly granted namespace. An ungranted namespace is namespace_forbidden whether or not it exists.",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "limit",
            "in": "query",
            "required": false,
            "description": "Page size, 1 to 200. Default 50.",
            "schema": {
              "type": "integer",
              "minimum": 1,
              "maximum": 200,
              "default": 50
            }
          },
          {
            "name": "cursor",
            "in": "query",
            "required": false,
            "description": "The opaque nextCursor of the previous page. Bound to actor, route, namespace and filters; expires after 15 minutes.",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "labelSelector",
            "in": "query",
            "required": false,
            "description": "Equality-only label selector, key=value terms separated by commas.",
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "One page.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/RestoreList"
                }
              }
            }
          },
          "default": {
            "description": "An application/problem+json error; the possible codes are listed in x-logweir-problem-codes.",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          }
        },
        "x-logweir-problem-codes": [
          "cursor_expired",
          "cursor_invalid",
          "forbidden",
          "header_not_allowed",
          "internal_error",
          "kubernetes_unavailable",
          "malformed_request",
          "method_not_allowed",
          "misdirected_request",
          "namespace_forbidden",
          "rate_limited",
          "session_expired",
          "unauthenticated",
          "upstream_timeout",
          "validation_failed"
        ]
      },
      "post": {
        "operationId": "createRestore",
        "summary": "Create a Restore. planBytes is opaque, checked against planHash and stored unchanged.",
        "parameters": [
          {
            "name": "ns",
            "in": "path",
            "required": true,
            "description": "An explicitly granted namespace. An ungranted namespace is namespace_forbidden whether or not it exists.",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "Idempotency-Key",
            "in": "header",
            "required": true,
            "description": "8 to 128 visible ASCII characters. The object name is derived from (actor, namespace, route, key); a replay of the same request returns 200 with the same object, a different request with the same key is idempotency_conflict.",
            "schema": {
              "type": "string",
              "minLength": 8,
              "maxLength": 128
            }
          },
          {
            "name": "Origin",
            "in": "header",
            "required": true,
            "description": "Must equal the configured public origin exactly.",
            "schema": {
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CreateRestoreRequest"
              }
            }
          }
        },
        "responses": {
          "201": {
            "description": "Created.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/RestoreResponse"
                }
              }
            }
          },
          "200": {
            "description": "Replayed an identical earlier request; the same object.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/RestoreResponse"
                }
              }
            }
          },
          "default": {
            "description": "An application/problem+json error; the possible codes are listed in x-logweir-problem-codes.",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          }
        },
        "x-logweir-problem-codes": [
          "forbidden",
          "header_not_allowed",
          "idempotency_conflict",
          "idempotency_key_invalid",
          "idempotency_key_required",
          "internal_error",
          "kubernetes_unavailable",
          "malformed_request",
          "method_not_allowed",
          "misdirected_request",
          "namespace_forbidden",
          "origin_mismatch",
          "payload_too_large",
          "rate_limited",
          "session_expired",
          "state_conflict",
          "unauthenticated",
          "unsupported_media_type",
          "upstream_timeout",
          "validation_failed"
        ]
      }
    },
    "/api/v1/namespaces/{ns}/restores/{name}": {
      "get": {
        "operationId": "getRestore",
        "summary": "One Restore projection, planBytes included.",
        "parameters": [
          {
            "name": "ns",
            "in": "path",
            "required": true,
            "description": "An explicitly granted namespace. An ungranted namespace is namespace_forbidden whether or not it exists.",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "name",
            "in": "path",
            "required": true,
            "description": "The object name.",
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "The object.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/RestoreResponse"
                }
              }
            }
          },
          "default": {
            "description": "An application/problem+json error; the possible codes are listed in x-logweir-problem-codes.",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          }
        },
        "x-logweir-problem-codes": [
          "forbidden",
          "header_not_allowed",
          "internal_error",
          "kubernetes_unavailable",
          "malformed_request",
          "method_not_allowed",
          "misdirected_request",
          "namespace_forbidden",
          "not_found",
          "rate_limited",
          "session_expired",
          "unauthenticated",
          "upstream_timeout"
        ]
      }
    },
    "/api/v1/namespaces/{ns}/approvals": {
      "get": {
        "operationId": "listApprovals",
        "summary": "Approval metadata; no documents.",
        "parameters": [
          {
            "name": "ns",
            "in": "path",
            "required": true,
            "description": "An explicitly granted namespace. An ungranted namespace is namespace_forbidden whether or not it exists.",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "limit",
            "in": "query",
            "required": false,
            "description": "Page size, 1 to 200. Default 50.",
            "schema": {
              "type": "integer",
              "minimum": 1,
              "maximum": 200,
              "default": 50
            }
          },
          {
            "name": "cursor",
            "in": "query",
            "required": false,
            "description": "The opaque nextCursor of the previous page. Bound to actor, route, namespace and filters; expires after 15 minutes.",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "labelSelector",
            "in": "query",
            "required": false,
            "description": "Equality-only label selector, key=value terms separated by commas.",
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "One page.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ApprovalList"
                }
              }
            }
          },
          "default": {
            "description": "An application/problem+json error; the possible codes are listed in x-logweir-problem-codes.",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          }
        },
        "x-logweir-problem-codes": [
          "cursor_expired",
          "cursor_invalid",
          "forbidden",
          "header_not_allowed",
          "internal_error",
          "kubernetes_unavailable",
          "malformed_request",
          "method_not_allowed",
          "misdirected_request",
          "namespace_forbidden",
          "rate_limited",
          "session_expired",
          "unauthenticated",
          "upstream_timeout",
          "validation_failed"
        ]
      }
    },
    "/api/v1/namespaces/{ns}/approvals/{name}": {
      "get": {
        "operationId": "getApproval",
        "summary": "One Approval's metadata; no documents.",
        "parameters": [
          {
            "name": "ns",
            "in": "path",
            "required": true,
            "description": "An explicitly granted namespace. An ungranted namespace is namespace_forbidden whether or not it exists.",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "name",
            "in": "path",
            "required": true,
            "description": "The object name.",
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "The object.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ApprovalResponse"
                }
              }
            }
          },
          "default": {
            "description": "An application/problem+json error; the possible codes are listed in x-logweir-problem-codes.",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          }
        },
        "x-logweir-problem-codes": [
          "forbidden",
          "header_not_allowed",
          "internal_error",
          "kubernetes_unavailable",
          "malformed_request",
          "method_not_allowed",
          "misdirected_request",
          "namespace_forbidden",
          "not_found",
          "rate_limited",
          "session_expired",
          "unauthenticated",
          "upstream_timeout"
        ]
      }
    },
    "/api/v1/namespaces/{ns}/approvals/{name}/packet": {
      "get": {
        "operationId": "getApprovalPacket",
        "summary": "The approval and sidecar documents, verbatim.",
        "parameters": [
          {
            "name": "ns",
            "in": "path",
            "required": true,
            "description": "An explicitly granted namespace. An ungranted namespace is namespace_forbidden whether or not it exists.",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "name",
            "in": "path",
            "required": true,
            "description": "The object name.",
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "The object.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ApprovalPacketResponse"
                }
              }
            }
          },
          "default": {
            "description": "An application/problem+json error; the possible codes are listed in x-logweir-problem-codes.",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          }
        },
        "x-logweir-problem-codes": [
          "forbidden",
          "header_not_allowed",
          "internal_error",
          "kubernetes_unavailable",
          "malformed_request",
          "method_not_allowed",
          "misdirected_request",
          "namespace_forbidden",
          "not_found",
          "rate_limited",
          "session_expired",
          "unauthenticated",
          "upstream_timeout"
        ]
      }
    },
    "/api/v1/namespaces/{ns}/destinations": {
      "get": {
        "operationId": "listDestinations",
        "summary": "Saved destinations. Credential REFERENCES only: a Secret name and the key names inside it, never a value.",
        "parameters": [
          {
            "name": "ns",
            "in": "path",
            "required": true,
            "description": "An explicitly granted namespace. An ungranted namespace is namespace_forbidden whether or not it exists.",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "limit",
            "in": "query",
            "required": false,
            "description": "Page size, 1 to 200. Default 50.",
            "schema": {
              "type": "integer",
              "minimum": 1,
              "maximum": 200,
              "default": 50
            }
          },
          {
            "name": "cursor",
            "in": "query",
            "required": false,
            "description": "The opaque nextCursor of the previous page. Bound to actor, route, namespace and filters; expires after 15 minutes.",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "labelSelector",
            "in": "query",
            "required": false,
            "description": "Equality-only label selector, key=value terms separated by commas.",
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "One page.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/DestinationList"
                }
              }
            }
          },
          "default": {
            "description": "An application/problem+json error; the possible codes are listed in x-logweir-problem-codes.",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          }
        },
        "x-logweir-problem-codes": [
          "cursor_expired",
          "cursor_invalid",
          "forbidden",
          "header_not_allowed",
          "internal_error",
          "kubernetes_unavailable",
          "malformed_request",
          "method_not_allowed",
          "misdirected_request",
          "namespace_forbidden",
          "rate_limited",
          "session_expired",
          "unauthenticated",
          "upstream_timeout",
          "validation_failed"
        ]
      },
      "post": {
        "operationId": "createDestination",
        "summary": "Create a BackupDestination. The object's name is the `name` field, because every schedule, backup and restore references a destination by name. A grant may carry a credential VALUE once in access.*.secret.new; it becomes a Secret owned by the destination and is never read back or echoed.",
        "parameters": [
          {
            "name": "ns",
            "in": "path",
            "required": true,
            "description": "An explicitly granted namespace. An ungranted namespace is namespace_forbidden whether or not it exists.",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "Idempotency-Key",
            "in": "header",
            "required": true,
            "description": "8 to 128 visible ASCII characters. The object name is derived from (actor, namespace, route, key); a replay of the same request returns 200 with the same object, a different request with the same key is idempotency_conflict.",
            "schema": {
              "type": "string",
              "minLength": 8,
              "maxLength": 128
            }
          },
          {
            "name": "Origin",
            "in": "header",
            "required": true,
            "description": "Must equal the configured public origin exactly.",
            "schema": {
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CreateDestinationRequest"
              }
            }
          }
        },
        "responses": {
          "201": {
            "description": "Created.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/DestinationResponse"
                }
              }
            }
          },
          "200": {
            "description": "Replayed an identical earlier request; the same object.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/DestinationResponse"
                }
              }
            }
          },
          "default": {
            "description": "An application/problem+json error; the possible codes are listed in x-logweir-problem-codes.",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          }
        },
        "x-logweir-problem-codes": [
          "destination_invalid",
          "destination_location_immutable",
          "forbidden",
          "header_not_allowed",
          "idempotency_conflict",
          "idempotency_key_invalid",
          "idempotency_key_required",
          "internal_error",
          "kubernetes_unavailable",
          "malformed_request",
          "method_not_allowed",
          "misdirected_request",
          "namespace_forbidden",
          "origin_mismatch",
          "payload_too_large",
          "rate_limited",
          "session_expired",
          "state_conflict",
          "transport_downgrade_forbidden",
          "unauthenticated",
          "unsupported_media_type",
          "upstream_timeout",
          "validation_failed"
        ]
      }
    },
    "/api/v1/namespaces/{ns}/destinations:from-legacy": {
      "post": {
        "operationId": "createDestinationFromLegacy",
        "summary": "Adopt the location of a legacy BackupSchedule or Backup as a BackupDestination. THIS BUILD ALWAYS REFUSES with legacy_location_unknown: a legacy archive.url carries the bucket and the prefix and nothing else, and the two sources that record the endpoint, region, addressing and transport — the frozen execution inputs of a succeeded Backup and the installation policy ConfigMap — are not readable here. Guessing them would move the location, and locationDigest is computed over exactly those fields. Create the destination explicitly until W11 lands. Existing objects are never mutated.",
        "parameters": [
          {
            "name": "ns",
            "in": "path",
            "required": true,
            "description": "An explicitly granted namespace. An ungranted namespace is namespace_forbidden whether or not it exists.",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "Idempotency-Key",
            "in": "header",
            "required": true,
            "description": "8 to 128 visible ASCII characters. The object name is derived from (actor, namespace, route, key); a replay of the same request returns 200 with the same object, a different request with the same key is idempotency_conflict.",
            "schema": {
              "type": "string",
              "minLength": 8,
              "maxLength": 128
            }
          },
          {
            "name": "Origin",
            "in": "header",
            "required": true,
            "description": "Must equal the configured public origin exactly.",
            "schema": {
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/DestinationFromLegacyRequest"
              }
            }
          }
        },
        "responses": {
          "201": {
            "description": "Created. No source this build can read produces one yet; see the summary.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/DestinationResponse"
                }
              }
            }
          },
          "200": {
            "description": "Replayed an identical earlier request.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/DestinationResponse"
                }
              }
            }
          },
          "default": {
            "description": "An application/problem+json error; the possible codes are listed in x-logweir-problem-codes.",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          }
        },
        "x-logweir-problem-codes": [
          "destination_invalid",
          "destination_location_immutable",
          "forbidden",
          "header_not_allowed",
          "idempotency_conflict",
          "idempotency_key_invalid",
          "idempotency_key_required",
          "internal_error",
          "kubernetes_unavailable",
          "legacy_location_mismatch",
          "legacy_location_unknown",
          "malformed_request",
          "method_not_allowed",
          "misdirected_request",
          "namespace_forbidden",
          "not_found",
          "origin_mismatch",
          "payload_too_large",
          "rate_limited",
          "session_expired",
          "state_conflict",
          "transport_downgrade_forbidden",
          "unauthenticated",
          "unsupported_media_type",
          "upstream_timeout",
          "validation_failed"
        ]
      }
    },
    "/api/v1/namespaces/{ns}/destinations/{name}": {
      "get": {
        "operationId": "getDestination",
        "summary": "One destination, with the last explicit access test.",
        "parameters": [
          {
            "name": "ns",
            "in": "path",
            "required": true,
            "description": "An explicitly granted namespace. An ungranted namespace is namespace_forbidden whether or not it exists.",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "name",
            "in": "path",
            "required": true,
            "description": "The object name.",
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "The object.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/DestinationResponse"
                }
              }
            }
          },
          "default": {
            "description": "An application/problem+json error; the possible codes are listed in x-logweir-problem-codes.",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          }
        },
        "x-logweir-problem-codes": [
          "forbidden",
          "header_not_allowed",
          "internal_error",
          "kubernetes_unavailable",
          "malformed_request",
          "method_not_allowed",
          "misdirected_request",
          "namespace_forbidden",
          "not_found",
          "rate_limited",
          "session_expired",
          "unauthenticated",
          "upstream_timeout"
        ]
      }
    },
    "/api/v1/namespaces/{ns}/destinations/{name}:update-access": {
      "post": {
        "operationId": "updateDestinationAccess",
        "summary": "Rotate the four grants and the CA reference under an expectedGeneration precondition. It cannot name the location or the transport, and it refuses Idempotency-Key. A grant carrying a credential VALUE whose deterministic Secret already exists is state_conflict: this service holds `create` on Secrets and nothing else, so the value was NOT written and nothing was changed.",
        "parameters": [
          {
            "name": "ns",
            "in": "path",
            "required": true,
            "description": "An explicitly granted namespace. An ungranted namespace is namespace_forbidden whether or not it exists.",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "name",
            "in": "path",
            "required": true,
            "description": "The object name.",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "Origin",
            "in": "header",
            "required": true,
            "description": "Must equal the configured public origin exactly.",
            "schema": {
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/UpdateDestinationAccessRequest"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "The rotated destination.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/DestinationResponse"
                }
              }
            }
          },
          "default": {
            "description": "An application/problem+json error; the possible codes are listed in x-logweir-problem-codes.",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          }
        },
        "x-logweir-problem-codes": [
          "destination_invalid",
          "destination_location_immutable",
          "forbidden",
          "header_not_allowed",
          "idempotency_key_invalid",
          "internal_error",
          "kubernetes_unavailable",
          "malformed_request",
          "method_not_allowed",
          "misdirected_request",
          "namespace_forbidden",
          "not_found",
          "origin_mismatch",
          "payload_too_large",
          "precondition_failed",
          "rate_limited",
          "session_expired",
          "state_conflict",
          "transport_downgrade_forbidden",
          "unauthenticated",
          "unsupported_media_type",
          "upstream_timeout",
          "validation_failed"
        ]
      }
    },
    "/api/v1/namespaces/{ns}/destinations/{name}:test": {
      "post": {
        "operationId": "testDestination",
        "summary": "Start a DestinationAccess Preflight for this destination and answer 202 with it. Absent roles means every role the destination configures.",
        "parameters": [
          {
            "name": "ns",
            "in": "path",
            "required": true,
            "description": "An explicitly granted namespace. An ungranted namespace is namespace_forbidden whether or not it exists.",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "name",
            "in": "path",
            "required": true,
            "description": "The object name.",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "Idempotency-Key",
            "in": "header",
            "required": true,
            "description": "8 to 128 visible ASCII characters. The object name is derived from (actor, namespace, route, key); a replay of the same request returns 200 with the same object, a different request with the same key is idempotency_conflict.",
            "schema": {
              "type": "string",
              "minLength": 8,
              "maxLength": 128
            }
          },
          {
            "name": "Origin",
            "in": "header",
            "required": true,
            "description": "Must equal the configured public origin exactly.",
            "schema": {
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/TestDestinationRequest"
              }
            }
          }
        },
        "responses": {
          "202": {
            "description": "The started test.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PreflightResponse"
                }
              }
            }
          },
          "200": {
            "description": "Replayed an identical earlier request.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PreflightResponse"
                }
              }
            }
          },
          "default": {
            "description": "An application/problem+json error; the possible codes are listed in x-logweir-problem-codes.",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          }
        },
        "x-logweir-problem-codes": [
          "forbidden",
          "header_not_allowed",
          "idempotency_conflict",
          "idempotency_key_invalid",
          "idempotency_key_required",
          "internal_error",
          "kubernetes_unavailable",
          "malformed_request",
          "method_not_allowed",
          "misdirected_request",
          "namespace_forbidden",
          "not_found",
          "origin_mismatch",
          "payload_too_large",
          "rate_limited",
          "session_expired",
          "state_conflict",
          "unauthenticated",
          "unsupported_media_type",
          "upstream_timeout",
          "validation_failed"
        ]
      }
    },
    "/api/v1/namespaces/{ns}/destinations/{name}/usage": {
      "get": {
        "operationId": "getDestinationUsage",
        "summary": "Schedules and backups labelled for this destination, bounded to 100 each. `basis` states how the lists were built, so an empty answer is not read as \"nothing uses this\".",
        "parameters": [
          {
            "name": "ns",
            "in": "path",
            "required": true,
            "description": "An explicitly granted namespace. An ungranted namespace is namespace_forbidden whether or not it exists.",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "name",
            "in": "path",
            "required": true,
            "description": "The object name.",
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "The bounded usage.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/DestinationUsageResponse"
                }
              }
            }
          },
          "default": {
            "description": "An application/problem+json error; the possible codes are listed in x-logweir-problem-codes.",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          }
        },
        "x-logweir-problem-codes": [
          "forbidden",
          "header_not_allowed",
          "internal_error",
          "kubernetes_unavailable",
          "malformed_request",
          "method_not_allowed",
          "misdirected_request",
          "namespace_forbidden",
          "not_found",
          "rate_limited",
          "session_expired",
          "unauthenticated",
          "upstream_timeout"
        ]
      }
    },
    "/api/v1/namespaces/{ns}/connections/{name}/topic-discoveries": {
      "get": {
        "operationId": "listConnectionTopicDiscoveries",
        "summary": "Discoveries for one connection. With latest=true it answers the two slots instead of a page: a failed attempt never hides the last successful inventory, and a successful inventory never hides that the newest attempt failed.",
        "parameters": [
          {
            "name": "ns",
            "in": "path",
            "required": true,
            "description": "An explicitly granted namespace. An ungranted namespace is namespace_forbidden whether or not it exists.",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "name",
            "in": "path",
            "required": true,
            "description": "The object name.",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "limit",
            "in": "query",
            "required": false,
            "description": "Page size, 1 to 200. Default 50.",
            "schema": {
              "type": "integer",
              "minimum": 1,
              "maximum": 200,
              "default": 50
            }
          },
          {
            "name": "cursor",
            "in": "query",
            "required": false,
            "description": "The opaque nextCursor of the previous page. Bound to actor, route, namespace and filters; expires after 15 minutes.",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "labelSelector",
            "in": "query",
            "required": false,
            "description": "Equality-only label selector, key=value terms separated by commas.",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "latest",
            "in": "query",
            "required": false,
            "description": "true returns {latestAttempt, lastSuccessful} instead of a page.",
            "schema": {
              "type": "boolean"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "One page, or the two slots with latest=true.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/TopicDiscoveryList"
                }
              }
            }
          },
          "default": {
            "description": "An application/problem+json error; the possible codes are listed in x-logweir-problem-codes.",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          }
        },
        "x-logweir-problem-codes": [
          "cursor_expired",
          "cursor_invalid",
          "forbidden",
          "header_not_allowed",
          "internal_error",
          "kubernetes_unavailable",
          "malformed_request",
          "method_not_allowed",
          "misdirected_request",
          "namespace_forbidden",
          "not_found",
          "rate_limited",
          "session_expired",
          "unauthenticated",
          "upstream_timeout",
          "validation_failed"
        ]
      },
      "post": {
        "operationId": "createTopicDiscovery",
        "summary": "Start a bounded topic inventory. 202 with the new check, or 200 with reused=true when a fresh succeeded discovery with the same parameters and connection binding exists and reuseFresh is set. Rate limited per actor and namespace.",
        "parameters": [
          {
            "name": "ns",
            "in": "path",
            "required": true,
            "description": "An explicitly granted namespace. An ungranted namespace is namespace_forbidden whether or not it exists.",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "name",
            "in": "path",
            "required": true,
            "description": "The object name.",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "Idempotency-Key",
            "in": "header",
            "required": true,
            "description": "8 to 128 visible ASCII characters. The object name is derived from (actor, namespace, route, key); a replay of the same request returns 200 with the same object, a different request with the same key is idempotency_conflict.",
            "schema": {
              "type": "string",
              "minLength": 8,
              "maxLength": 128
            }
          },
          {
            "name": "Origin",
            "in": "header",
            "required": true,
            "description": "Must equal the configured public origin exactly.",
            "schema": {
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CreateTopicDiscoveryRequest"
              }
            }
          }
        },
        "responses": {
          "202": {
            "description": "Accepted; the check has been created.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/TopicDiscoveryResponse"
                }
              }
            }
          },
          "200": {
            "description": "A replay, or a reused fresh result.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/TopicDiscoveryResponse"
                }
              }
            }
          },
          "default": {
            "description": "An application/problem+json error; the possible codes are listed in x-logweir-problem-codes.",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          }
        },
        "x-logweir-problem-codes": [
          "forbidden",
          "header_not_allowed",
          "idempotency_conflict",
          "idempotency_key_invalid",
          "idempotency_key_required",
          "internal_error",
          "kubernetes_unavailable",
          "malformed_request",
          "method_not_allowed",
          "misdirected_request",
          "namespace_forbidden",
          "not_found",
          "origin_mismatch",
          "payload_too_large",
          "rate_limited",
          "session_expired",
          "state_conflict",
          "unauthenticated",
          "unsupported_media_type",
          "upstream_timeout",
          "validation_failed"
        ]
      }
    },
    "/api/v1/namespaces/{ns}/topic-discoveries/{id}": {
      "get": {
        "operationId": "getTopicDiscovery",
        "summary": "One discovery. `visibility` is never better than unknown without an explicit attestation: a successful Kafka list alone says nothing about whether it was whole. `stale` is recomputed per read against the connection as it is now.",
        "parameters": [
          {
            "name": "ns",
            "in": "path",
            "required": true,
            "description": "An explicitly granted namespace. An ungranted namespace is namespace_forbidden whether or not it exists.",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "id",
            "in": "path",
            "required": true,
            "description": "The check's id, which is its object name.",
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "The discovery.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/TopicDiscoveryResponse"
                }
              }
            }
          },
          "default": {
            "description": "An application/problem+json error; the possible codes are listed in x-logweir-problem-codes.",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          }
        },
        "x-logweir-problem-codes": [
          "forbidden",
          "header_not_allowed",
          "internal_error",
          "kubernetes_unavailable",
          "malformed_request",
          "method_not_allowed",
          "misdirected_request",
          "namespace_forbidden",
          "not_found",
          "rate_limited",
          "session_expired",
          "unauthenticated",
          "upstream_timeout"
        ]
      }
    },
    "/api/v1/namespaces/{ns}/topic-discoveries/{id}:cancel": {
      "post": {
        "operationId": "cancelTopicDiscovery",
        "summary": "Ask an unfinished discovery to stop. Idempotent: repeating it is 200, and cancelling a finished check is 200 with alreadyTerminal. An operator may cancel only the checks it started; an administrator of the namespace may cancel any. Nothing is deleted. Idempotency-Key is refused.",
        "parameters": [
          {
            "name": "ns",
            "in": "path",
            "required": true,
            "description": "An explicitly granted namespace. An ungranted namespace is namespace_forbidden whether or not it exists.",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "id",
            "in": "path",
            "required": true,
            "description": "The check's id, which is its object name.",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "Origin",
            "in": "header",
            "required": true,
            "description": "Must equal the configured public origin exactly.",
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "The state after the request.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CancelResponse"
                }
              }
            }
          },
          "default": {
            "description": "An application/problem+json error; the possible codes are listed in x-logweir-problem-codes.",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          }
        },
        "x-logweir-problem-codes": [
          "forbidden",
          "header_not_allowed",
          "idempotency_key_invalid",
          "internal_error",
          "kubernetes_unavailable",
          "malformed_request",
          "method_not_allowed",
          "misdirected_request",
          "namespace_forbidden",
          "not_found",
          "origin_mismatch",
          "payload_too_large",
          "precondition_failed",
          "rate_limited",
          "session_expired",
          "state_conflict",
          "unauthenticated",
          "unsupported_media_type",
          "upstream_timeout",
          "validation_failed"
        ]
      }
    },
    "/api/v1/namespaces/{ns}/topic-discoveries/{id}/topics": {
      "get": {
        "operationId": "listDiscoveredTopics",
        "summary": "One page of a stored inventory, read from the immutable result chunks the controller committed. Each chunk is verified by owner UID, immutability and digest before a row is served. At most eight chunks are read per request: a sparse q returns a short page with scan.complete=false and a cursor.",
        "parameters": [
          {
            "name": "ns",
            "in": "path",
            "required": true,
            "description": "An explicitly granted namespace. An ungranted namespace is namespace_forbidden whether or not it exists.",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "id",
            "in": "path",
            "required": true,
            "description": "The check's id, which is its object name.",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "limit",
            "in": "query",
            "required": false,
            "description": "Page size, 1 to 200. Default 50.",
            "schema": {
              "type": "integer",
              "minimum": 1,
              "maximum": 200,
              "default": 50
            }
          },
          {
            "name": "cursor",
            "in": "query",
            "required": false,
            "description": "The opaque nextCursor of the previous page, bound to the actor, the discovery UID, the inventory digest and the filters.",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "q",
            "in": "query",
            "required": false,
            "description": "Case-insensitive substring of the topic name.",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "prefix",
            "in": "query",
            "required": false,
            "description": "Name prefix. Whole chunks outside it are skipped without being read.",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "internal",
            "in": "query",
            "required": false,
            "description": "include or exclude (default). Only meaningful when internal topics were stored.",
            "schema": {
              "type": "string",
              "enum": [
                "include",
                "exclude"
              ]
            }
          },
          {
            "name": "errored",
            "in": "query",
            "required": false,
            "description": "include (default), exclude or only.",
            "schema": {
              "type": "string",
              "enum": [
                "include",
                "exclude",
                "only"
              ]
            }
          }
        ],
        "responses": {
          "200": {
            "description": "One page, in stored (bytewise name) order.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/TopicPageResponse"
                }
              }
            }
          },
          "default": {
            "description": "An application/problem+json error; the possible codes are listed in x-logweir-problem-codes.",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          }
        },
        "x-logweir-problem-codes": [
          "cursor_expired",
          "cursor_invalid",
          "forbidden",
          "header_not_allowed",
          "internal_error",
          "kubernetes_unavailable",
          "malformed_request",
          "method_not_allowed",
          "misdirected_request",
          "namespace_forbidden",
          "not_found",
          "rate_limited",
          "result_integrity_failed",
          "session_expired",
          "unauthenticated",
          "upstream_timeout",
          "validation_failed"
        ]
      }
    },
    "/api/v1/namespaces/{ns}/preflights": {
      "post": {
        "operationId": "createPreflight",
        "summary": "Start a readiness check for a Backup, a Restore or a destination's grants. planBytes is opaque: planHash is checked against the SHA-256 of exactly those bytes and they are stored unchanged. The verdict is advisory and every execution-time guard still runs.",
        "parameters": [
          {
            "name": "ns",
            "in": "path",
            "required": true,
            "description": "An explicitly granted namespace. An ungranted namespace is namespace_forbidden whether or not it exists.",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "Idempotency-Key",
            "in": "header",
            "required": true,
            "description": "8 to 128 visible ASCII characters. The object name is derived from (actor, namespace, route, key); a replay of the same request returns 200 with the same object, a different request with the same key is idempotency_conflict.",
            "schema": {
              "type": "string",
              "minLength": 8,
              "maxLength": 128
            }
          },
          {
            "name": "Origin",
            "in": "header",
            "required": true,
            "description": "Must equal the configured public origin exactly.",
            "schema": {
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CreatePreflightRequest"
              }
            }
          }
        },
        "responses": {
          "202": {
            "description": "Accepted; the check has been created.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PreflightResponse"
                }
              }
            }
          },
          "200": {
            "description": "Replayed an identical earlier request.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PreflightResponse"
                }
              }
            }
          },
          "default": {
            "description": "An application/problem+json error; the possible codes are listed in x-logweir-problem-codes.",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          }
        },
        "x-logweir-problem-codes": [
          "forbidden",
          "header_not_allowed",
          "idempotency_conflict",
          "idempotency_key_invalid",
          "idempotency_key_required",
          "internal_error",
          "kubernetes_unavailable",
          "malformed_request",
          "method_not_allowed",
          "misdirected_request",
          "namespace_forbidden",
          "not_found",
          "origin_mismatch",
          "payload_too_large",
          "rate_limited",
          "session_expired",
          "state_conflict",
          "unauthenticated",
          "unsupported_media_type",
          "upstream_timeout",
          "validation_failed"
        ]
      }
    },
    "/api/v1/namespaces/{ns}/preflights/{id}": {
      "get": {
        "operationId": "getPreflight",
        "summary": "One readiness result. `applicable` and `stale` are recomputed on every read against the planHash the caller sends, so an edited plan, a replaced referent or an expired result is never rendered as current readiness. An actor bound only as Approver reads Restore results and nothing else.",
        "parameters": [
          {
            "name": "ns",
            "in": "path",
            "required": true,
            "description": "An explicitly granted namespace. An ungranted namespace is namespace_forbidden whether or not it exists.",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "id",
            "in": "path",
            "required": true,
            "description": "The check's id, which is its object name.",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "planHash",
            "in": "query",
            "required": false,
            "description": "sha256:<64 lowercase hex> of the plan the caller is looking at now. A result bound to a different hash is stale.",
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "The result.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PreflightResponse"
                }
              }
            }
          },
          "default": {
            "description": "An application/problem+json error; the possible codes are listed in x-logweir-problem-codes.",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          }
        },
        "x-logweir-problem-codes": [
          "forbidden",
          "header_not_allowed",
          "internal_error",
          "kubernetes_unavailable",
          "malformed_request",
          "method_not_allowed",
          "misdirected_request",
          "namespace_forbidden",
          "not_found",
          "rate_limited",
          "session_expired",
          "unauthenticated",
          "upstream_timeout",
          "validation_failed"
        ]
      }
    },
    "/api/v1/namespaces/{ns}/preflights/{id}:cancel": {
      "post": {
        "operationId": "cancelPreflight",
        "summary": "Ask an unfinished preflight to stop. Same rules as a discovery cancel.",
        "parameters": [
          {
            "name": "ns",
            "in": "path",
            "required": true,
            "description": "An explicitly granted namespace. An ungranted namespace is namespace_forbidden whether or not it exists.",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "id",
            "in": "path",
            "required": true,
            "description": "The check's id, which is its object name.",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "Origin",
            "in": "header",
            "required": true,
            "description": "Must equal the configured public origin exactly.",
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "The state after the request.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CancelResponse"
                }
              }
            }
          },
          "default": {
            "description": "An application/problem+json error; the possible codes are listed in x-logweir-problem-codes.",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          }
        },
        "x-logweir-problem-codes": [
          "forbidden",
          "header_not_allowed",
          "idempotency_key_invalid",
          "internal_error",
          "kubernetes_unavailable",
          "malformed_request",
          "method_not_allowed",
          "misdirected_request",
          "namespace_forbidden",
          "not_found",
          "origin_mismatch",
          "payload_too_large",
          "precondition_failed",
          "rate_limited",
          "session_expired",
          "state_conflict",
          "unauthenticated",
          "unsupported_media_type",
          "upstream_timeout",
          "validation_failed"
        ]
      }
    },
    "/api/v1/namespaces/{ns}/preflights/{id}/details": {
      "get": {
        "operationId": "listPreflightDetails",
        "summary": "One page of a preflight's detail document, verified by owner UID, immutability and digest before a row is served.",
        "parameters": [
          {
            "name": "ns",
            "in": "path",
            "required": true,
            "description": "An explicitly granted namespace. An ungranted namespace is namespace_forbidden whether or not it exists.",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "id",
            "in": "path",
            "required": true,
            "description": "The check's id, which is its object name.",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "check",
            "in": "query",
            "required": false,
            "description": "Only entries the producer attributed to this check id.",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "limit",
            "in": "query",
            "required": false,
            "description": "Page size, 1 to 200. Default 50.",
            "schema": {
              "type": "integer",
              "minimum": 1,
              "maximum": 200,
              "default": 50
            }
          },
          {
            "name": "cursor",
            "in": "query",
            "required": false,
            "description": "The opaque nextCursor of the previous page.",
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "One page, in stored order.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/DetailPageResponse"
                }
              }
            }
          },
          "default": {
            "description": "An application/problem+json error; the possible codes are listed in x-logweir-problem-codes.",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          }
        },
        "x-logweir-problem-codes": [
          "cursor_expired",
          "cursor_invalid",
          "forbidden",
          "header_not_allowed",
          "internal_error",
          "kubernetes_unavailable",
          "malformed_request",
          "method_not_allowed",
          "misdirected_request",
          "namespace_forbidden",
          "not_found",
          "rate_limited",
          "result_integrity_failed",
          "session_expired",
          "unauthenticated",
          "upstream_timeout",
          "validation_failed"
        ]
      }
    },
    "/api/v1/namespaces/{ns}/operations/{kind}/{name}": {
      "get": {
        "operationId": "getOperation",
        "summary": "The normalized status of one Backup, Restore, TopicDiscovery or Preflight. For a backup or a restore the body is an OperationResponse: result and evidence verification are separate, and NotAttempted is never verified success. For a discovery or a preflight it is a CheckOperationResponse, which carries none of those fields because a transient check has none of those facts.",
        "parameters": [
          {
            "name": "ns",
            "in": "path",
            "required": true,
            "description": "An explicitly granted namespace. An ungranted namespace is namespace_forbidden whether or not it exists.",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "kind",
            "in": "path",
            "required": true,
            "description": "backup, restore, discovery or preflight.",
            "schema": {
              "type": "string",
              "enum": [
                "backup",
                "restore",
                "discovery",
                "preflight"
              ]
            }
          },
          {
            "name": "name",
            "in": "path",
            "required": true,
            "description": "The object name.",
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "The normalized durable run (backup, restore), or the normalized transient check (discovery, preflight) — two shapes, because a check has no archive result, no signed evidence and no verification verdict.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/OperationViewResponse"
                }
              }
            }
          },
          "default": {
            "description": "An application/problem+json error; the possible codes are listed in x-logweir-problem-codes.",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          }
        },
        "x-logweir-problem-codes": [
          "forbidden",
          "header_not_allowed",
          "internal_error",
          "kubernetes_unavailable",
          "malformed_request",
          "method_not_allowed",
          "misdirected_request",
          "namespace_forbidden",
          "not_found",
          "rate_limited",
          "session_expired",
          "unauthenticated",
          "upstream_timeout"
        ]
      }
    },
    "/api/v1/namespaces/{ns}/operations/{kind}/{name}/events": {
      "get": {
        "operationId": "streamOperationEvents",
        "summary": "A bounded text/event-stream of one backup's or restore's normalized status. Event types are operation, reset, heartbeat and end; the event id is the object's resourceVersion and Last-Event-ID resumes with a reset snapshot when it is not the version the stream opened at. Every bound is the server's: the connection is closed after 300 s, a heartbeat is sent every 15 s of silence, and concurrent streams per principal per namespace are capped. NO query parameter is accepted, so no token can be placed in the URL; the stream is authenticated by the same session cookie as every other route and authorized once, at subscribe, for operation.read and operation.stream.",
        "parameters": [
          {
            "name": "ns",
            "in": "path",
            "required": true,
            "description": "An explicitly granted namespace. An ungranted namespace is namespace_forbidden whether or not it exists.",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "kind",
            "in": "path",
            "required": true,
            "description": "backup or restore. A transient check has a read route and no stream.",
            "schema": {
              "type": "string",
              "enum": [
                "backup",
                "restore"
              ]
            }
          },
          {
            "name": "name",
            "in": "path",
            "required": true,
            "description": "The object name.",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "Last-Event-ID",
            "in": "header",
            "required": false,
            "description": "The resourceVersion of the last event received. Decimal digits, at most 64 of them; anything else is malformed_request.",
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "The event stream. text/event-stream, not application/json."
          },
          "default": {
            "description": "An application/problem+json error; the possible codes are listed in x-logweir-problem-codes.",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          }
        },
        "x-logweir-problem-codes": [
          "forbidden",
          "header_not_allowed",
          "internal_error",
          "kubernetes_unavailable",
          "malformed_request",
          "method_not_allowed",
          "misdirected_request",
          "namespace_forbidden",
          "not_found",
          "rate_limited",
          "session_expired",
          "unauthenticated",
          "upstream_timeout"
        ]
      }
    },
    "/api/v1/namespaces/{ns}/protection-policies": {
      "get": {
        "operationId": "listProtectionPolicies",
        "summary": "Protection health: the newest point that can actually be recovered from, how availability was decided, the objective, the failed and missed runs, each schedule's OWN readiness beside protection health, and the alert ledger with its delivery state. No sink URL, no routing key and no Secret name: each route publishes its name and which channels it has as booleans.",
        "parameters": [
          {
            "name": "ns",
            "in": "path",
            "required": true,
            "description": "An explicitly granted namespace. An ungranted namespace is namespace_forbidden whether or not it exists.",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "limit",
            "in": "query",
            "required": false,
            "description": "Page size, 1 to 200. Default 50.",
            "schema": {
              "type": "integer",
              "minimum": 1,
              "maximum": 200,
              "default": 50
            }
          },
          {
            "name": "cursor",
            "in": "query",
            "required": false,
            "description": "The opaque nextCursor of the previous page. Bound to actor, route, namespace and filters; expires after 15 minutes.",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "labelSelector",
            "in": "query",
            "required": false,
            "description": "Equality-only label selector, key=value terms separated by commas.",
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "One page of protection policies.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProtectionPolicyList"
                }
              }
            }
          },
          "default": {
            "description": "An application/problem+json error; the possible codes are listed in x-logweir-problem-codes.",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          }
        },
        "x-logweir-problem-codes": [
          "cursor_expired",
          "cursor_invalid",
          "forbidden",
          "header_not_allowed",
          "internal_error",
          "kubernetes_unavailable",
          "malformed_request",
          "method_not_allowed",
          "misdirected_request",
          "namespace_forbidden",
          "rate_limited",
          "session_expired",
          "unauthenticated",
          "upstream_timeout",
          "validation_failed"
        ]
      }
    },
    "/api/v1/namespaces/{ns}/protection-policies/{name}": {
      "get": {
        "operationId": "getProtectionPolicy",
        "summary": "One protection policy. `health` absent means nothing has been evaluated, which is not Healthy.",
        "parameters": [
          {
            "name": "ns",
            "in": "path",
            "required": true,
            "description": "An explicitly granted namespace. An ungranted namespace is namespace_forbidden whether or not it exists.",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "name",
            "in": "path",
            "required": true,
            "description": "The object name.",
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "The policy.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProtectionPolicyResponse"
                }
              }
            }
          },
          "default": {
            "description": "An application/problem+json error; the possible codes are listed in x-logweir-problem-codes.",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          }
        },
        "x-logweir-problem-codes": [
          "forbidden",
          "header_not_allowed",
          "internal_error",
          "kubernetes_unavailable",
          "malformed_request",
          "method_not_allowed",
          "misdirected_request",
          "namespace_forbidden",
          "not_found",
          "rate_limited",
          "session_expired",
          "unauthenticated",
          "upstream_timeout"
        ]
      }
    },
    "/api/v1/namespaces/{ns}/rehearsal-schedules": {
      "get": {
        "operationId": "listRehearsalSchedules",
        "summary": "Recurring recovery rehearsals: the last pass, the last failure and the last SKIP with its reason, plus the leftover topics that block the next slot. The signed standing authorization is named, never carried.",
        "parameters": [
          {
            "name": "ns",
            "in": "path",
            "required": true,
            "description": "An explicitly granted namespace. An ungranted namespace is namespace_forbidden whether or not it exists.",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "limit",
            "in": "query",
            "required": false,
            "description": "Page size, 1 to 200. Default 50.",
            "schema": {
              "type": "integer",
              "minimum": 1,
              "maximum": 200,
              "default": 50
            }
          },
          {
            "name": "cursor",
            "in": "query",
            "required": false,
            "description": "The opaque nextCursor of the previous page. Bound to actor, route, namespace and filters; expires after 15 minutes.",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "labelSelector",
            "in": "query",
            "required": false,
            "description": "Equality-only label selector, key=value terms separated by commas.",
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "One page of rehearsal schedules.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/RehearsalScheduleList"
                }
              }
            }
          },
          "default": {
            "description": "An application/problem+json error; the possible codes are listed in x-logweir-problem-codes.",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          }
        },
        "x-logweir-problem-codes": [
          "cursor_expired",
          "cursor_invalid",
          "forbidden",
          "header_not_allowed",
          "internal_error",
          "kubernetes_unavailable",
          "malformed_request",
          "method_not_allowed",
          "misdirected_request",
          "namespace_forbidden",
          "rate_limited",
          "session_expired",
          "unauthenticated",
          "upstream_timeout",
          "validation_failed"
        ]
      }
    },
    "/api/v1/namespaces/{ns}/rehearsal-schedules/{name}": {
      "get": {
        "operationId": "getRehearsalSchedule",
        "summary": "One rehearsal schedule.",
        "parameters": [
          {
            "name": "ns",
            "in": "path",
            "required": true,
            "description": "An explicitly granted namespace. An ungranted namespace is namespace_forbidden whether or not it exists.",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "name",
            "in": "path",
            "required": true,
            "description": "The object name.",
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "The schedule.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/RehearsalScheduleResponse"
                }
              }
            }
          },
          "default": {
            "description": "An application/problem+json error; the possible codes are listed in x-logweir-problem-codes.",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          }
        },
        "x-logweir-problem-codes": [
          "forbidden",
          "header_not_allowed",
          "internal_error",
          "kubernetes_unavailable",
          "malformed_request",
          "method_not_allowed",
          "misdirected_request",
          "namespace_forbidden",
          "not_found",
          "rate_limited",
          "session_expired",
          "unauthenticated",
          "upstream_timeout"
        ]
      }
    },
    "/api/v1/namespaces/{ns}/catalogs": {
      "get": {
        "operationId": "listCatalogs",
        "summary": "Recovery catalogs: the ten verdict counts, the signer list, whether the Kubernetes view is a WINDOW over a larger archive, and when that window ages out with its sync Job.",
        "parameters": [
          {
            "name": "ns",
            "in": "path",
            "required": true,
            "description": "An explicitly granted namespace. An ungranted namespace is namespace_forbidden whether or not it exists.",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "limit",
            "in": "query",
            "required": false,
            "description": "Page size, 1 to 200. Default 50.",
            "schema": {
              "type": "integer",
              "minimum": 1,
              "maximum": 200,
              "default": 50
            }
          },
          {
            "name": "cursor",
            "in": "query",
            "required": false,
            "description": "The opaque nextCursor of the previous page. Bound to actor, route, namespace and filters; expires after 15 minutes.",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "labelSelector",
            "in": "query",
            "required": false,
            "description": "Equality-only label selector, key=value terms separated by commas.",
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "One page of catalogs.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CatalogList"
                }
              }
            }
          },
          "default": {
            "description": "An application/problem+json error; the possible codes are listed in x-logweir-problem-codes.",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          }
        },
        "x-logweir-problem-codes": [
          "cursor_expired",
          "cursor_invalid",
          "forbidden",
          "header_not_allowed",
          "internal_error",
          "kubernetes_unavailable",
          "malformed_request",
          "method_not_allowed",
          "misdirected_request",
          "namespace_forbidden",
          "rate_limited",
          "session_expired",
          "unauthenticated",
          "upstream_timeout",
          "validation_failed"
        ]
      },
      "post": {
        "operationId": "connectArchive",
        "summary": "Connect an existing archive (PLAT-15.2): create a RecoveryCatalog under the name in the body, because every protection, rehearsal and retention policy references it by that name. The controller owns the object; this route creates it and nothing else. Repeating the request with the same Idempotency-Key returns the same object.",
        "parameters": [
          {
            "name": "ns",
            "in": "path",
            "required": true,
            "description": "An explicitly granted namespace. An ungranted namespace is namespace_forbidden whether or not it exists.",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "Idempotency-Key",
            "in": "header",
            "required": true,
            "description": "8 to 128 visible ASCII characters. The object name is derived from (actor, namespace, route, key); a replay of the same request returns 200 with the same object, a different request with the same key is idempotency_conflict.",
            "schema": {
              "type": "string",
              "minLength": 8,
              "maxLength": 128
            }
          },
          {
            "name": "Origin",
            "in": "header",
            "required": true,
            "description": "Must equal the configured public origin exactly.",
            "schema": {
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/ConnectArchiveRequest"
              }
            }
          }
        },
        "responses": {
          "201": {
            "description": "Created.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CatalogResponse"
                }
              }
            }
          },
          "200": {
            "description": "Replayed an identical earlier request.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CatalogResponse"
                }
              }
            }
          },
          "default": {
            "description": "An application/problem+json error; the possible codes are listed in x-logweir-problem-codes.",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          }
        },
        "x-logweir-problem-codes": [
          "forbidden",
          "header_not_allowed",
          "idempotency_conflict",
          "idempotency_key_invalid",
          "idempotency_key_required",
          "internal_error",
          "kubernetes_unavailable",
          "malformed_request",
          "method_not_allowed",
          "misdirected_request",
          "namespace_forbidden",
          "origin_mismatch",
          "payload_too_large",
          "rate_limited",
          "session_expired",
          "state_conflict",
          "unauthenticated",
          "unsupported_media_type",
          "upstream_timeout",
          "validation_failed"
        ]
      }
    },
    "/api/v1/namespaces/{ns}/catalogs/{name}": {
      "get": {
        "operationId": "getCatalog",
        "summary": "One catalog.",
        "parameters": [
          {
            "name": "ns",
            "in": "path",
            "required": true,
            "description": "An explicitly granted namespace. An ungranted namespace is namespace_forbidden whether or not it exists.",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "name",
            "in": "path",
            "required": true,
            "description": "The object name.",
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "The catalog.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CatalogResponse"
                }
              }
            }
          },
          "default": {
            "description": "An application/problem+json error; the possible codes are listed in x-logweir-problem-codes.",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          }
        },
        "x-logweir-problem-codes": [
          "forbidden",
          "header_not_allowed",
          "internal_error",
          "kubernetes_unavailable",
          "malformed_request",
          "method_not_allowed",
          "misdirected_request",
          "namespace_forbidden",
          "not_found",
          "rate_limited",
          "session_expired",
          "unauthenticated",
          "upstream_timeout"
        ]
      }
    },
    "/api/v1/namespaces/{ns}/catalogs/{name}/points": {
      "get": {
        "operationId": "listCatalogPoints",
        "summary": "One page of the materialised point view, with availability and verification as SEPARATE columns and the controller's own `selectable` conjunction beside them. Pages are read by the names the catalog's status records, at most eight per request, and each one is refused unless it is immutable and its bytes match the digest the status recorded. The cursor is bound to the view generation: a view replaced under a paging client is cursor_invalid with 'restart the list', never a silently different page. An empty list with viewExpired true means the window aged out, not that the archive is empty. incomplete true means a named materialised page disappeared during this read; any rows are a prefix, never proof that omitted rows are absent.",
        "parameters": [
          {
            "name": "ns",
            "in": "path",
            "required": true,
            "description": "An explicitly granted namespace. An ungranted namespace is namespace_forbidden whether or not it exists.",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "name",
            "in": "path",
            "required": true,
            "description": "The object name.",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "limit",
            "in": "query",
            "required": false,
            "description": "Page size, 1 to 200. Default 50.",
            "schema": {
              "type": "integer",
              "minimum": 1,
              "maximum": 200,
              "default": 50
            }
          },
          {
            "name": "cursor",
            "in": "query",
            "required": false,
            "description": "The opaque nextCursor of the previous page.",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "selectable",
            "in": "query",
            "required": false,
            "description": "true returns only points an ordinary restore may select (Available and verified).",
            "schema": {
              "type": "string",
              "enum": [
                "true",
                "false"
              ]
            }
          }
        ],
        "responses": {
          "200": {
            "description": "One page of recovery points.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PointPageResponse"
                }
              }
            }
          },
          "default": {
            "description": "An application/problem+json error; the possible codes are listed in x-logweir-problem-codes.",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          }
        },
        "x-logweir-problem-codes": [
          "cursor_expired",
          "cursor_invalid",
          "forbidden",
          "header_not_allowed",
          "internal_error",
          "kubernetes_unavailable",
          "malformed_request",
          "method_not_allowed",
          "misdirected_request",
          "namespace_forbidden",
          "not_found",
          "rate_limited",
          "result_integrity_failed",
          "session_expired",
          "unauthenticated",
          "upstream_timeout",
          "validation_failed"
        ]
      }
    },
    "/api/v1/namespaces/{ns}/catalogs/{name}/signers": {
      "get": {
        "operationId": "listCatalogSigners",
        "summary": "The untrusted-signer panel: each key id that signed points in this archive, how many, and whether the bound TrustPolicy accepts it — with the out-of-band fingerprint command and NO route that would trust a key. Absent `trusted` means the catalog could not tell, which is not false and is certainly not true.",
        "parameters": [
          {
            "name": "ns",
            "in": "path",
            "required": true,
            "description": "An explicitly granted namespace. An ungranted namespace is namespace_forbidden whether or not it exists.",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "name",
            "in": "path",
            "required": true,
            "description": "The object name.",
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "The signers.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/SignerPageResponse"
                }
              }
            }
          },
          "default": {
            "description": "An application/problem+json error; the possible codes are listed in x-logweir-problem-codes.",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          }
        },
        "x-logweir-problem-codes": [
          "forbidden",
          "header_not_allowed",
          "internal_error",
          "kubernetes_unavailable",
          "malformed_request",
          "method_not_allowed",
          "misdirected_request",
          "namespace_forbidden",
          "not_found",
          "rate_limited",
          "session_expired",
          "unauthenticated",
          "upstream_timeout"
        ]
      }
    },
    "/api/v1/namespaces/{ns}/retention-policies": {
      "get": {
        "operationId": "listRetentionPolicies",
        "summary": "Retention: what the last evaluation would remove, what is ACTUALLY enforcing it (RecommendationOnly, LogweirWorker or ExternalLifecycleDeclared), which guarantees are Logweir-enforced, provider-declared-unverified or not enforced at all, where the approved-plan gate stands, and whether three consecutive failed runs have set EnforcementDegraded. The delete-capable credential is published as a boolean, never as a Secret name.",
        "parameters": [
          {
            "name": "ns",
            "in": "path",
            "required": true,
            "description": "An explicitly granted namespace. An ungranted namespace is namespace_forbidden whether or not it exists.",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "limit",
            "in": "query",
            "required": false,
            "description": "Page size, 1 to 200. Default 50.",
            "schema": {
              "type": "integer",
              "minimum": 1,
              "maximum": 200,
              "default": 50
            }
          },
          {
            "name": "cursor",
            "in": "query",
            "required": false,
            "description": "The opaque nextCursor of the previous page. Bound to actor, route, namespace and filters; expires after 15 minutes.",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "labelSelector",
            "in": "query",
            "required": false,
            "description": "Equality-only label selector, key=value terms separated by commas.",
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "One page of retention policies.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/RetentionPolicyList"
                }
              }
            }
          },
          "default": {
            "description": "An application/problem+json error; the possible codes are listed in x-logweir-problem-codes.",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          }
        },
        "x-logweir-problem-codes": [
          "cursor_expired",
          "cursor_invalid",
          "forbidden",
          "header_not_allowed",
          "internal_error",
          "kubernetes_unavailable",
          "malformed_request",
          "method_not_allowed",
          "misdirected_request",
          "namespace_forbidden",
          "rate_limited",
          "session_expired",
          "unauthenticated",
          "upstream_timeout",
          "validation_failed"
        ]
      }
    },
    "/api/v1/namespaces/{ns}/retention-policies/{name}": {
      "get": {
        "operationId": "getRetentionPolicy",
        "summary": "One retention policy, with the plan preview its last evaluation produced. Nothing in `lastEvaluation` was deleted.",
        "parameters": [
          {
            "name": "ns",
            "in": "path",
            "required": true,
            "description": "An explicitly granted namespace. An ungranted namespace is namespace_forbidden whether or not it exists.",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "name",
            "in": "path",
            "required": true,
            "description": "The object name.",
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "The policy.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/RetentionPolicyResponse"
                }
              }
            }
          },
          "default": {
            "description": "An application/problem+json error; the possible codes are listed in x-logweir-problem-codes.",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          }
        },
        "x-logweir-problem-codes": [
          "forbidden",
          "header_not_allowed",
          "internal_error",
          "kubernetes_unavailable",
          "malformed_request",
          "method_not_allowed",
          "misdirected_request",
          "namespace_forbidden",
          "not_found",
          "rate_limited",
          "session_expired",
          "unauthenticated",
          "upstream_timeout"
        ]
      }
    },
    "/api/v1/trust-policies": {
      "get": {
        "operationId": "listTrustPolicies",
        "summary": "The installation's trust policies: which keys are accepted, for what usage, and how fresh that verdict is. CLUSTER-SCOPED, so it has no namespace and requires the administrator role in at least one bound namespace. A key whose evaluation is not fresh — no status, a status from an older generation, or an evaluatedAt older than 15 minutes by THIS server's clock — is published with effectiveState 'unknown' and no usability verdict: unknown is not valid. No public key bytes and no write route.",
        "parameters": [
          {
            "name": "limit",
            "in": "query",
            "required": false,
            "description": "Page size, 1 to 200. Default 50.",
            "schema": {
              "type": "integer",
              "minimum": 1,
              "maximum": 200,
              "default": 50
            }
          },
          {
            "name": "cursor",
            "in": "query",
            "required": false,
            "description": "The opaque nextCursor of the previous page.",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "labelSelector",
            "in": "query",
            "required": false,
            "description": "Equality-only label selector.",
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "One page of trust policies.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/TrustPolicyList"
                }
              }
            }
          },
          "default": {
            "description": "An application/problem+json error; the possible codes are listed in x-logweir-problem-codes.",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          }
        },
        "x-logweir-problem-codes": [
          "cursor_expired",
          "cursor_invalid",
          "forbidden",
          "header_not_allowed",
          "internal_error",
          "kubernetes_unavailable",
          "malformed_request",
          "method_not_allowed",
          "misdirected_request",
          "rate_limited",
          "session_expired",
          "unauthenticated",
          "upstream_timeout",
          "validation_failed"
        ]
      }
    },
    "/api/v1/trust-policies/{name}": {
      "get": {
        "operationId": "getTrustPolicy",
        "summary": "One trust policy.",
        "parameters": [
          {
            "name": "name",
            "in": "path",
            "required": true,
            "description": "The object name.",
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "The policy.",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/TrustPolicyResponse"
                }
              }
            }
          },
          "default": {
            "description": "An application/problem+json error; the possible codes are listed in x-logweir-problem-codes.",
            "content": {
              "application/problem+json": {
                "schema": {
                  "$ref": "#/components/schemas/Problem"
                }
              }
            }
          }
        },
        "x-logweir-problem-codes": [
          "forbidden",
          "header_not_allowed",
          "internal_error",
          "kubernetes_unavailable",
          "malformed_request",
          "method_not_allowed",
          "misdirected_request",
          "not_found",
          "rate_limited",
          "session_expired",
          "unauthenticated",
          "upstream_timeout"
        ]
      }
    }
  },
  "components": {
    "schemas": {
      "AccessGrantRequest": {
        "description": "One role's grant.",
        "type": "object",
        "required": [
          "mode"
        ],
        "properties": {
          "mode": {
            "description": "How the credential is obtained.",
            "$ref": "#/components/schemas/AccessModeDto"
          },
          "secret": {
            "description": "The Secret, for `secretKeys`.",
            "default": null,
            "$ref": "#/components/schemas/SecretSourceRequest",
            "nullable": true
          },
          "workloadIdentity": {
            "description": "The ServiceAccount, for `workloadIdentity`.",
            "default": null,
            "$ref": "#/components/schemas/WorkloadIdentityRequest",
            "nullable": true
          }
        },
        "additionalProperties": false
      },
      "AccessGrantView": {
        "description": "One role's grant, as stored. NEVER A CREDENTIAL VALUE: a Secret name and the key names inside it, both of which are public references.",
        "type": "object",
        "required": [
          "keys",
          "mode"
        ],
        "properties": {
          "keys": {
            "description": "The data KEY NAMES inside it, sorted.",
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "mode": {
            "description": "How the credential is obtained.",
            "$ref": "#/components/schemas/AccessModeDto"
          },
          "secretName": {
            "description": "The Secret's NAME, for `secretKeys`.",
            "type": "string",
            "nullable": true
          },
          "serviceAccountName": {
            "description": "The ServiceAccount, for `workloadIdentity`.",
            "type": "string",
            "nullable": true
          }
        }
      },
      "AccessModeDto": {
        "description": "How one role's credential is obtained.",
        "oneOf": [
          {
            "description": "Keys read from a Secret in this namespace, projected into the Job.",
            "type": "string",
            "enum": [
              "secretKeys"
            ]
          },
          {
            "description": "The pod's own ServiceAccount identity; no Secret is projected.",
            "type": "string",
            "enum": [
              "workloadIdentity"
            ]
          },
          {
            "description": "The controller's own read-only object-store handle. `evidenceRead` only.",
            "type": "string",
            "enum": [
              "controllerIdentity"
            ]
          },
          {
            "description": "The destination's explicit read-only `archiveRead` grant. `evidenceRead` only.",
            "type": "string",
            "enum": [
              "archiveReadGrant"
            ]
          },
          {
            "description": "RESPONSES ONLY: the grant is absent and `archiveWrite` is used. It is never accepted in a request, because \"absent\" and \"explicitly say the thing absence means\" would then be two spellings of one state.",
            "type": "string",
            "enum": [
              "inheritsArchiveWrite"
            ]
          },
          {
            "description": "RESPONSES ONLY: `evidenceRead` is absent, so verification is `NotAttempted` and this says so rather than leaving a blank field.",
            "type": "string",
            "enum": [
              "notConfigured"
            ]
          }
        ]
      },
      "AccessRequest": {
        "description": "The four grants. Absent is a DEFINED answer and never a wider one: `archiveRead` and `evidenceWrite` absent mean \"use `archiveWrite`\", and `evidenceRead` absent means verification is not attempted and says so.",
        "type": "object",
        "required": [
          "archiveWrite"
        ],
        "properties": {
          "archiveRead": {
            "description": "The read-only archive grant.",
            "default": null,
            "$ref": "#/components/schemas/AccessGrantRequest",
            "nullable": true
          },
          "archiveWrite": {
            "description": "The grant execution Jobs write the archive with. Required.",
            "$ref": "#/components/schemas/AccessGrantRequest"
          },
          "evidenceRead": {
            "description": "The grant that reads evidence back for verification.",
            "default": null,
            "$ref": "#/components/schemas/AccessGrantRequest",
            "nullable": true
          },
          "evidenceWrite": {
            "description": "The grant that writes evidence under `logweir/`.",
            "default": null,
            "$ref": "#/components/schemas/AccessGrantRequest",
            "nullable": true
          }
        },
        "additionalProperties": false
      },
      "AccessView": {
        "description": "The four grants, as stored.",
        "type": "object",
        "required": [
          "archiveRead",
          "archiveWrite",
          "evidenceRead",
          "evidenceWrite"
        ],
        "properties": {
          "archiveRead": {
            "description": "The archive read grant, or `inheritsArchiveWrite`.",
            "$ref": "#/components/schemas/AccessGrantView"
          },
          "archiveWrite": {
            "description": "The archive write grant.",
            "$ref": "#/components/schemas/AccessGrantView"
          },
          "evidenceRead": {
            "description": "The evidence read grant, or `notConfigured`.",
            "$ref": "#/components/schemas/AccessGrantView"
          },
          "evidenceWrite": {
            "description": "The evidence write grant, or `inheritsArchiveWrite`.",
            "$ref": "#/components/schemas/AccessGrantView"
          }
        }
      },
      "AcknowledgedReadiness": {
        "description": "The readiness verdict a person clicked past.",
        "oneOf": [
          {
            "description": "A preflight said a prerequisite was not met and the person ran anyway.",
            "type": "string",
            "enum": [
              "notReady"
            ]
          },
          {
            "description": "Readiness could not be established.",
            "type": "string",
            "enum": [
              "unknown"
            ]
          }
        ]
      },
      "ActiveRunView": {
        "description": "One schedule-created run that is not terminal (D1 §4.8).",
        "type": "object",
        "required": [
          "attempt",
          "kind",
          "name"
        ],
        "properties": {
          "attempt": {
            "description": "0 for a scheduled or catch-up run, 1 to 3 for a retry.",
            "type": "integer",
            "format": "int32"
          },
          "kind": {
            "description": "`Scheduled`, `CatchUp` or `Retry`. A manual run is never here: it is not counted by, and never blocked by, `concurrencyPolicy`.",
            "type": "string"
          },
          "name": {
            "description": "The `Backup`'s name.",
            "type": "string"
          }
        }
      },
      "ActorView": {
        "description": "The authenticated actor.",
        "type": "object",
        "required": [
          "displayName",
          "id",
          "issuer",
          "subject"
        ],
        "properties": {
          "displayName": {
            "description": "A display name. Never used for authorization.",
            "type": "string"
          },
          "id": {
            "description": "The stable actor ID, `<issuer>#<subject>`.",
            "type": "string"
          },
          "issuer": {
            "description": "The identity issuer.",
            "type": "string"
          },
          "subject": {
            "description": "The subject.",
            "type": "string"
          }
        }
      },
      "AddressingDto": {
        "description": "How a request names the bucket.\n\nNEVER A TRANSPORT CHOICE, in either direction. Addressing says how the bucket appears in the URL; `transport.security` alone decides whether the connection is encrypted. This is defect G5 written into the contract.",
        "oneOf": [
          {
            "description": "`https://endpoint/bucket/key`.",
            "type": "string",
            "enum": [
              "pathStyle"
            ]
          },
          {
            "description": "`https://bucket.endpoint/key`.",
            "type": "string",
            "enum": [
              "virtualHosted"
            ]
          }
        ]
      },
      "AlertDeliveryView": {
        "description": "How a transition was delivered, or why it was not.",
        "type": "object",
        "properties": {
          "attempts": {
            "description": "How many attempts.",
            "type": "integer",
            "format": "int64",
            "nullable": true
          },
          "lastAttemptAt": {
            "description": "When the last one was.",
            "type": "string",
            "format": "date-time",
            "nullable": true
          },
          "lastError": {
            "description": "The last error, redacted by the controller and bounded again here. Never a routing key and never a URL.",
            "type": "string",
            "nullable": true
          },
          "state": {
            "description": "`Pending`, `Delivered`, `Failed` or `Suppressed`.",
            "type": "string",
            "nullable": true
          }
        }
      },
      "AlertView": {
        "description": "One entry of the deduplication ledger.",
        "type": "object",
        "required": [
          "key",
          "kind",
          "state"
        ],
        "properties": {
          "delivery": {
            "description": "How that delivery went.",
            "$ref": "#/components/schemas/AlertDeliveryView",
            "nullable": true
          },
          "key": {
            "description": "The stable dedup key.",
            "type": "string"
          },
          "kind": {
            "description": "What it is about.",
            "type": "string"
          },
          "notifiedTransition": {
            "description": "The last transition a delivery was created for. `transition` ahead of this one is an alert whose latest state has not been delivered.",
            "type": "integer",
            "format": "int64",
            "nullable": true
          },
          "openedAt": {
            "description": "When it opened.",
            "type": "string",
            "format": "date-time",
            "nullable": true
          },
          "resolvedAt": {
            "description": "When it resolved.",
            "type": "string",
            "format": "date-time",
            "nullable": true
          },
          "state": {
            "description": "`Open` or `Resolved`.",
            "type": "string"
          },
          "transition": {
            "description": "The transition counter: it increments on open, on resolve and on each re-notify.",
            "type": "integer",
            "format": "int64",
            "nullable": true
          }
        }
      },
      "AllUserTopics": {
        "description": "Dynamic selection: every user topic the run's principal can see, minus the exclusions.",
        "type": "object",
        "required": [
          "incompleteDiscovery"
        ],
        "properties": {
          "exclude": {
            "description": "What to leave out.",
            "default": null,
            "$ref": "#/components/schemas/TopicExclusions",
            "nullable": true
          },
          "incompleteDiscovery": {
            "description": "Required.",
            "$ref": "#/components/schemas/IncompleteDiscoveryPolicy"
          }
        },
        "additionalProperties": false
      },
      "Approval": {
        "description": "Public approval metadata. The documents themselves are only on the packet route.",
        "type": "object",
        "required": [
          "approvalBytesLength",
          "conditions",
          "name",
          "namespace",
          "planHash",
          "resourceVersion",
          "sidecarBytesLength",
          "subjectRef",
          "uid"
        ],
        "properties": {
          "approvalBytesLength": {
            "description": "The approval document's length in bytes.",
            "type": "integer",
            "format": "uint",
            "minimum": 0.0
          },
          "approver": {
            "description": "The approver subject from the roster.",
            "type": "string",
            "nullable": true
          },
          "conditions": {
            "description": "The status conditions, at most 16.",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/ConditionView"
            }
          },
          "createdAt": {
            "description": "When the object was created.",
            "type": "string",
            "format": "date-time",
            "nullable": true
          },
          "matchedKeyId": {
            "description": "The approver key that verified it.",
            "type": "string",
            "nullable": true
          },
          "name": {
            "description": "The object name.",
            "type": "string"
          },
          "namespace": {
            "description": "The namespace.",
            "type": "string"
          },
          "planHash": {
            "description": "The plan hash the create form supplied.",
            "type": "string"
          },
          "resourceVersion": {
            "description": "The resourceVersion.",
            "type": "string"
          },
          "selfAttestedRisk": {
            "description": "Whether approver and requester may be the same principal.",
            "type": "boolean",
            "nullable": true
          },
          "sidecarBytesLength": {
            "description": "The sidecar's length in bytes.",
            "type": "integer",
            "format": "uint",
            "minimum": 0.0
          },
          "subjectRef": {
            "description": "The subject.",
            "$ref": "#/components/schemas/SubjectRefView"
          },
          "ticket": {
            "description": "The change ticket the document names.",
            "type": "string",
            "nullable": true
          },
          "uid": {
            "description": "The Kubernetes UID.",
            "type": "string"
          },
          "verified": {
            "description": "Whether the controller verified the approval.",
            "type": "boolean",
            "nullable": true
          },
          "verifiedSubject": {
            "description": "The exact verified subject.",
            "$ref": "#/components/schemas/VerifiedSubjectView",
            "nullable": true
          }
        }
      },
      "ApprovalList": {
        "type": "object",
        "required": [
          "items",
          "page",
          "requestId"
        ],
        "properties": {
          "items": {
            "description": "The items on this page.",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/Approval"
            }
          },
          "page": {
            "description": "Paging.",
            "$ref": "#/components/schemas/Page"
          },
          "requestId": {
            "description": "The request ID.",
            "type": "string"
          }
        }
      },
      "ApprovalPacket": {
        "description": "`GET .../approvals/{name}/packet`: the raw documents, verbatim.",
        "type": "object",
        "required": [
          "approvalBytes",
          "name",
          "namespace",
          "planHash",
          "sidecarBytes",
          "subjectRef",
          "uid"
        ],
        "properties": {
          "approvalBytes": {
            "description": "The approval document text, verbatim.",
            "type": "string"
          },
          "name": {
            "description": "The object name.",
            "type": "string"
          },
          "namespace": {
            "description": "The namespace.",
            "type": "string"
          },
          "planHash": {
            "description": "The plan hash the create form supplied.",
            "type": "string"
          },
          "sidecarBytes": {
            "description": "The DSSE sidecar text, verbatim.",
            "type": "string"
          },
          "subjectRef": {
            "description": "The subject.",
            "$ref": "#/components/schemas/SubjectRefView"
          },
          "uid": {
            "description": "The Kubernetes UID.",
            "type": "string"
          }
        }
      },
      "ApprovalPacketResponse": {
        "description": "`GET .../approvals/{name}/packet`.",
        "type": "object",
        "required": [
          "item",
          "requestId"
        ],
        "properties": {
          "item": {
            "description": "The packet.",
            "$ref": "#/components/schemas/ApprovalPacket"
          },
          "requestId": {
            "description": "The request ID.",
            "type": "string"
          }
        }
      },
      "ApprovalResponse": {
        "type": "object",
        "required": [
          "item",
          "requestId"
        ],
        "properties": {
          "item": {
            "description": "The item.",
            "$ref": "#/components/schemas/Approval"
          },
          "replayed": {
            "description": "On a durable create: whether this response replays an earlier identical request (HTTP 200) rather than creating (HTTP 201).",
            "type": "boolean",
            "nullable": true
          },
          "requestId": {
            "description": "The request ID.",
            "type": "string"
          }
        }
      },
      "ApprovedPlanState": {
        "description": "Where the approved-plan gate stands.",
        "oneOf": [
          {
            "description": "The policy does not enforce, so there is nothing to approve.",
            "type": "string",
            "enum": [
              "notApplicable"
            ]
          },
          {
            "description": "`requireApprovedPlan: false`. Deletion runs unattended, and the controller records `Enforced/UnattendedDeletionEnabled` so the choice is visible on the object rather than only in a values file.",
            "type": "string",
            "enum": [
              "notRequired"
            ]
          },
          {
            "description": "Enforcement is configured and no evaluation has produced a plan yet.",
            "type": "string",
            "enum": [
              "noPlan"
            ]
          },
          {
            "description": "A plan exists and no approved digest matches it.",
            "type": "string",
            "enum": [
              "awaitingApproval"
            ]
          },
          {
            "description": "The approved digest matches the current plan and the plan is young enough.",
            "type": "string",
            "enum": [
              "approved"
            ]
          },
          {
            "description": "The approved digest matches and the plan has aged out; the next evaluation replaces it and it must be approved again.",
            "type": "string",
            "enum": [
              "expired"
            ]
          },
          {
            "description": "Something the rule needs is absent. **Never `Approved`.**",
            "type": "string",
            "enum": [
              "unknown"
            ]
          }
        ]
      },
      "ArchiveRequest": {
        "description": "An archive location in a request.",
        "type": "object",
        "required": [
          "url"
        ],
        "properties": {
          "credentialRef": {
            "description": "An existing Secret in this namespace carrying the object-store credential.",
            "default": null,
            "$ref": "#/components/schemas/NameRef",
            "nullable": true
          },
          "url": {
            "description": "`s3://`, `gs://`, `az://` or `file:///`; no userinfo.",
            "type": "string"
          }
        },
        "additionalProperties": false
      },
      "ArchiveView": {
        "description": "An archive location and the name of the Secret that reaches it.",
        "type": "object",
        "required": [
          "url"
        ],
        "properties": {
          "credentialRef": {
            "description": "The name of the Secret carrying the object-store credential. A name only; this service never reads Secrets.",
            "$ref": "#/components/schemas/NameRef",
            "nullable": true
          },
          "url": {
            "description": "The object-store URL, with any userinfo redacted.",
            "type": "string"
          }
        }
      },
      "AuthenticationMode": {
        "description": "How the current actor was authenticated.",
        "oneOf": [
          {
            "description": "The explicit loopback administrator mode.",
            "type": "string",
            "enum": [
              "localAdmin"
            ]
          },
          {
            "description": "A validated OpenID Connect session.",
            "type": "string",
            "enum": [
              "oidc"
            ]
          }
        ]
      },
      "AvailablePointView": {
        "description": "The newest point this policy can actually recover from.",
        "type": "object",
        "required": [
          "topicsTruncated"
        ],
        "properties": {
          "ageSeconds": {
            "description": "How old the point was when the objective was evaluated.",
            "type": "integer",
            "format": "int64",
            "nullable": true
          },
          "backupRef": {
            "description": "The `Backup` object, while one still exists.",
            "$ref": "#/components/schemas/NameRef",
            "nullable": true
          },
          "evidence": {
            "description": "`Valid`, `ValidHistorical`, `Invalid`, `Untrusted` or `NotAttempted`.",
            "type": "string",
            "nullable": true
          },
          "newestRecordAt": {
            "description": "The newest record the point contains. A DIFFERENT NUMBER, published beside the one above so nobody reads an idle topic as stale protection.",
            "type": "string",
            "format": "date-time",
            "nullable": true
          },
          "pointId": {
            "description": "The durable point identity, when the catalog supplied one.",
            "type": "string",
            "nullable": true
          },
          "recoveryPointAt": {
            "description": "*Capture start** — what the objective is measured from.",
            "type": "string",
            "format": "date-time",
            "nullable": true
          },
          "topics": {
            "description": "The topics it covers, bounded.",
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "topicsTruncated": {
            "description": "Whether `topics` was cut short.",
            "type": "boolean"
          }
        }
      },
      "Backup": {
        "description": "A backup run.",
        "type": "object",
        "required": [
          "archive",
          "deadlineSeconds",
          "name",
          "namespace",
          "operation",
          "resourceVersion",
          "sourceRef",
          "topics",
          "triggeredBy",
          "uid"
        ],
        "properties": {
          "archive": {
            "description": "Where the archive is written. With `destinationRef` this is the CRD's sentinel URL `logweir-destination://<name>` and carries no credential; a console renders the destination instead of the URL.",
            "$ref": "#/components/schemas/ArchiveView"
          },
          "backupId": {
            "description": "The archive backup ID.",
            "type": "string",
            "nullable": true
          },
          "createdAt": {
            "description": "When the object was created.",
            "type": "string",
            "format": "date-time",
            "nullable": true
          },
          "deadlineSeconds": {
            "description": "The Job deadline.",
            "type": "integer",
            "format": "int64"
          },
          "destinationRef": {
            "description": "The saved `BackupDestination` this run was written to, in this namespace.\n\nABSENT means the run carries its archive location INLINE in `archive`, exactly as every run did before saved destinations existed. It is a legacy inline-archive run, not a degraded one.",
            "$ref": "#/components/schemas/BackupDestinationRefView",
            "nullable": true
          },
          "locationDigest": {
            "description": "`sha256:<lowercase hex>` over the canonical location this run was FROZEN against — **where this recovery point's archive actually is**, copied verbatim from `Backup.status.destination.locationDigest`.\n\nABSENT for a legacy inline-archive run, and for any run frozen by a controller that predates the frozen-destination block. Absent means \"this recovery point publishes no frozen location\", never \"its location is unknown to be wrong\" — and NOTHING recomputes one from a live `BackupDestination`: a destination edited after a run froze must not be able to make a moved recovery point look settled.",
            "type": "string",
            "nullable": true
          },
          "manifestKey": {
            "description": "The manifest key.",
            "type": "string",
            "nullable": true
          },
          "name": {
            "description": "The object name.",
            "type": "string"
          },
          "namespace": {
            "description": "The namespace.",
            "type": "string"
          },
          "observedAuth": {
            "description": "The identity the run presented: mode and username, never a password.",
            "$ref": "#/components/schemas/ObservedAuthView",
            "nullable": true
          },
          "operation": {
            "description": "The normalized status summary.",
            "$ref": "#/components/schemas/OperationSummary"
          },
          "records": {
            "description": "Records archived.",
            "type": "integer",
            "format": "int64",
            "nullable": true
          },
          "resourceVersion": {
            "description": "The resourceVersion.",
            "type": "string"
          },
          "schedule": {
            "description": "The schedule that created the run.",
            "type": "string",
            "nullable": true
          },
          "scheduleRef": {
            "description": "The schedule revision this run copied. ABSENT — or present with no `uid`/`generation` — on a run created before PLAT-05.1: the console shows \"revision not recorded\", never a guess.",
            "$ref": "#/components/schemas/ScheduleRefView",
            "nullable": true
          },
          "slot": {
            "description": "The schedule slot.",
            "type": "string",
            "nullable": true
          },
          "sourceRef": {
            "description": "The source connection.",
            "$ref": "#/components/schemas/NameRef"
          },
          "topics": {
            "description": "Named topics.",
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "trigger": {
            "description": "What caused this run. ABSENT on a run created before PLAT-04.2: read `triggeredBy` then, and say \"trigger not recorded\" rather than inventing one.",
            "$ref": "#/components/schemas/TriggerView",
            "nullable": true
          },
          "triggeredBy": {
            "description": "`schedule` or `manual`.",
            "type": "string"
          },
          "uid": {
            "description": "The Kubernetes UID.",
            "type": "string"
          },
          "windowCovered": {
            "description": "The covered window, epoch milliseconds, end exclusive.",
            "$ref": "#/components/schemas/WindowCoveredView",
            "nullable": true
          }
        }
      },
      "BackupDestinationRefView": {
        "description": "The saved destination a run was written to.\n\nTWO HALVES FROM TWO PLACES, AND THE DIFFERENCE IS THE POINT. `name` is what the run ASKED for — `Backup.spec.destinationRef`, immutable with the rest of the spec. `uid` is what the controller RESOLVED at the freeze — `Backup.status.destination.uid` — and it is ABSENT until the freeze wrote one, and on every run frozen by a controller that predates the block. A destination deleted and recreated under the same name is a different object, and the uid is what says so; nothing here reads a live `BackupDestination` to fill it in.",
        "type": "object",
        "required": [
          "name"
        ],
        "properties": {
          "name": {
            "description": "The destination's name, in this namespace.",
            "type": "string"
          },
          "uid": {
            "description": "Its `metadata.uid` as the freeze recorded it.",
            "type": "string",
            "nullable": true
          }
        }
      },
      "BackupList": {
        "type": "object",
        "required": [
          "items",
          "page",
          "requestId"
        ],
        "properties": {
          "items": {
            "description": "The items on this page.",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/Backup"
            }
          },
          "page": {
            "description": "Paging.",
            "$ref": "#/components/schemas/Page"
          },
          "requestId": {
            "description": "The request ID.",
            "type": "string"
          }
        }
      },
      "BackupPreflightRequest": {
        "description": "The backup half of a preflight request.",
        "type": "object",
        "required": [
          "sourceConnection",
          "topics"
        ],
        "properties": {
          "destination": {
            "description": "The destination, in this namespace.",
            "default": null,
            "type": "string",
            "nullable": true
          },
          "legacyArchive": {
            "description": "Or a legacy inline archive.",
            "default": null,
            "$ref": "#/components/schemas/ArchiveRequest",
            "nullable": true
          },
          "schedule": {
            "description": "The schedule this readiness is about, for context.",
            "default": null,
            "type": "string",
            "nullable": true
          },
          "sourceConnection": {
            "description": "The source connection, in this namespace.",
            "type": "string"
          },
          "topics": {
            "description": "Named topics, 1 to 1000.",
            "type": "array",
            "items": {
              "type": "string"
            }
          }
        },
        "additionalProperties": false
      },
      "BackupResponse": {
        "type": "object",
        "required": [
          "item",
          "requestId"
        ],
        "properties": {
          "item": {
            "description": "The item.",
            "$ref": "#/components/schemas/Backup"
          },
          "replayed": {
            "description": "On a durable create: whether this response replays an earlier identical request (HTTP 200) rather than creating (HTTP 201).",
            "type": "boolean",
            "nullable": true
          },
          "requestId": {
            "description": "The request ID.",
            "type": "string"
          }
        }
      },
      "BackupScheduleRefRequest": {
        "description": "The schedule a manual run copies its policy from.",
        "type": "object",
        "required": [
          "name"
        ],
        "properties": {
          "expectedGeneration": {
            "description": "The revision the caller believes it is running. A different current generation is `409 policy_changed`, with the current generation and digest in the body.",
            "default": null,
            "type": "integer",
            "format": "int64",
            "nullable": true
          },
          "name": {
            "description": "The `BackupSchedule` name, in this namespace.",
            "type": "string"
          }
        },
        "additionalProperties": false
      },
      "CaBundleRequest": {
        "description": "A CA bundle in a `ConfigMap` in this namespace. A `ConfigMap` and never a Secret: a CA certificate is public material.",
        "type": "object",
        "required": [
          "configMapName"
        ],
        "properties": {
          "configMapName": {
            "description": "The `ConfigMap` name, in this namespace.",
            "type": "string"
          },
          "key": {
            "description": "The data key holding the PEM bundle. Absent means `ca.crt`.",
            "default": null,
            "type": "string",
            "nullable": true
          }
        },
        "additionalProperties": false
      },
      "CaBundleView": {
        "description": "A CA bundle reference, with the digest the controller observed.",
        "type": "object",
        "required": [
          "configMapName",
          "key"
        ],
        "properties": {
          "configMapName": {
            "description": "The `ConfigMap` name.",
            "type": "string"
          },
          "key": {
            "description": "The data key.",
            "type": "string"
          },
          "sha256": {
            "description": "`sha256:<hex>` over the CA bytes the controller read, so a rotation is visible without reading the bundle again.",
            "type": "string",
            "nullable": true
          }
        }
      },
      "CadenceAdjustment": {
        "description": "The DST marker on one firing (D1 §4.3/§4.4).\n\nTHE SPELLING IS THE CONTROLLER'S, NOT THIS CRATE'S. `BackupSchedule.status. nextRuns[].adjustment` carries these exact PascalCase strings, and a console renders the preview and the saved status with one branch. A marker this build does not recognise is OMITTED from a projection rather than echoed: an unknown word is not a fact about a time zone.",
        "oneOf": [
          {
            "description": "The matched local time does not exist (spring forward); this instant is the end of the gap.",
            "type": "string",
            "enum": [
              "NonexistentLocalTimeShifted"
            ]
          },
          {
            "description": "The matched local time happens twice; this is the FIRST occurrence.",
            "type": "string",
            "enum": [
              "RepeatedLocalTimeFirst"
            ]
          },
          {
            "description": "The matched local time happens twice; this is the SECOND occurrence.",
            "type": "string",
            "enum": [
              "RepeatedLocalTimeSecond"
            ]
          }
        ]
      },
      "CadencePreset": {
        "description": "One of the five preset cadences (D1 §4.2), with its parameters.\n\nA PRESET IS NEVER STORED. `spec.schedule` is the single source of truth; this is the catalogue entry an expression IS, so a form can round-trip it. `ui/tests/fixtures/cadence-presets.json` is the same catalogue as data.",
        "oneOf": [
          {
            "description": "Every hour at `minute`.",
            "type": "object",
            "required": [
              "kind",
              "minute"
            ],
            "properties": {
              "kind": {
                "type": "string",
                "enum": [
                  "hourly"
                ]
              },
              "minute": {
                "description": "0–59.",
                "type": "integer",
                "format": "uint32",
                "minimum": 0.0
              }
            }
          },
          {
            "description": "At local wall-clock hours divisible by `n`, at `minute`.",
            "type": "object",
            "required": [
              "kind",
              "minute",
              "n"
            ],
            "properties": {
              "kind": {
                "type": "string",
                "enum": [
                  "everyNHours"
                ]
              },
              "minute": {
                "description": "0–59.",
                "type": "integer",
                "format": "uint32",
                "minimum": 0.0
              },
              "n": {
                "description": "2, 3, 4, 6, 8 or 12.",
                "type": "integer",
                "format": "uint32",
                "minimum": 0.0
              }
            }
          },
          {
            "description": "Every day at `hour`:`minute`.",
            "type": "object",
            "required": [
              "hour",
              "kind",
              "minute"
            ],
            "properties": {
              "hour": {
                "description": "0–23.",
                "type": "integer",
                "format": "uint32",
                "minimum": 0.0
              },
              "kind": {
                "type": "string",
                "enum": [
                  "daily"
                ]
              },
              "minute": {
                "description": "0–59.",
                "type": "integer",
                "format": "uint32",
                "minimum": 0.0
              }
            }
          },
          {
            "description": "Every week on `dayOfWeek` at `hour`:`minute`.",
            "type": "object",
            "required": [
              "dayOfWeek",
              "hour",
              "kind",
              "minute"
            ],
            "properties": {
              "dayOfWeek": {
                "description": "0–6, 0 = Sunday.",
                "type": "integer",
                "format": "uint32",
                "minimum": 0.0
              },
              "hour": {
                "description": "0–23.",
                "type": "integer",
                "format": "uint32",
                "minimum": 0.0
              },
              "kind": {
                "type": "string",
                "enum": [
                  "weekly"
                ]
              },
              "minute": {
                "description": "0–59.",
                "type": "integer",
                "format": "uint32",
                "minimum": 0.0
              }
            }
          },
          {
            "description": "Every month on `dayOfMonth` at `hour`:`minute`.",
            "type": "object",
            "required": [
              "dayOfMonth",
              "hour",
              "kind",
              "minute"
            ],
            "properties": {
              "dayOfMonth": {
                "description": "1–28.",
                "type": "integer",
                "format": "uint32",
                "minimum": 0.0
              },
              "hour": {
                "description": "0–23.",
                "type": "integer",
                "format": "uint32",
                "minimum": 0.0
              },
              "kind": {
                "type": "string",
                "enum": [
                  "monthly"
                ]
              },
              "minute": {
                "description": "0–59.",
                "type": "integer",
                "format": "uint32",
                "minimum": 0.0
              }
            }
          }
        ]
      },
      "CadencePreviewResponse": {
        "description": "`GET /api/v1/cadence-previews`.\n\nA DRAFT PREVIEW, COMPUTED IN RUST AND NOWHERE ELSE (D1 §4.4). The browser renders what this returns and never evaluates cron; the saved schedule's own previews are `status.nextRuns`, written by the controller from the same module.",
        "type": "object",
        "required": [
          "after",
          "requestId",
          "runs",
          "schedule",
          "timeZone",
          "tzdb"
        ],
        "properties": {
          "after": {
            "description": "The instant the walk started strictly after — the server's now unless `after` was sent.",
            "type": "string",
            "format": "date-time"
          },
          "preset": {
            "description": "The preset this expression IS, or absent for \"Advanced cron\".",
            "$ref": "#/components/schemas/CadencePreset",
            "nullable": true
          },
          "requestId": {
            "description": "The request ID.",
            "type": "string"
          },
          "runs": {
            "description": "Up to `count` firings, ascending. SHORTER IS A REAL ANSWER: an expression such as `0 0 29 2 *` runs out of firings, and an empty list means \"it does not fire again\", not \"the server gave up\".",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/NextRunView"
            }
          },
          "schedule": {
            "description": "The canonical five-field expression that was evaluated. With `preset=` it is what the preset compiled to, so the form can save exactly this.",
            "type": "string"
          },
          "timeZone": {
            "description": "The EFFECTIVE zone: `UTC` when `timeZone` was not sent, so a reader never has to know the default.",
            "type": "string"
          },
          "tzdb": {
            "description": "Which time-zone database resolved it, e.g. `chrono-tz 0.10.4`. Rule updates ship with a release, so two releases can disagree about a slot and this says which one answered.",
            "type": "string"
          }
        }
      },
      "CancelResponse": {
        "description": "`POST .../topic-discoveries/{id}:cancel` and `POST .../preflights/{id}:cancel`.\n\nCANCELLATION IS A WISH, RECORDED. It never deletes archive data, Kafka topics, durable runs or signed evidence; the controller verifies the exact owned Job and UID before stopping anything.",
        "type": "object",
        "required": [
          "alreadyTerminal",
          "id",
          "requestId",
          "state"
        ],
        "properties": {
          "alreadyTerminal": {
            "description": "True when the check had already finished, in which case nothing was written. Repeating a cancel is 200 either way.",
            "type": "boolean"
          },
          "id": {
            "description": "The check's id.",
            "type": "string"
          },
          "requestId": {
            "description": "The request ID.",
            "type": "string"
          },
          "state": {
            "description": "Its state after the request.",
            "type": "string"
          }
        }
      },
      "CandidateView": {
        "description": "One point the evaluation would remove.",
        "type": "object",
        "required": [
          "pointId"
        ],
        "properties": {
          "bytes": {
            "description": "How many bytes, when the view knows.",
            "type": "integer",
            "format": "int64",
            "nullable": true
          },
          "objects": {
            "description": "How many objects it holds, when the view knows.",
            "type": "integer",
            "format": "int64",
            "nullable": true
          },
          "pointId": {
            "description": "The point.",
            "type": "string"
          },
          "reason": {
            "description": "Which rule made it a candidate.",
            "type": "string",
            "nullable": true
          },
          "recoveryPointAt": {
            "description": "Its capture start.",
            "type": "string",
            "format": "date-time",
            "nullable": true
          }
        }
      },
      "Capabilities": {
        "description": "The capability flags of one namespace grant.\n\nEvery flag is `implemented && allowed`. The eight domains that have no route in this build are always `false`.",
        "type": "object",
        "required": [
          "approvalPacketRead",
          "approvalSubmit",
          "approvalsRead",
          "backupsRead",
          "catalogConnect",
          "catalogWindowQuery",
          "catalogs",
          "connectionCreate",
          "connectionTest",
          "connectionsRead",
          "credentialWrite",
          "destinations",
          "manualBackupCreate",
          "operationEvents",
          "operationsRead",
          "preflight",
          "protection",
          "rehearsals",
          "restoreCreate",
          "restoresRead",
          "retention",
          "scheduleCreate",
          "scheduleSetSuspension",
          "schedulesRead",
          "topicDiscovery",
          "trustAdministration",
          "trustPoliciesRead"
        ],
        "properties": {
          "approvalPacketRead": {
            "description": "`GET .../approvals/{name}/packet`.",
            "type": "boolean"
          },
          "approvalSubmit": {
            "description": "Approval submission (PLAT-19.2): no route.",
            "type": "boolean"
          },
          "approvalsRead": {
            "description": "`GET .../approvals[/{name}]`.",
            "type": "boolean"
          },
          "backupsRead": {
            "description": "`GET .../backups[/{name}]`.",
            "type": "boolean"
          },
          "catalogConnect": {
            "description": "`POST .../catalogs` — connect an existing archive (PLAT-15.2).",
            "type": "boolean"
          },
          "catalogWindowQuery": {
            "description": "Windowed catalog point queries (filters beyond the published window) have no route in v1 and are advertised `false` for everybody, per D3 §5.3's \"an absent capability, never a fake stub\".",
            "type": "boolean"
          },
          "catalogs": {
            "description": "The recovery-catalog domain (PLAT-15.1) is served AND readable here.",
            "type": "boolean"
          },
          "connectionCreate": {
            "description": "`POST .../connections`.",
            "type": "boolean"
          },
          "connectionTest": {
            "description": "Connection tests (PLAT-07.2): no route.",
            "type": "boolean"
          },
          "connectionsRead": {
            "description": "`GET .../connections[/{name}]`.",
            "type": "boolean"
          },
          "credentialWrite": {
            "description": "Write-only credential input: `POST .../destinations` and `:update-access` accept a credential value that is created once and never read back.",
            "type": "boolean"
          },
          "destinations": {
            "description": "The saved-destination domain (PLAT-08) is served AND readable here.",
            "type": "boolean"
          },
          "manualBackupCreate": {
            "description": "`POST .../backups` — the canonical manual `Backup` (D1 §8.2).",
            "type": "boolean"
          },
          "operationEvents": {
            "description": "`GET .../operations/{kind}/{name}/events` — the server-sent stream.",
            "type": "boolean"
          },
          "operationsRead": {
            "description": "`GET .../operations/{kind}/{name}`.",
            "type": "boolean"
          },
          "preflight": {
            "description": "The preflight domain (PLAT-03) is served AND readable here.",
            "type": "boolean"
          },
          "protection": {
            "description": "The protection domain (PLAT-14.2) is served AND readable here.",
            "type": "boolean"
          },
          "rehearsals": {
            "description": "The rehearsal domain (PLAT-14.3) is served AND readable here.",
            "type": "boolean"
          },
          "restoreCreate": {
            "description": "`POST .../restores`.",
            "type": "boolean"
          },
          "restoresRead": {
            "description": "`GET .../restores[/{name}]`.",
            "type": "boolean"
          },
          "retention": {
            "description": "The retention domain (PLAT-16.1/16.2) is served AND readable here.",
            "type": "boolean"
          },
          "scheduleCreate": {
            "description": "`POST .../schedules` — AND `PUT .../schedules/{name}`.\n\nONE FLAG FOR TWO ROUTES, BECAUSE IT IS ONE AUTHORITY. D1 §5.1 makes a schedule's future policy editable, and this crate's decision (recorded in `docs/api.md`) is that an operator who may WRITE a policy into a bound namespace may write a new one over it: the edit reaches exactly the fields `POST` already sets, never `spec.sourceRef`, and never a created run. `Action::EditSchedulePolicy` is a separate action so the AUDIT record says `schedule.editPolicy` rather than `schedule.create`, but it has the same role allowance — pinned by `tests/role_matrix.rs::editing_a_policy_is_exactly_the_authority_to_create_one` — so a console reads this one flag for both buttons. D2 §8's rule stands: `ui/contract.js`'s `CAPABILITY_FLAGS` is a frozen 19-entry list, and splitting a flag is a contract change that lands on both sides at once.",
            "type": "boolean"
          },
          "scheduleSetSuspension": {
            "description": "`POST .../schedules/{name}:set-suspension`.",
            "type": "boolean"
          },
          "schedulesRead": {
            "description": "`GET .../schedules[/{name}]`.",
            "type": "boolean"
          },
          "topicDiscovery": {
            "description": "The topic-discovery domain (PLAT-09.1) is served AND readable here.\n\nONE FLAG PER DOMAIN, AND IT IS THE READ FLOOR. D2 §8 names exactly three flags — `destinations`, `topicDiscovery`, `preflight` — so a domain gets one bit, not a bit per verb, and the bit answers \"is this domain served and visible to me\". Whether the actor may also START or CANCEL is the role table above, which `/session` publishes per grant in `roles`; a console reads `roles.includes(\"operator\")` for the buttons and this flag for the page. Splitting these into read/start pairs is a contract change that must land together with `ui/contract.js`'s `CAPABILITY_FLAGS`.",
            "type": "boolean"
          },
          "trustAdministration": {
            "description": "Trust ADMINISTRATION (adding, retiring or revoking a key) has no route in v1 and is advertised `false` for everybody: D3 §10 keeps cluster-scoped trust writes on `kubectl apply` and the `logweir trust` helpers. There is no `Action` behind this flag, on purpose — a flag with no action cannot be turned on by a role table edit.",
            "type": "boolean"
          },
          "trustPoliciesRead": {
            "description": "`GET /api/v1/trust-policies[/{name}]` — the cluster-scoped read.",
            "type": "boolean"
          }
        }
      },
      "CaptureView": {
        "description": "When the capture actually happened — a Backup's.",
        "type": "object",
        "properties": {
          "finishedAt": {
            "description": "When it finished.",
            "type": "string",
            "format": "date-time",
            "nullable": true
          },
          "records": {
            "description": "How many records the VERIFIED receipt attested.\n\nAbsent is a fact about the run, not a zero. `Backup.status.records` was declared with a printer column and written by nothing until D3 W2 (defect `STATUS-RECORDS`), so every object older than that fix has it absent — and \"no receipt has been verified for this run\" is a different statement from \"this run captured nothing\".",
            "type": "integer",
            "format": "int64",
            "nullable": true
          },
          "startedAt": {
            "description": "Capture START, which is what protection freshness is measured from.",
            "type": "string",
            "format": "date-time",
            "nullable": true
          }
        }
      },
      "CatalogCountsView": {
        "description": "What the last sync found, by verdict. **Ten counts, not two**: \"how many points are there\" and \"how many can I restore from\" are different numbers.",
        "type": "object",
        "properties": {
          "available": {
            "description": "Readable, digest-matching, selectable.",
            "type": "integer",
            "format": "int64",
            "nullable": true
          },
          "conflict": {
            "description": "Two records disagree about one point.",
            "type": "integer",
            "format": "int64",
            "nullable": true
          },
          "deleted": {
            "description": "Recorded as deleted by an attributable retention run.",
            "type": "integer",
            "format": "int64",
            "nullable": true
          },
          "invalid": {
            "description": "The signature did not verify.",
            "type": "integer",
            "format": "int64",
            "nullable": true
          },
          "missing": {
            "description": "A definite `NotFound`.",
            "type": "integer",
            "format": "int64",
            "nullable": true
          },
          "total": {
            "description": "Every point the walk saw.",
            "type": "integer",
            "format": "int64",
            "nullable": true
          },
          "unreadable": {
            "description": "Present but unreadable — NOT the same as absent.",
            "type": "integer",
            "format": "int64",
            "nullable": true
          },
          "unsupportedFormat": {
            "description": "Written by a format this build does not understand.",
            "type": "integer",
            "format": "int64",
            "nullable": true
          },
          "untrustedSigner": {
            "description": "Signed by a key the bound policy does not trust.",
            "type": "integer",
            "format": "int64",
            "nullable": true
          },
          "unverified": {
            "description": "No signature verdict was reached.",
            "type": "integer",
            "format": "int64",
            "nullable": true
          }
        }
      },
      "CatalogList": {
        "description": "A page of catalogs.",
        "type": "object",
        "required": [
          "items",
          "page",
          "requestId"
        ],
        "properties": {
          "items": {
            "description": "The items on this page.",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/CatalogView"
            }
          },
          "page": {
            "description": "Paging.",
            "$ref": "#/components/schemas/Page"
          },
          "requestId": {
            "description": "The request ID.",
            "type": "string"
          }
        }
      },
      "CatalogResponse": {
        "description": "One catalog.",
        "type": "object",
        "required": [
          "item",
          "requestId"
        ],
        "properties": {
          "item": {
            "description": "The item.",
            "$ref": "#/components/schemas/CatalogView"
          },
          "replayed": {
            "description": "Whether this replays an earlier identical request (HTTP 200) rather than creating (HTTP 201).",
            "type": "boolean",
            "nullable": true
          },
          "requestId": {
            "description": "The request ID.",
            "type": "string"
          }
        }
      },
      "CatalogView": {
        "description": "A `RecoveryCatalog`, projected.",
        "type": "object",
        "required": [
          "counts",
          "deepCheck",
          "intervalSeconds",
          "mode",
          "name",
          "namespace",
          "resourceVersion",
          "truncated",
          "uid",
          "viewExpired",
          "viewLimit",
          "viewPoints"
        ],
        "properties": {
          "conditions": {
            "description": "`Ready`, `Synced`, `Stale` and `TrustAvailable`.",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/ConditionView"
            }
          },
          "counts": {
            "description": "What it found.",
            "$ref": "#/components/schemas/CatalogCountsView"
          },
          "createdAt": {
            "description": "When the object was created.",
            "type": "string",
            "format": "date-time",
            "nullable": true
          },
          "cursor": {
            "description": "Where the walk got to.",
            "$ref": "#/components/schemas/SyncCursorView",
            "nullable": true
          },
          "deepCheck": {
            "description": "`None`, `ManifestDigest` or `SegmentSample`.",
            "type": "string"
          },
          "destinationRef": {
            "description": "The saved destination it indexes.",
            "$ref": "#/components/schemas/NameRef",
            "nullable": true
          },
          "generation": {
            "description": "`metadata.generation`.",
            "type": "integer",
            "format": "int64",
            "nullable": true
          },
          "histogram": {
            "description": "Points per day.",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/HistogramBucketView"
            }
          },
          "intervalSeconds": {
            "description": "How often it syncs; `0` is manual only.",
            "type": "integer",
            "format": "int32"
          },
          "lastSync": {
            "description": "The last sync Job.",
            "$ref": "#/components/schemas/LastSyncView",
            "nullable": true
          },
          "legacyArchive": {
            "description": "The inline archive, with any userinfo redacted and the credential as a Secret NAME.",
            "$ref": "#/components/schemas/ArchiveView",
            "nullable": true
          },
          "mode": {
            "description": "`Index` or `Full`.",
            "type": "string"
          },
          "name": {
            "description": "The object name.",
            "type": "string"
          },
          "namespace": {
            "description": "The namespace.",
            "type": "string"
          },
          "observedGeneration": {
            "description": "The generation the view was built from.",
            "type": "integer",
            "format": "int64",
            "nullable": true
          },
          "observedSyncRequest": {
            "description": "The `syncRequest` token the controller has acted on. A request whose token is not this one has not been served yet.",
            "type": "string",
            "nullable": true
          },
          "resourceVersion": {
            "description": "The resourceVersion this projection was read at.",
            "type": "string"
          },
          "signers": {
            "description": "Who signed them.",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/SignerView"
            }
          },
          "syncedAt": {
            "description": "When the view was last refreshed.",
            "type": "string",
            "format": "date-time",
            "nullable": true
          },
          "truncated": {
            "description": "Whether the archive holds more points than the view carries.",
            "type": "boolean"
          },
          "uid": {
            "description": "The Kubernetes UID.",
            "type": "string"
          },
          "viewExpired": {
            "description": "Whether the view has already expired, by this server's clock.",
            "type": "boolean"
          },
          "viewExpiresAt": {
            "description": "When the pages age out with their Job. After it the view is gone and this object says so rather than reporting an empty archive.",
            "type": "string",
            "format": "date-time",
            "nullable": true
          },
          "viewLimit": {
            "description": "How many newest points are materialised into Kubernetes.",
            "type": "integer",
            "format": "int32"
          },
          "viewPoints": {
            "description": "How many points the materialised view actually holds.",
            "type": "integer",
            "format": "int64"
          }
        }
      },
      "CatchUpPolicy": {
        "description": "Whether a slot past its starting deadline may still run.",
        "oneOf": [
          {
            "description": "Count it in `status.missedSlots` and move on — today's behaviour, and what an absent field means.",
            "type": "string",
            "enum": [
              "None"
            ]
          },
          {
            "description": "The LATEST due slot, and only that one, may still run. At most one catch-up run, ever.",
            "type": "string",
            "enum": [
              "Latest"
            ]
          }
        ]
      },
      "CheckEntryView": {
        "description": "One check entry.",
        "type": "object",
        "required": [
          "id",
          "state"
        ],
        "properties": {
          "authority": {
            "description": "Who observed the fact.",
            "type": "string",
            "nullable": true
          },
          "category": {
            "description": "Its category.",
            "type": "string",
            "nullable": true
          },
          "code": {
            "description": "The CamelCase code.",
            "type": "string",
            "nullable": true
          },
          "expiresAt": {
            "description": "When it stops counting.",
            "type": "string",
            "format": "date-time",
            "nullable": true
          },
          "gating": {
            "description": "Whether it gates.",
            "$ref": "#/components/schemas/CheckGating",
            "nullable": true
          },
          "id": {
            "description": "The stable check id, e.g. `target.mappedTopics`.",
            "type": "string"
          },
          "message": {
            "description": "The redacted message.",
            "type": "string",
            "nullable": true
          },
          "observedAt": {
            "description": "When the fact was observed.",
            "type": "string",
            "format": "date-time",
            "nullable": true
          },
          "remedy": {
            "description": "What to do about it.",
            "type": "string",
            "nullable": true
          },
          "scope": {
            "description": "What it looked at.",
            "$ref": "#/components/schemas/CheckScopeView",
            "nullable": true
          },
          "state": {
            "description": "The verdict.",
            "$ref": "#/components/schemas/CheckVerdict"
          }
        }
      },
      "CheckErrorView": {
        "description": "A bounded, redacted failure.",
        "type": "object",
        "required": [
          "code"
        ],
        "properties": {
          "code": {
            "description": "The CamelCase code.",
            "type": "string"
          },
          "message": {
            "description": "The redacted message.",
            "type": "string",
            "nullable": true
          }
        }
      },
      "CheckGating": {
        "description": "Whether a check's verdict gates the operation.",
        "oneOf": [
          {
            "description": "A `notReady` here makes the whole operation `notReady`.",
            "type": "string",
            "enum": [
              "blocking"
            ]
          },
          {
            "description": "Reported as a warning; never changes the aggregate.",
            "type": "string",
            "enum": [
              "advisory"
            ]
          },
          {
            "description": "Cannot be checked before execution; always `unknown` and always excluded from the aggregate.",
            "type": "string",
            "enum": [
              "executionOnly"
            ]
          }
        ]
      },
      "CheckLifecycle": {
        "description": "The lifecycle of a transient check.",
        "oneOf": [
          {
            "description": "Accepted; the controller has not observed it yet.",
            "type": "string",
            "enum": [
              "pending"
            ]
          },
          {
            "description": "Waiting for a check slot.",
            "type": "string",
            "enum": [
              "queued"
            ]
          },
          {
            "description": "The check Job exists and has not finished.",
            "type": "string",
            "enum": [
              "running"
            ]
          },
          {
            "description": "A result was produced.",
            "type": "string",
            "enum": [
              "succeeded"
            ]
          },
          {
            "description": "No result could be produced. Distinct from a result that says \"no\".",
            "type": "string",
            "enum": [
              "failed"
            ]
          },
          {
            "description": "Cancelled before a result.",
            "type": "string",
            "enum": [
              "cancelled"
            ]
          },
          {
            "description": "A phase this build does not recognise.",
            "type": "string",
            "enum": [
              "unknown"
            ]
          }
        ]
      },
      "CheckOperation": {
        "description": "The normalized status of one transient check.",
        "type": "object",
        "required": [
          "cancellable",
          "conditions",
          "kind",
          "name",
          "namespace",
          "resourceVersion",
          "state",
          "terminal",
          "uid"
        ],
        "properties": {
          "cancellable": {
            "description": "Whether this check may still be cancelled.",
            "type": "boolean"
          },
          "conditions": {
            "description": "The status conditions, at most 16.",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/ConditionView"
            }
          },
          "createdAt": {
            "description": "When the object was created.",
            "type": "string",
            "format": "date-time",
            "nullable": true
          },
          "kind": {
            "description": "`discovery` or `preflight`.",
            "$ref": "#/components/schemas/CheckOperationKind"
          },
          "message": {
            "description": "The bounded message.",
            "type": "string",
            "nullable": true
          },
          "name": {
            "description": "The object name.",
            "type": "string"
          },
          "namespace": {
            "description": "The namespace.",
            "type": "string"
          },
          "observedAt": {
            "description": "When the facts were observed.",
            "type": "string",
            "format": "date-time",
            "nullable": true
          },
          "resourceVersion": {
            "description": "The resourceVersion this status was read at.",
            "type": "string"
          },
          "state": {
            "description": "The lifecycle state.",
            "$ref": "#/components/schemas/CheckLifecycle"
          },
          "stateReason": {
            "description": "The CamelCase reason.",
            "type": "string",
            "nullable": true
          },
          "terminal": {
            "description": "Whether the state is final.",
            "type": "boolean"
          },
          "uid": {
            "description": "The Kubernetes UID.",
            "type": "string"
          }
        }
      },
      "CheckOperationKind": {
        "description": "The two transient check kinds with a status route.\n\nSEPARATE FROM [`OperationKind`] ON PURPOSE. A check has no archive result, no signed evidence and no verification verdict, so folding it into [`Operation`] would mean publishing three fields that are meaningless for it and inviting a console to render \"verification: pending\" for a topic list.",
        "oneOf": [
          {
            "description": "A `TopicDiscovery`.",
            "type": "string",
            "enum": [
              "discovery"
            ]
          },
          {
            "description": "A `Preflight`.",
            "type": "string",
            "enum": [
              "preflight"
            ]
          }
        ]
      },
      "CheckOperationResponse": {
        "description": "`GET .../operations/discovery/{name}` and `.../operations/preflight/{name}`.",
        "type": "object",
        "required": [
          "item",
          "requestId"
        ],
        "properties": {
          "item": {
            "description": "The normalized check.",
            "$ref": "#/components/schemas/CheckOperation"
          },
          "requestId": {
            "description": "The request ID.",
            "type": "string"
          }
        }
      },
      "CheckScopeView": {
        "description": "What a check looked at.",
        "type": "object",
        "properties": {
          "kind": {
            "description": "The kind.",
            "type": "string",
            "nullable": true
          },
          "name": {
            "description": "The name.",
            "type": "string",
            "nullable": true
          },
          "uid": {
            "description": "The UID.",
            "type": "string",
            "nullable": true
          }
        }
      },
      "CheckVerdict": {
        "description": "One check's verdict.",
        "oneOf": [
          {
            "description": "The check passed.",
            "type": "string",
            "enum": [
              "ready"
            ]
          },
          {
            "description": "The check failed.",
            "type": "string",
            "enum": [
              "notReady"
            ]
          },
          {
            "description": "The check could not be decided.",
            "type": "string",
            "enum": [
              "unknown"
            ]
          },
          {
            "description": "The caller asked for it to be skipped. A skipped blocking check keeps the aggregate `unknown`; it never counts as a pass.",
            "type": "string",
            "enum": [
              "skipped"
            ]
          }
        ]
      },
      "CleanupStateView": {
        "description": "Topics a previous teardown could not remove. While this is non-empty the next slot is SKIPPED rather than allowed to adopt topics it did not create.",
        "type": "object",
        "required": [
          "pendingTopicsTruncated"
        ],
        "properties": {
          "pendingTopics": {
            "description": "The topics still there.",
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "pendingTopicsTruncated": {
            "description": "Whether the list was cut short.",
            "type": "boolean"
          },
          "since": {
            "description": "Since when.",
            "type": "string",
            "format": "date-time",
            "nullable": true
          }
        }
      },
      "CompletionView": {
        "description": "D3 §3.5's incident-facing completion panel — a Restore's, copied from the signed scorecard and never recomputed.",
        "type": "object",
        "properties": {
          "integrityLevel": {
            "description": "`byte-fingerprint`, `consume-only` or `not-attempted`, verbatim.",
            "type": "string",
            "nullable": true
          },
          "newTopics": {
            "description": "The topics the run created, with their partition counts.",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/CreatedTopicView"
            }
          },
          "recordsExpected": {
            "description": "The canary size.",
            "type": "integer",
            "format": "int64",
            "nullable": true
          },
          "recordsRestored": {
            "description": "How many records were restored in the sampled window.",
            "type": "integer",
            "format": "int64",
            "nullable": true
          },
          "recordsSampled": {
            "description": "How many records were sampled.",
            "type": "integer",
            "format": "int64",
            "nullable": true
          },
          "recordsSampledMatching": {
            "description": "How many of them matched.",
            "type": "integer",
            "format": "int64",
            "nullable": true
          },
          "sampleWindow": {
            "description": "The window the sample covered.",
            "$ref": "#/components/schemas/SampleWindowView",
            "nullable": true
          }
        }
      },
      "ConcurrencyPolicy": {
        "description": "Whether a slot may start while an earlier run is unfinished.",
        "oneOf": [
          {
            "description": "Never overlap (the default).",
            "type": "string",
            "enum": [
              "Forbid"
            ]
          },
          {
            "description": "Permit overlapping slots.",
            "type": "string",
            "enum": [
              "Allow"
            ]
          }
        ]
      },
      "ConditionView": {
        "description": "A status condition, with a bounded message.",
        "type": "object",
        "required": [
          "status",
          "type"
        ],
        "properties": {
          "lastTransitionTime": {
            "description": "When the condition last changed status or reason.",
            "type": "string",
            "format": "date-time",
            "nullable": true
          },
          "message": {
            "description": "The message, at most 1024 bytes.",
            "type": "string",
            "nullable": true
          },
          "reason": {
            "description": "The CamelCase reason.",
            "type": "string",
            "nullable": true
          },
          "status": {
            "description": "`True`, `False` or `Unknown`.",
            "type": "string"
          },
          "type": {
            "description": "The condition type.",
            "type": "string"
          }
        }
      },
      "ConnectArchiveRequest": {
        "description": "`POST .../catalogs` — connect an archive.\n\nTHE NAME IS THE CALLER'S, like a destination's and for the same reason: a `ProtectionPolicy`, a `RehearsalSchedule` and a `RetentionPolicy` all reference a catalog by name, and a hashed name would make every one of those references unreadable.",
        "type": "object",
        "required": [
          "name",
          "syncMode"
        ],
        "properties": {
          "deepCheck": {
            "description": "How hard to check each point. Absent takes the CRD's default.",
            "default": null,
            "$ref": "#/components/schemas/ConnectDeepCheck",
            "nullable": true
          },
          "destinationRef": {
            "description": "The saved destination to index.",
            "default": null,
            "$ref": "#/components/schemas/NameRef",
            "nullable": true
          },
          "intervalSeconds": {
            "description": "Seconds between syncs; `0` is manual only. Absent takes the CRD's default.",
            "default": null,
            "type": "integer",
            "format": "int32",
            "nullable": true
          },
          "legacyArchive": {
            "description": "An archive that predates saved destinations. Exactly one of this and `destinationRef`.",
            "default": null,
            "$ref": "#/components/schemas/ArchiveRequest",
            "nullable": true
          },
          "name": {
            "description": "The catalog's name, a DNS-1123 subdomain.",
            "type": "string"
          },
          "syncMode": {
            "description": "`index` or `full`. Connecting an existing archive wants `full`: an index walk only reads shards a Logweir installation wrote.",
            "$ref": "#/components/schemas/ConnectSyncMode"
          },
          "viewLimit": {
            "description": "How many newest points are materialised. Absent takes the CRD's default.",
            "default": null,
            "type": "integer",
            "format": "int32",
            "nullable": true
          }
        },
        "additionalProperties": false
      },
      "ConnectDeepCheck": {
        "description": "The request spelling of [`DeepCheck`].",
        "oneOf": [
          {
            "description": "Existence only.",
            "type": "string",
            "enum": [
              "none"
            ]
          },
          {
            "description": "The manifest digest equals the receipt's.",
            "type": "string",
            "enum": [
              "manifestDigest"
            ]
          },
          {
            "description": "Additionally sample segment bytes.",
            "type": "string",
            "enum": [
              "segmentSample"
            ]
          }
        ]
      },
      "ConnectSyncMode": {
        "description": "The request spelling of [`SyncMode`].",
        "oneOf": [
          {
            "description": "Day shards at or after the recorded cursor.",
            "type": "string",
            "enum": [
              "index"
            ]
          },
          {
            "description": "A full, resumable rescan of receipts and manifests.",
            "type": "string",
            "enum": [
              "full"
            ]
          }
        ]
      },
      "Connection": {
        "description": "A saved connection.",
        "type": "object",
        "required": [
          "auth",
          "bootstrapServers",
          "name",
          "namespace",
          "reachability",
          "resourceVersion",
          "role",
          "uid"
        ],
        "properties": {
          "auth": {
            "description": "Authentication settings.",
            "$ref": "#/components/schemas/ConnectionAuthView"
          },
          "bootstrapServers": {
            "description": "Bootstrap addresses.",
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "createdAt": {
            "description": "When the object was created.",
            "type": "string",
            "format": "date-time",
            "nullable": true
          },
          "lastTest": {
            "description": "The newest `SourceConnection` preflight for this connection, when the detail route found one.\n\nA DIFFERENT FACT FROM `reachability`, and the console renders them apart. `reachability` is the controller's own probe on its own cadence; this is a check somebody asked for, with its own instant and its own staleness. Absent on a list and on a create: one detail read is one bounded label scan, and a page of connections would be one per row.",
            "$ref": "#/components/schemas/LastTestView",
            "nullable": true
          },
          "markerTopic": {
            "description": "The marker topic.",
            "type": "string",
            "nullable": true
          },
          "name": {
            "description": "The object name.",
            "type": "string"
          },
          "namespace": {
            "description": "The namespace.",
            "type": "string"
          },
          "reachability": {
            "description": "The controller's reachability observation.",
            "$ref": "#/components/schemas/ReachabilityView"
          },
          "resourceVersion": {
            "description": "The resourceVersion this projection was read at.",
            "type": "string"
          },
          "role": {
            "description": "The role label.",
            "type": "string"
          },
          "uid": {
            "description": "The Kubernetes UID.",
            "type": "string"
          }
        }
      },
      "ConnectionAuthMode": {
        "description": "The SASL mechanism, or `plaintext` for none.",
        "oneOf": [
          {
            "description": "No SASL.",
            "type": "string",
            "enum": [
              "plaintext"
            ]
          },
          {
            "description": "SASL/SCRAM-SHA-512.",
            "type": "string",
            "enum": [
              "scramSha512"
            ]
          }
        ]
      },
      "ConnectionAuthRequest": {
        "description": "Authentication for a new connection. Existing credentials only: this request names a Secret; it never carries a password.",
        "type": "object",
        "required": [
          "mode",
          "tls"
        ],
        "properties": {
          "credentialRef": {
            "description": "An existing Secret in this namespace holding the SASL password; required for `scramSha512`, refused for `plaintext`.",
            "default": null,
            "$ref": "#/components/schemas/NameRef",
            "nullable": true
          },
          "mode": {
            "description": "`plaintext` or `scramSha512`.",
            "$ref": "#/components/schemas/ConnectionAuthMode"
          },
          "tls": {
            "description": "Whether the transport is TLS.",
            "type": "boolean"
          },
          "username": {
            "description": "The SASL principal; required for `scramSha512`, refused for `plaintext`.",
            "default": null,
            "type": "string",
            "nullable": true
          }
        },
        "additionalProperties": false
      },
      "ConnectionAuthView": {
        "description": "Authentication settings of a stored connection.",
        "type": "object",
        "required": [
          "mode",
          "tls"
        ],
        "properties": {
          "credentialRef": {
            "description": "The credential Secret's NAME. Never its data.",
            "$ref": "#/components/schemas/NameRef",
            "nullable": true
          },
          "mode": {
            "description": "`plaintext` or `scramSha512`.",
            "$ref": "#/components/schemas/ConnectionAuthMode"
          },
          "tls": {
            "description": "Whether the transport is TLS.",
            "type": "boolean"
          },
          "username": {
            "description": "The SASL principal.",
            "type": "string",
            "nullable": true
          }
        }
      },
      "ConnectionList": {
        "type": "object",
        "required": [
          "items",
          "page",
          "requestId"
        ],
        "properties": {
          "items": {
            "description": "The items on this page.",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/Connection"
            }
          },
          "page": {
            "description": "Paging.",
            "$ref": "#/components/schemas/Page"
          },
          "requestId": {
            "description": "The request ID.",
            "type": "string"
          }
        }
      },
      "ConnectionResponse": {
        "type": "object",
        "required": [
          "item",
          "requestId"
        ],
        "properties": {
          "item": {
            "description": "The item.",
            "$ref": "#/components/schemas/Connection"
          },
          "replayed": {
            "description": "On a durable create: whether this response replays an earlier identical request (HTTP 200) rather than creating (HTTP 201).",
            "type": "boolean",
            "nullable": true
          },
          "requestId": {
            "description": "The request ID.",
            "type": "string"
          }
        }
      },
      "ConnectionRole": {
        "description": "What a connection is to Logweir.",
        "oneOf": [
          {
            "description": "A cluster backed up from.",
            "type": "string",
            "enum": [
              "source"
            ]
          },
          {
            "description": "A cluster restored into.",
            "type": "string",
            "enum": [
              "target"
            ]
          }
        ]
      },
      "CreateBackupRequest": {
        "description": "`POST /api/v1/namespaces/{ns}/backups` — \"Back up now\" and \"Run first backup now\" (D1 §8.2).\n\nTWO BODIES, ONE CR PATH. With `scheduleRef` the API copies the schedule's current revision — selection, archive, deadline — and records `{uid, generation, runPolicySha256}`; a policy field in the body beside it is `422`, because a \"manual run of this schedule\" that quietly ran a different policy would be the wrong thing recorded in the receipt. Without it the body IS the policy, for a cluster with no schedule yet.",
        "type": "object",
        "properties": {
          "deadlineSeconds": {
            "description": "Body B: the run's `activeDeadlineSeconds`, 60 to 86400. Absent means 3600.",
            "default": null,
            "type": "integer",
            "format": "int64",
            "nullable": true
          },
          "destinationRef": {
            "description": "Body B: a saved `BackupDestination` instead of an inline archive.",
            "default": null,
            "$ref": "#/components/schemas/NameRef",
            "nullable": true
          },
          "legacyArchive": {
            "description": "Body B: an inline archive location.",
            "default": null,
            "$ref": "#/components/schemas/ArchiveRequest",
            "nullable": true
          },
          "readinessAcknowledgement": {
            "description": "Either body: \"Run anyway\" after a `notReady` or `unknown` verdict.",
            "default": null,
            "$ref": "#/components/schemas/ReadinessAcknowledgementRequest",
            "nullable": true
          },
          "scheduleRef": {
            "description": "Body A: copy this schedule's current revision.",
            "default": null,
            "$ref": "#/components/schemas/BackupScheduleRefRequest",
            "nullable": true
          },
          "sourceRef": {
            "description": "Body B: the source connection, in this namespace.",
            "default": null,
            "$ref": "#/components/schemas/NameRef",
            "nullable": true
          },
          "topicSelection": {
            "description": "Body B: the topic selection.",
            "default": null,
            "$ref": "#/components/schemas/TopicSelectionRequest",
            "nullable": true
          }
        },
        "additionalProperties": false
      },
      "CreateConnectionRequest": {
        "description": "`POST /api/v1/namespaces/{ns}/connections`.",
        "type": "object",
        "required": [
          "auth",
          "bootstrapServers",
          "role"
        ],
        "properties": {
          "auth": {
            "description": "How Logweir authenticates.",
            "$ref": "#/components/schemas/ConnectionAuthRequest"
          },
          "bootstrapServers": {
            "description": "Broker bootstrap addresses, `host:port`, 1 to 16 entries.",
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "markerTopic": {
            "description": "The marker topic that proves a scratch target.",
            "default": null,
            "type": "string",
            "nullable": true
          },
          "role": {
            "description": "`source` or `target`.",
            "$ref": "#/components/schemas/ConnectionRole"
          }
        },
        "additionalProperties": false
      },
      "CreateDestinationRequest": {
        "description": "`POST /api/v1/namespaces/{ns}/destinations`.",
        "type": "object",
        "required": [
          "access",
          "name",
          "storage",
          "transport"
        ],
        "properties": {
          "access": {
            "description": "The four credential grants.",
            "$ref": "#/components/schemas/AccessRequest"
          },
          "default": {
            "description": "Whether this becomes the namespace's default destination. At most one destination per namespace may hold it, and a second request naming another default is a conflict rather than a silent takeover.",
            "default": null,
            "type": "boolean",
            "nullable": true
          },
          "description": {
            "description": "What this destination is, for a human reading a list.",
            "default": null,
            "type": "string",
            "nullable": true
          },
          "name": {
            "description": "The destination's name, which is also its Kubernetes object name: a schedule, a backup and a restore all reference it by this name.",
            "type": "string"
          },
          "readiness": {
            "description": "Readiness settings.",
            "default": null,
            "$ref": "#/components/schemas/ReadinessRequest",
            "nullable": true
          },
          "storage": {
            "description": "The location. Immutable once created.",
            "$ref": "#/components/schemas/StorageRequest"
          },
          "transport": {
            "description": "Transport security and its trust material.",
            "$ref": "#/components/schemas/TransportRequest"
          }
        },
        "additionalProperties": false
      },
      "CreatePreflightRequest": {
        "description": "`POST .../preflights`.",
        "type": "object",
        "required": [
          "operation"
        ],
        "properties": {
          "backup": {
            "description": "The backup block.",
            "default": null,
            "$ref": "#/components/schemas/BackupPreflightRequest",
            "nullable": true
          },
          "destinationAccess": {
            "description": "The destination-access block.",
            "default": null,
            "$ref": "#/components/schemas/DestinationAccessPreflightRequest",
            "nullable": true
          },
          "operation": {
            "description": "What this is about. Exactly the matching block below may be set.",
            "$ref": "#/components/schemas/PreflightOperationDto"
          },
          "restore": {
            "description": "The restore block.",
            "default": null,
            "$ref": "#/components/schemas/RestorePreflightRequest",
            "nullable": true
          },
          "skipChecks": {
            "description": "Blocking checks to skip, at most 32. A skipped blocking check keeps the aggregate `unknown`.",
            "default": null,
            "type": "array",
            "items": {
              "type": "string"
            },
            "nullable": true
          },
          "sourceConnection": {
            "description": "The source-connection block.",
            "default": null,
            "$ref": "#/components/schemas/SourceConnectionPreflightRequest",
            "nullable": true
          },
          "timeoutSeconds": {
            "description": "The check budget in seconds, 30 to 600.",
            "default": null,
            "type": "integer",
            "format": "int32",
            "nullable": true
          }
        },
        "additionalProperties": false
      },
      "CreateRestoreRequest": {
        "description": "`POST /api/v1/namespaces/{ns}/restores`.\n\n`planBytes` is OPAQUE: the service checks `planHash` against the SHA-256 of exactly these bytes and stores them unchanged. It never parses or re-emits the plan document.",
        "type": "object",
        "required": [
          "approvalRef",
          "backupSetRef",
          "deadlineSeconds",
          "planBytes",
          "planHash",
          "pointInTime",
          "sourceArchive",
          "target"
        ],
        "properties": {
          "approvalRef": {
            "description": "The Approval that will authorise this restore, in this namespace. It need not exist yet: the controller holds the restore until it verifies.",
            "$ref": "#/components/schemas/NameRef"
          },
          "backupSetRef": {
            "description": "The backup set ID inside the archive.",
            "type": "string"
          },
          "deadlineSeconds": {
            "description": "The Job deadline, 60 to 86400 seconds.",
            "type": "integer",
            "format": "int64"
          },
          "evidenceDestinationRef": {
            "description": "The saved destination evidence is written to. Set together with `sourceDestinationRef`.",
            "$ref": "#/components/schemas/NameRef",
            "nullable": true
          },
          "planBytes": {
            "description": "The exact plan document bytes the approval will bind, at most 256 KiB.",
            "type": "string"
          },
          "planHash": {
            "description": "`sha256:<lowercase hex>` of `planBytes`, as the client computed it.",
            "type": "string"
          },
          "pointInTime": {
            "description": "The RFC 3339 point in time to restore to.",
            "type": "string"
          },
          "sourceArchive": {
            "description": "The archive to restore from.",
            "$ref": "#/components/schemas/ArchiveRequest"
          },
          "sourceDestinationRef": {
            "description": "The saved destination the archive is read from. Set together with `evidenceDestinationRef`; when set, `sourceArchive.url` is the `logweir-destination://<name>` sentinel and carries no credential.",
            "$ref": "#/components/schemas/NameRef",
            "nullable": true
          },
          "target": {
            "description": "Where the restore writes.",
            "$ref": "#/components/schemas/RestoreTargetRequest"
          },
          "topicMapping": {
            "description": "The exact source→target mapping the caller previewed, checked against `target.topicNaming.prefix` and refused row by row.\n\nABSENT IS EXACTLY THE BEHAVIOUR THIS ROUTE HAD BEFORE THE FIELD EXISTED, and `skip_serializing_if` keeps an absent one out of the idempotency request hash, so a client that predates it replays onto the same object it always did.",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/TopicMappingRow"
            },
            "nullable": true
          }
        },
        "additionalProperties": false
      },
      "CreateScheduleRequest": {
        "description": "`POST /api/v1/namespaces/{ns}/schedules`.\n\n# It creates what the edit route can edit (PLAT-10.1)\n\nUntil PLAT-10.1 this DTO carried five fields and the edit route carried thirteen, so a schedule the console could *edit* into shape could not be *created* in that shape: a saved destination, a dynamic selection, a zone, a catch-up policy and retries all had to be added afterwards by a second request against an object that was already scheduling runs from a policy nobody asked for. D2 W13's record named the first of those — \"`CreateScheduleRequest.destinationRef`\" — under *owed by the API before these pages are complete*, and the console's create form printed the debt on screen. The optional fields below close it, and they are exactly [`UpdateSchedulePolicyRequest`]'s, so one form serves both routes.\n\n# Every addition is optional, and an old body still means what it meant\n\n`archive` became optional so that `destinationRef` can take its place — **exactly one** of the two, as on the edit route — and `topics` became optional so that `allUserTopics` can take *its* place. A pre-PLAT-10.1 body names `archive` and a non-empty `topics` and is refused by neither rule, so it creates byte for byte the schedule it created before: absent `timeZone` is UTC, absent `catchUpPolicy` is `None`, absent `retry` is no retries, and absent deadlines are the documented defaults.",
        "type": "object",
        "required": [
          "schedule",
          "sourceRef",
          "suspended"
        ],
        "properties": {
          "activeDeadlineSeconds": {
            "description": "The run's `activeDeadlineSeconds`, 60 to 86400. Absent means 3600.",
            "default": null,
            "type": "integer",
            "format": "int64",
            "nullable": true
          },
          "allUserTopics": {
            "description": "Dynamic selection (PLAT-09.2). Either this or a non-empty `topics`; an empty selection is refused before any write.\n\nSPELLED FLAT, NOT UNDER `topicSelection`. The edit route nests the two halves because it was written as a whole-policy replace; this route has carried a top-level `topics` since PLAT-17.1 and moving it would break every existing caller to gain nothing. Both routes validate through the same `validate_selection`.",
            "default": null,
            "$ref": "#/components/schemas/AllUserTopics",
            "nullable": true
          },
          "archive": {
            "description": "Where backups are written. Exactly one of `archive` or `destinationRef`.",
            "default": null,
            "$ref": "#/components/schemas/ArchiveRequest",
            "nullable": true
          },
          "catchUpPolicy": {
            "description": "Absent means `None`.",
            "default": null,
            "$ref": "#/components/schemas/CatchUpPolicy",
            "nullable": true
          },
          "concurrencyPolicy": {
            "description": "`Forbid` (default) or `Allow`.",
            "default": null,
            "$ref": "#/components/schemas/ConcurrencyPolicy",
            "nullable": true
          },
          "destinationRef": {
            "description": "A saved `BackupDestination` in this namespace. Exactly one of `archive` or `destinationRef`; the sentinel `archive.url` the CRD requires is built by the route and is never accepted from a body.",
            "default": null,
            "$ref": "#/components/schemas/NameRef",
            "nullable": true
          },
          "retention": {
            "description": "Retention reporting.",
            "default": null,
            "$ref": "#/components/schemas/RetentionRequest",
            "nullable": true
          },
          "retry": {
            "description": "Absent means no retries.",
            "default": null,
            "$ref": "#/components/schemas/RetryPolicy",
            "nullable": true
          },
          "schedule": {
            "description": "A five-field cron expression or `@hourly`/`@daily`/`@weekly`, parsed by the controller's own parser in `timeZone`.",
            "type": "string"
          },
          "sourceRef": {
            "description": "The source connection, in this namespace.",
            "$ref": "#/components/schemas/NameRef"
          },
          "startingDeadlineSeconds": {
            "description": "How long after its instant a slot may still start, 60 to 604800. Absent means 3600.",
            "default": null,
            "type": "integer",
            "format": "int64",
            "nullable": true
          },
          "suspended": {
            "description": "Whether the schedule starts suspended.",
            "type": "boolean"
          },
          "timeZone": {
            "description": "An IANA zone name. Absent means UTC, which is what every pre-PLAT-04.2 schedule does.",
            "default": null,
            "type": "string",
            "nullable": true
          },
          "topics": {
            "description": "Named topics, up to 256; patterns are refused. Empty only with `allUserTopics`.",
            "default": [],
            "type": "array",
            "items": {
              "type": "string"
            }
          }
        },
        "additionalProperties": false
      },
      "CreateTopicDiscoveryRequest": {
        "description": "`POST .../connections/{name}/topic-discoveries`.",
        "type": "object",
        "properties": {
          "expectedTopics": {
            "description": "Names to ask about explicitly, at most 500.",
            "default": null,
            "type": "array",
            "items": {
              "type": "string"
            },
            "nullable": true
          },
          "includeInternal": {
            "description": "Whether `__`-prefixed topics are kept. Absent means false.",
            "default": null,
            "type": "boolean",
            "nullable": true
          },
          "maxTopics": {
            "description": "The ceiling on stored entries.",
            "default": null,
            "type": "integer",
            "format": "int32",
            "nullable": true
          },
          "reuseFresh": {
            "description": "Whether a fresh, identical, succeeded discovery may be returned instead of starting another. Absent means true.",
            "default": null,
            "type": "boolean",
            "nullable": true
          },
          "timeoutSeconds": {
            "description": "The in-Job Kafka budget in seconds.",
            "default": null,
            "type": "integer",
            "format": "int32",
            "nullable": true
          }
        },
        "additionalProperties": false
      },
      "CreatedTopicView": {
        "description": "One topic the run created.",
        "type": "object",
        "required": [
          "name"
        ],
        "properties": {
          "name": {
            "description": "Its name.",
            "type": "string"
          },
          "partitions": {
            "description": "Its partition count.",
            "type": "integer",
            "format": "int64",
            "nullable": true
          }
        }
      },
      "Destination": {
        "description": "A saved destination.",
        "type": "object",
        "required": [
          "access",
          "canonicalUrl",
          "default",
          "generation",
          "name",
          "namespace",
          "resourceVersion",
          "status",
          "storage",
          "transport",
          "uid",
          "writeProbe"
        ],
        "properties": {
          "access": {
            "description": "The four grants, references only.",
            "$ref": "#/components/schemas/AccessView"
          },
          "canonicalUrl": {
            "description": "The archive root as one URL, so an operator and a plan never spell one location two ways.",
            "type": "string"
          },
          "createdAt": {
            "description": "When the object was created.",
            "type": "string",
            "format": "date-time",
            "nullable": true
          },
          "default": {
            "description": "Whether this is the namespace's default destination.",
            "type": "boolean"
          },
          "description": {
            "description": "What this destination is.",
            "type": "string",
            "nullable": true
          },
          "generation": {
            "description": "The `metadata.generation`; send it back as `expectedGeneration`.",
            "type": "integer",
            "format": "int64"
          },
          "lastTest": {
            "description": "The most recent explicit access test.",
            "$ref": "#/components/schemas/LastTestView",
            "nullable": true
          },
          "locationDigest": {
            "description": "`sha256:<hex>` over the canonical location, as the controller computed it. Absent until the controller has observed the object.",
            "type": "string",
            "nullable": true
          },
          "name": {
            "description": "The object name, which is how everything else references it.",
            "type": "string"
          },
          "namespace": {
            "description": "The namespace.",
            "type": "string"
          },
          "resourceVersion": {
            "description": "The resourceVersion this projection was read at.",
            "type": "string"
          },
          "status": {
            "description": "The controller's verdict.",
            "$ref": "#/components/schemas/DestinationStatusView"
          },
          "storage": {
            "description": "The location.",
            "$ref": "#/components/schemas/StorageView"
          },
          "transport": {
            "description": "Transport security.",
            "$ref": "#/components/schemas/TransportView"
          },
          "uid": {
            "description": "The Kubernetes UID.",
            "type": "string"
          },
          "writeProbe": {
            "description": "Whether a test may write a marker.",
            "$ref": "#/components/schemas/WriteProbeDto"
          }
        }
      },
      "DestinationAccessPreflightRequest": {
        "description": "The destination-access half of a preflight request.",
        "type": "object",
        "required": [
          "destination",
          "roles"
        ],
        "properties": {
          "destination": {
            "description": "The destination to exercise.",
            "type": "string"
          },
          "roles": {
            "description": "The roles to exercise, 1 to 4.",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/DestinationRoleDto"
            }
          }
        },
        "additionalProperties": false
      },
      "DestinationFromLegacyRequest": {
        "description": "`POST .../destinations:from-legacy`.",
        "type": "object",
        "required": [
          "access",
          "name"
        ],
        "properties": {
          "access": {
            "description": "The four credential grants for the derived location.",
            "$ref": "#/components/schemas/AccessRequest"
          },
          "description": {
            "description": "What this destination is.",
            "default": null,
            "type": "string",
            "nullable": true
          },
          "name": {
            "description": "The new destination's name.",
            "type": "string"
          },
          "sourceBackup": {
            "description": "Or the legacy `Backup`.",
            "default": null,
            "type": "string",
            "nullable": true
          },
          "sourceSchedule": {
            "description": "The legacy `BackupSchedule` to derive the location from.",
            "default": null,
            "type": "string",
            "nullable": true
          }
        },
        "additionalProperties": false
      },
      "DestinationList": {
        "description": "`GET .../destinations` — one page of rows.",
        "type": "object",
        "required": [
          "items",
          "page",
          "requestId"
        ],
        "properties": {
          "items": {
            "description": "The rows on this page.",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/DestinationSummary"
            }
          },
          "page": {
            "description": "Paging.",
            "$ref": "#/components/schemas/Page"
          },
          "requestId": {
            "description": "The request ID.",
            "type": "string"
          }
        }
      },
      "DestinationResponse": {
        "description": "One `Destination`.",
        "type": "object",
        "required": [
          "item",
          "requestId"
        ],
        "properties": {
          "item": {
            "description": "The item.",
            "$ref": "#/components/schemas/Destination"
          },
          "replayed": {
            "description": "On a durable create: whether this replays an earlier identical request.",
            "type": "boolean",
            "nullable": true
          },
          "requestId": {
            "description": "The request ID.",
            "type": "string"
          }
        }
      },
      "DestinationRoleDto": {
        "description": "Which credential a destination test exercises.",
        "oneOf": [
          {
            "description": "The grant execution Jobs write the archive with.",
            "type": "string",
            "enum": [
              "archiveWrite"
            ]
          },
          {
            "description": "The read-only archive grant.",
            "type": "string",
            "enum": [
              "archiveRead"
            ]
          },
          {
            "description": "The grant that writes evidence under `logweir/`.",
            "type": "string",
            "enum": [
              "evidenceWrite"
            ]
          },
          {
            "description": "The grant that reads evidence back for verification.",
            "type": "string",
            "enum": [
              "evidenceRead"
            ]
          }
        ]
      },
      "DestinationStatusView": {
        "description": "The controller's verdict on a destination.",
        "type": "object",
        "properties": {
          "message": {
            "description": "The bounded message.",
            "type": "string",
            "nullable": true
          },
          "observedAt": {
            "description": "When the controller last reached it.",
            "type": "string",
            "format": "date-time",
            "nullable": true
          },
          "observedGeneration": {
            "description": "The generation the verdict was computed from.",
            "type": "integer",
            "format": "int64",
            "nullable": true
          },
          "reason": {
            "description": "The CamelCase reason.",
            "type": "string",
            "nullable": true
          },
          "valid": {
            "description": "Whether the `Valid` condition is `True`. `null` means the controller has not reached a verdict yet — which is NOT the same as invalid.",
            "type": "boolean",
            "nullable": true
          }
        }
      },
      "DestinationSummary": {
        "description": "A destination, as a list row.",
        "type": "object",
        "required": [
          "addressing",
          "canonicalUrl",
          "default",
          "generation",
          "name",
          "status",
          "transport",
          "uid"
        ],
        "properties": {
          "addressing": {
            "description": "How a request names the bucket.",
            "$ref": "#/components/schemas/AddressingDto"
          },
          "canonicalUrl": {
            "description": "The archive root as one URL.",
            "type": "string"
          },
          "default": {
            "description": "Whether this is the namespace's default destination.",
            "type": "boolean"
          },
          "description": {
            "description": "What this destination is.",
            "type": "string",
            "nullable": true
          },
          "endpoint": {
            "description": "The endpoint origin; absent means AWS S3.",
            "type": "string",
            "nullable": true
          },
          "generation": {
            "description": "The `metadata.generation`.",
            "type": "integer",
            "format": "int64"
          },
          "name": {
            "description": "The object name.",
            "type": "string"
          },
          "status": {
            "description": "The controller's verdict.",
            "$ref": "#/components/schemas/DestinationStatusView"
          },
          "transport": {
            "description": "`tls` or `insecureHttp`.",
            "$ref": "#/components/schemas/TransportSecurityDto"
          },
          "uid": {
            "description": "The Kubernetes UID.",
            "type": "string"
          }
        }
      },
      "DestinationUsageResponse": {
        "description": "`GET .../destinations/{name}/usage`.",
        "type": "object",
        "required": [
          "backups",
          "basis",
          "name",
          "requestId",
          "schedules",
          "truncated"
        ],
        "properties": {
          "backups": {
            "description": "Recent backups that name it, at most 100, newest name last.",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/DestinationUseView"
            }
          },
          "basis": {
            "description": "How the lists were built, stated so an empty answer is not read as \"nothing uses this\".",
            "type": "string"
          },
          "name": {
            "description": "The destination.",
            "type": "string"
          },
          "requestId": {
            "description": "The request ID.",
            "type": "string"
          },
          "schedules": {
            "description": "Schedules that name it, at most 100.",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/DestinationUseView"
            }
          },
          "truncated": {
            "description": "Whether either list was cut.",
            "type": "boolean"
          }
        }
      },
      "DestinationUseView": {
        "description": "One object a destination is referenced by.",
        "type": "object",
        "required": [
          "kind",
          "name"
        ],
        "properties": {
          "createdAt": {
            "description": "When it was created.",
            "type": "string",
            "format": "date-time",
            "nullable": true
          },
          "kind": {
            "description": "`BackupSchedule` or `Backup`.",
            "type": "string"
          },
          "name": {
            "description": "The object name.",
            "type": "string"
          }
        }
      },
      "DetailEntryView": {
        "description": "One entry of a preflight's detail document.",
        "type": "object",
        "required": [
          "entry"
        ],
        "properties": {
          "check": {
            "description": "The check id the entry belongs to, when the producer named one.",
            "type": "string",
            "nullable": true
          },
          "entry": {
            "description": "The entry, verbatim. The producer's own bounded, redacted JSON object."
          }
        }
      },
      "DetailPageResponse": {
        "description": "`GET .../preflights/{id}/details`.",
        "type": "object",
        "required": [
          "items",
          "page",
          "requestId"
        ],
        "properties": {
          "items": {
            "description": "The entries on this page, in stored order.",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/DetailEntryView"
            }
          },
          "page": {
            "description": "Paging. `snapshot` is the details document's own digest.",
            "$ref": "#/components/schemas/Page"
          },
          "requestId": {
            "description": "The request ID.",
            "type": "string"
          }
        }
      },
      "DiagnosticObjectView": {
        "description": "The object a diagnostic is about.",
        "type": "object",
        "required": [
          "kind",
          "name"
        ],
        "properties": {
          "kind": {
            "description": "`Pod` or `Job`.",
            "type": "string"
          },
          "name": {
            "description": "Its name.",
            "type": "string"
          }
        }
      },
      "DiagnosticView": {
        "description": "One entry of the closed diagnosis list (D3 §2.3).",
        "type": "object",
        "required": [
          "code",
          "severity"
        ],
        "properties": {
          "code": {
            "description": "The CamelCase code, from D2's one classification table.",
            "type": "string"
          },
          "count": {
            "description": "How many times, capped by the controller.",
            "type": "integer",
            "format": "int64",
            "nullable": true
          },
          "firstSeen": {
            "description": "When it was first seen.",
            "type": "string",
            "format": "date-time",
            "nullable": true
          },
          "lastSeen": {
            "description": "When it was last seen.",
            "type": "string",
            "format": "date-time",
            "nullable": true
          },
          "message": {
            "description": "The sanitized explanation, at most 512 bytes.",
            "type": "string",
            "nullable": true
          },
          "object": {
            "description": "What it is about.",
            "$ref": "#/components/schemas/DiagnosticObjectView",
            "nullable": true
          },
          "severity": {
            "description": "`Warning` or `Error`.",
            "type": "string"
          }
        }
      },
      "DiscoveryConnectionView": {
        "description": "The connection a discovery was bound to when it ran.",
        "type": "object",
        "required": [
          "name"
        ],
        "properties": {
          "authMode": {
            "description": "The SASL mechanism, or `plaintext`.",
            "type": "string",
            "nullable": true
          },
          "generation": {
            "description": "Its generation when the check ran.",
            "type": "integer",
            "format": "int64",
            "nullable": true
          },
          "name": {
            "description": "The connection name.",
            "type": "string"
          },
          "principal": {
            "description": "The principal the check presented, e.g. `User:backup`.",
            "type": "string",
            "nullable": true
          },
          "uid": {
            "description": "Its UID when the check ran.",
            "type": "string",
            "nullable": true
          }
        }
      },
      "DiscoveryCountsView": {
        "description": "The counts a discovery recorded.",
        "type": "object",
        "required": [
          "errored",
          "internalExcluded",
          "listed",
          "returned"
        ],
        "properties": {
          "errored": {
            "description": "Entries whose metadata could not be read.",
            "type": "integer",
            "format": "int64"
          },
          "internalExcluded": {
            "description": "Internal topics excluded.",
            "type": "integer",
            "format": "int64"
          },
          "listed": {
            "description": "Names the broker listed.",
            "type": "integer",
            "format": "int64"
          },
          "returned": {
            "description": "Entries stored.",
            "type": "integer",
            "format": "int64"
          }
        }
      },
      "DiscoveryLatestResponse": {
        "description": "`GET .../connections/{name}/topic-discoveries?latest=true`.\n\nTWO SLOTS, NOT ONE. A failed attempt never hides the last successful inventory, and a successful inventory never hides that the newest attempt failed.",
        "type": "object",
        "required": [
          "requestId"
        ],
        "properties": {
          "lastSuccessful": {
            "description": "The newest one that produced a result.",
            "$ref": "#/components/schemas/TopicDiscovery",
            "nullable": true
          },
          "latestAttempt": {
            "description": "The newest discovery of any state.",
            "$ref": "#/components/schemas/TopicDiscovery",
            "nullable": true
          },
          "requestId": {
            "description": "The request ID.",
            "type": "string"
          }
        }
      },
      "EnforcementRunView": {
        "description": "What the last enforcement run actually did.",
        "type": "object",
        "required": [
          "deletedTruncated"
        ],
        "properties": {
          "deleted": {
            "description": "The points it deleted, by id.\n\nA LIST AND NOT A COUNT, for `TeardownView::deleted`'s reason and D3 §6.5's: the attributable record names \"every deleted point id\", and a count cannot be reconciled against the plan an administrator approved.",
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "deletedTruncated": {
            "description": "Whether `deleted` was cut short by this projection's row bound.",
            "type": "boolean"
          },
          "exitCode": {
            "description": "The worker's exit code.",
            "type": "integer",
            "format": "int32",
            "nullable": true
          },
          "failed": {
            "description": "The ones it could not.",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/FailedDeletionView"
            }
          },
          "finishedAt": {
            "description": "When it finished. Absent while a run is in flight.",
            "type": "string",
            "format": "date-time",
            "nullable": true
          },
          "objectsDeleted": {
            "description": "How many objects went.",
            "type": "integer",
            "format": "int64",
            "nullable": true
          },
          "planSha256": {
            "description": "The plan digest it executed.",
            "type": "string",
            "nullable": true
          },
          "recordKey": {
            "description": "Where the attributable record is. **Create-only and UNSIGNED**, verified by the digest beside it; no surface calls it signed.",
            "type": "string",
            "nullable": true
          },
          "recordSha256": {
            "description": "That record's digest.",
            "type": "string",
            "nullable": true
          },
          "runId": {
            "description": "The run id.",
            "type": "string",
            "nullable": true
          },
          "startedAt": {
            "description": "When it started.",
            "type": "string",
            "format": "date-time",
            "nullable": true
          }
        }
      },
      "EnforcementSettingsView": {
        "description": "The settings that make `Enforce` bounded and attributable — without the credential.",
        "type": "object",
        "required": [
          "credentialConfigured",
          "deadlineSeconds",
          "maxDeletionsPerRun",
          "maxObjectsPerRun",
          "planMaxAgeSeconds",
          "requireApprovedPlan",
          "schedule"
        ],
        "properties": {
          "approvedPlanSha256": {
            "description": "The digest an administrator approved, when there is one.",
            "type": "string",
            "nullable": true
          },
          "credentialConfigured": {
            "description": "Whether a delete-capable credential is configured. A boolean, never a name.",
            "type": "boolean"
          },
          "deadlineSeconds": {
            "description": "The run deadline.",
            "type": "integer",
            "format": "int32"
          },
          "maxDeletionsPerRun": {
            "description": "The per-run point ceiling.",
            "type": "integer",
            "format": "int32"
          },
          "maxObjectsPerRun": {
            "description": "The per-run object ceiling.",
            "type": "integer",
            "format": "int32"
          },
          "planMaxAgeSeconds": {
            "description": "How long an approved plan stays fresh.",
            "type": "integer",
            "format": "int32"
          },
          "requireApprovedPlan": {
            "description": "Whether a plan digest must be approved before any Job is created.",
            "type": "boolean"
          },
          "schedule": {
            "description": "The evaluation/enforcement cadence.",
            "type": "string"
          }
        }
      },
      "EvaluationReason": {
        "description": "Why the evaluation is not fresh, when it is not.",
        "oneOf": [
          {
            "description": "The controller evaluated this generation recently.",
            "type": "string",
            "enum": [
              "evaluated"
            ]
          },
          {
            "description": "The object carries no status at all.",
            "type": "string",
            "enum": [
              "notEvaluated"
            ]
          },
          {
            "description": "The status was computed from an older `metadata.generation`.",
            "type": "string",
            "enum": [
              "generationBehind"
            ]
          },
          {
            "description": "The status has no `evaluatedAt`.",
            "type": "string",
            "enum": [
              "noEvaluationTime"
            ]
          },
          {
            "description": "`evaluatedAt` is older than the freshness window.",
            "type": "string",
            "enum": [
              "stale"
            ]
          }
        ]
      },
      "EvaluationState": {
        "description": "Why an evaluation is or is not believed.",
        "oneOf": [
          {
            "description": "The controller evaluated the current generation recently.",
            "type": "string",
            "enum": [
              "fresh"
            ]
          },
          {
            "description": "Nothing fresh: no status, a status computed from an older generation, or an `evaluatedAt` older than [`FRESH_EVALUATION_SECONDS`]. **Not `valid`.**",
            "type": "string",
            "enum": [
              "unknown"
            ]
          }
        ]
      },
      "ExecutionOnlyView": {
        "description": "A check that cannot be answered before the run.",
        "type": "object",
        "required": [
          "id",
          "note"
        ],
        "properties": {
          "id": {
            "description": "The check id.",
            "type": "string"
          },
          "note": {
            "description": "Why it is only knowable at execution time.",
            "type": "string"
          }
        }
      },
      "ExistingSecretRequest": {
        "description": "An existing Secret in this namespace, and the keys inside it.",
        "type": "object",
        "required": [
          "name"
        ],
        "properties": {
          "accessKeyIdKey": {
            "description": "The data key holding the access key id. Absent means `access-key-id`.",
            "default": null,
            "type": "string",
            "nullable": true
          },
          "name": {
            "description": "The Secret name.",
            "type": "string"
          },
          "secretAccessKeyKey": {
            "description": "The data key holding the secret access key. Absent means `secret-access-key`.",
            "default": null,
            "type": "string",
            "nullable": true
          },
          "sessionTokenKey": {
            "description": "The data key holding a session token. No default.",
            "default": null,
            "type": "string",
            "nullable": true
          }
        },
        "additionalProperties": false
      },
      "ExpectedTopicsView": {
        "description": "What became of the expected topics.",
        "type": "object",
        "required": [
          "notAuthorized",
          "notFound",
          "requested",
          "unknown",
          "visible"
        ],
        "properties": {
          "notAuthorized": {
            "description": "Refused by authorization.",
            "type": "integer",
            "format": "int64"
          },
          "notFound": {
            "description": "Absent from the cluster.",
            "type": "integer",
            "format": "int64"
          },
          "requested": {
            "description": "How many were asked about.",
            "type": "integer",
            "format": "int64"
          },
          "unknown": {
            "description": "Neither confirmed nor refuted.",
            "type": "integer",
            "format": "int64"
          },
          "visible": {
            "description": "Visible in the listing.",
            "type": "integer",
            "format": "int64"
          }
        }
      },
      "ExternalLifecycleView": {
        "description": "The declared, unverified provider lifecycle rule.",
        "type": "object",
        "required": [
          "expirationDays",
          "prefix",
          "provider",
          "ruleId"
        ],
        "properties": {
          "expirationDays": {
            "description": "The declared expiry.",
            "type": "integer",
            "format": "int32"
          },
          "prefix": {
            "description": "The declared prefix.",
            "type": "string"
          },
          "provider": {
            "description": "`s3`, `gcs` or `azure`.",
            "type": "string"
          },
          "ruleId": {
            "description": "The rule id, as the operator declared it. **Logweir has not read it.**",
            "type": "string"
          }
        }
      },
      "FailedDeletionView": {
        "description": "One point a run could not delete.",
        "type": "object",
        "required": [
          "code",
          "pointId"
        ],
        "properties": {
          "code": {
            "description": "The refusal code — `AccessDenied`, `Locked`, `PreconditionFailed` and the rest are NOT retried and keep the point.",
            "type": "string"
          },
          "pointId": {
            "description": "The point.",
            "type": "string"
          }
        }
      },
      "FieldError": {
        "description": "One field-level validation failure.",
        "type": "object",
        "required": [
          "code",
          "field",
          "message"
        ],
        "properties": {
          "code": {
            "description": "A stable machine-readable code for this field, e.g. `required`.",
            "type": "string"
          },
          "field": {
            "description": "The JSON path of the field, e.g. `topics[2]`, or a header or query parameter name.",
            "type": "string"
          },
          "message": {
            "description": "A human-readable sentence. Never echoes a credential.",
            "type": "string"
          }
        }
      },
      "GuaranteesView": {
        "description": "Which guarantees are in force, and by whom.",
        "type": "object",
        "properties": {
          "activeRestoreProtection": {
            "description": "Protection of a point an active restore needs.",
            "type": "string",
            "nullable": true
          },
          "ageExpiry": {
            "description": "Age-based expiry.",
            "type": "string",
            "nullable": true
          },
          "legalHold": {
            "description": "Legal hold.",
            "type": "string",
            "nullable": true
          },
          "minUsablePoints": {
            "description": "The floor on usable points.",
            "type": "string",
            "nullable": true
          },
          "sharedSegments": {
            "description": "Protection of a segment two manifests share.",
            "type": "string",
            "nullable": true
          }
        }
      },
      "HealthStatus": {
        "description": "Health and readiness bodies.",
        "type": "object",
        "required": [
          "status"
        ],
        "properties": {
          "status": {
            "description": "`ok` (liveness), `ready` or `notReady`.",
            "type": "string"
          }
        }
      },
      "HistogramBucketView": {
        "description": "How many points a day holds.",
        "type": "object",
        "required": [
          "day",
          "points"
        ],
        "properties": {
          "day": {
            "description": "`YYYY-MM-DD`.",
            "type": "string"
          },
          "points": {
            "description": "How many points.",
            "type": "integer",
            "format": "int64"
          }
        }
      },
      "IncompleteDiscoveryPolicy": {
        "description": "What a dynamic run does when discovery cannot prove it saw everything. REQUIRED, WITH NO DEFAULT: both possible defaults are wrong in a way the operator would not notice (D1 §0.1).",
        "oneOf": [
          {
            "description": "Fail the run.",
            "type": "string",
            "enum": [
              "Refuse"
            ]
          },
          {
            "description": "Back up what was visible and label the run `VisibleUserTopicsOnly`.",
            "type": "string",
            "enum": [
              "BackUpVisibleTopics"
            ]
          }
        ]
      },
      "KeyPrincipalView": {
        "description": "Who holds a key.",
        "type": "object",
        "required": [
          "id"
        ],
        "properties": {
          "display": {
            "description": "A label. Never authority.",
            "type": "string",
            "nullable": true
          },
          "id": {
            "description": "The stable identity.",
            "type": "string"
          }
        }
      },
      "LastAttemptView": {
        "description": "The most recent run, whatever became of it.",
        "type": "object",
        "properties": {
          "at": {
            "description": "When.",
            "type": "string",
            "format": "date-time",
            "nullable": true
          },
          "backupRef": {
            "description": "The `Backup`.",
            "$ref": "#/components/schemas/NameRef",
            "nullable": true
          },
          "phase": {
            "description": "Its phase.",
            "type": "string",
            "nullable": true
          },
          "reason": {
            "description": "Its reason.",
            "type": "string",
            "nullable": true
          }
        }
      },
      "LastSyncView": {
        "description": "The sync Job that produced this view.",
        "type": "object",
        "properties": {
          "exitCode": {
            "description": "Its exit code.",
            "type": "integer",
            "format": "int32",
            "nullable": true
          },
          "finishedAt": {
            "description": "When it finished.",
            "type": "string",
            "format": "date-time",
            "nullable": true
          },
          "refusalReason": {
            "description": "Its refusal reason, for an exit 3.",
            "type": "string",
            "nullable": true
          },
          "startedAt": {
            "description": "When it started.",
            "type": "string",
            "format": "date-time",
            "nullable": true
          }
        }
      },
      "LastTestView": {
        "description": "The most recent explicit access test.",
        "type": "object",
        "required": [
          "preflightId",
          "stale",
          "state",
          "truncated"
        ],
        "properties": {
          "observedAt": {
            "description": "When the check observed the destination.",
            "type": "string",
            "format": "date-time",
            "nullable": true
          },
          "preflightId": {
            "description": "The `Preflight` that ran it.",
            "type": "string"
          },
          "stale": {
            "description": "Whether the result has expired or its inputs changed. A stale test is never rendered as health.",
            "type": "boolean"
          },
          "state": {
            "description": "Its aggregate state.",
            "$ref": "#/components/schemas/PreflightState"
          },
          "truncated": {
            "description": "Whether the search for the newest test hit its page bound, so this may not be the newest one. A \"last test\" that might not be last is worse than none, so the caller is told rather than reassured.",
            "type": "boolean"
          }
        }
      },
      "ManualBackupResponse": {
        "description": "`POST /api/v1/namespaces/{ns}/backups`: the created run, and the schedule it came from.",
        "type": "object",
        "required": [
          "item",
          "replayed",
          "requestId"
        ],
        "properties": {
          "item": {
            "description": "The created run.",
            "$ref": "#/components/schemas/Backup"
          },
          "replayed": {
            "description": "Whether this response replays an earlier identical request (HTTP 200) rather than creating (HTTP 201).",
            "type": "boolean"
          },
          "requestId": {
            "description": "The request ID.",
            "type": "string"
          },
          "schedule": {
            "description": "The schedule, when the run was taken from one.",
            "$ref": "#/components/schemas/ScheduleContextView",
            "nullable": true
          }
        }
      },
      "NameRef": {
        "description": "A same-namespace reference by name.",
        "type": "object",
        "required": [
          "name"
        ],
        "properties": {
          "name": {
            "description": "The referenced object's name, in the same namespace.",
            "type": "string"
          }
        },
        "additionalProperties": false
      },
      "NamespaceConflictView": {
        "description": "A namespace two policies both claim. It resolves to NOTHING, and every approval and verification in it is refused.",
        "type": "object",
        "required": [
          "namespace",
          "policies"
        ],
        "properties": {
          "namespace": {
            "description": "The namespace.",
            "type": "string"
          },
          "policies": {
            "description": "The policies claiming it.",
            "type": "array",
            "items": {
              "type": "string"
            }
          }
        }
      },
      "NamespaceGrant": {
        "description": "One explicitly granted namespace.",
        "type": "object",
        "required": [
          "capabilities",
          "name",
          "roles"
        ],
        "properties": {
          "capabilities": {
            "description": "What the actor may do there. Domains without a route are `false`.",
            "$ref": "#/components/schemas/Capabilities"
          },
          "name": {
            "description": "The namespace name.",
            "type": "string"
          },
          "roles": {
            "description": "The product roles the actor holds here, sorted and unioned across every binding that matched. Empty in localAdmin mode, which has no roles.",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/Role"
            }
          }
        }
      },
      "NamespaceListResponse": {
        "description": "`GET /api/v1/namespaces`.",
        "type": "object",
        "required": [
          "items",
          "requestId"
        ],
        "properties": {
          "items": {
            "description": "The configured grants. Core `Namespace` objects are never listed.",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/NamespaceGrant"
            }
          },
          "requestId": {
            "description": "The request ID.",
            "type": "string"
          }
        }
      },
      "NewCredentialRequest": {
        "description": "A credential value typed once.\n\nWRITE-ONLY. These bytes are turned into a Secret and are never echoed, in any response, log line, status, annotation or audit record. The Secret is created and never read back; the only thing that comes out of this field is a Secret NAME.",
        "type": "object",
        "required": [
          "accessKeyId",
          "secretAccessKey"
        ],
        "properties": {
          "accessKeyId": {
            "description": "The access key id.",
            "type": "string"
          },
          "secretAccessKey": {
            "description": "The secret access key.",
            "type": "string"
          },
          "sessionToken": {
            "description": "A session token, when the credential is temporary.",
            "default": null,
            "type": "string",
            "nullable": true
          }
        },
        "additionalProperties": false
      },
      "NextRunView": {
        "description": "One upcoming firing: the UTC instant, the local wall time with its offset, and what the local clock did to reach it.",
        "type": "object",
        "required": [
          "at",
          "localTime"
        ],
        "properties": {
          "adjustment": {
            "description": "The marker, omitted when the instant needed no adjustment.",
            "$ref": "#/components/schemas/CadenceAdjustment",
            "nullable": true
          },
          "at": {
            "description": "The UTC instant. This, and only this, is the slot's identity.",
            "type": "string",
            "format": "date-time"
          },
          "localTime": {
            "description": "The same instant in the schedule's zone, offset included — `2026-10-25T02:30:00+02:00`. The offset is what distinguishes the two occurrences of a repeated hour.",
            "type": "string"
          }
        }
      },
      "NotificationRouteView": {
        "description": "One configured alert destination — its name and which channels it has.\n\nTHE CHANNELS ARE BOOLEANS ON PURPOSE. A PagerDuty routing key, a webhook URL and a Slack webhook URL are each a credential; so is the name of the Secret that holds one, to a reader deciding what to try to read next.",
        "type": "object",
        "required": [
          "name",
          "pagerDuty",
          "slack",
          "webhook"
        ],
        "properties": {
          "name": {
            "description": "What status and logs call it.",
            "type": "string"
          },
          "pagerDuty": {
            "description": "Whether a PagerDuty channel is configured.",
            "type": "boolean"
          },
          "slack": {
            "description": "Whether a Slack channel is configured.",
            "type": "boolean"
          },
          "webhook": {
            "description": "Whether a webhook channel is configured.",
            "type": "boolean"
          }
        }
      },
      "NotificationsView": {
        "description": "Where alerts go, and which ones.",
        "type": "object",
        "required": [
          "renotifyAfterSeconds",
          "sendResolved"
        ],
        "properties": {
          "kinds": {
            "description": "Which alert kinds are sent. Absent means all of them.",
            "type": "array",
            "items": {
              "type": "string"
            },
            "nullable": true
          },
          "renotifyAfterSeconds": {
            "description": "How long before an open alert is re-sent; `0` disables re-notify.",
            "type": "integer",
            "format": "int32"
          },
          "routes": {
            "description": "The routes, at most four.",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/NotificationRouteView"
            }
          },
          "sendResolved": {
            "description": "Whether a resolution is delivered too.",
            "type": "boolean"
          }
        }
      },
      "ObjectivesView": {
        "description": "What \"protected\" means here.",
        "type": "object",
        "required": [
          "maxConsecutiveFailedRuns",
          "maxRecoveryPointAgeSeconds",
          "requireCatalogAvailability",
          "requireVerifiedEvidence"
        ],
        "properties": {
          "maxConsecutiveFailedRuns": {
            "description": "How many consecutive failed runs are tolerated.",
            "type": "integer",
            "format": "int32"
          },
          "maxRecoveryPointAgeSeconds": {
            "description": "How old the newest recovery point may be, measured from CAPTURE START.",
            "type": "integer",
            "format": "int32"
          },
          "maxRehearsalAgeSeconds": {
            "description": "How old the newest successful rehearsal may be.",
            "type": "integer",
            "format": "int32",
            "nullable": true
          },
          "requireCatalogAvailability": {
            "description": "Whether the catalog must say the point is still in storage.",
            "type": "boolean"
          },
          "requireVerifiedEvidence": {
            "description": "Whether unverified evidence counts as protection.",
            "type": "boolean"
          }
        }
      },
      "ObservedAuthView": {
        "description": "The identity a run presented.",
        "type": "object",
        "properties": {
          "mode": {
            "description": "`plaintext` or `scramSha512`.",
            "type": "string",
            "nullable": true
          },
          "username": {
            "description": "The SASL principal presented.",
            "type": "string",
            "nullable": true
          }
        }
      },
      "Operation": {
        "description": "The normalized status of one Backup or Restore.",
        "type": "object",
        "required": [
          "conditions",
          "evidence",
          "kind",
          "name",
          "namespace",
          "resourceVersion",
          "result",
          "state",
          "terminal",
          "uid",
          "verification",
          "verifiedSuccess"
        ],
        "properties": {
          "conditions": {
            "description": "The status conditions, at most 16.",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/ConditionView"
            }
          },
          "createdAt": {
            "description": "When the object was created.",
            "type": "string",
            "format": "date-time",
            "nullable": true
          },
          "evidence": {
            "description": "Evidence object references.",
            "$ref": "#/components/schemas/OperationEvidence"
          },
          "kind": {
            "description": "`backup` or `restore`.",
            "$ref": "#/components/schemas/OperationKind"
          },
          "lastUpdatedAt": {
            "description": "The most recent condition transition or verification instant.",
            "type": "string",
            "format": "date-time",
            "nullable": true
          },
          "message": {
            "description": "The message of the condition that carries the reason, bounded.",
            "type": "string",
            "nullable": true
          },
          "name": {
            "description": "The object name.",
            "type": "string"
          },
          "namespace": {
            "description": "The namespace.",
            "type": "string"
          },
          "resourceVersion": {
            "description": "The resourceVersion this status was read at.",
            "type": "string"
          },
          "result": {
            "description": "The result, separate from verification.",
            "$ref": "#/components/schemas/OperationResult"
          },
          "state": {
            "description": "The lifecycle state.",
            "$ref": "#/components/schemas/OperationState"
          },
          "stateReason": {
            "description": "The CamelCase reason for the state.",
            "type": "string",
            "nullable": true
          },
          "terminal": {
            "description": "Whether the state is final.",
            "type": "boolean"
          },
          "uid": {
            "description": "The Kubernetes UID.",
            "type": "string"
          },
          "verification": {
            "description": "Evidence verification, separate from the result.",
            "$ref": "#/components/schemas/OperationVerification"
          },
          "verifiedSuccess": {
            "description": "True only when the result passed AND the evidence verified `Valid` (the controller's green-badge rule). `NotAttempted` is never verified success.",
            "type": "boolean"
          }
        }
      },
      "OperationEvidence": {
        "description": "Evidence object references. Keys and digests only.",
        "type": "object",
        "properties": {
          "offsetReportKey": {
            "description": "The restore offset report key.",
            "type": "string",
            "nullable": true
          },
          "offsetReportSha256": {
            "description": "The restore offset report SHA-256.",
            "type": "string",
            "nullable": true
          },
          "payloadKey": {
            "description": "The signed receipt (backup) or scorecard (restore) key.",
            "type": "string",
            "nullable": true
          },
          "payloadSha256": {
            "description": "Its recorded SHA-256.",
            "type": "string",
            "nullable": true
          },
          "sidecarKey": {
            "description": "The detached DSSE sidecar key.",
            "type": "string",
            "nullable": true
          }
        }
      },
      "OperationKind": {
        "description": "The two operation kinds with a status route in this build.",
        "oneOf": [
          {
            "description": "A `Backup`.",
            "type": "string",
            "enum": [
              "backup"
            ]
          },
          {
            "description": "A `Restore`.",
            "type": "string",
            "enum": [
              "restore"
            ]
          }
        ]
      },
      "OperationResponse": {
        "description": "`GET .../operations/{kind}/{name}`.",
        "type": "object",
        "required": [
          "item",
          "requestId"
        ],
        "properties": {
          "item": {
            "description": "The normalized operation.",
            "$ref": "#/components/schemas/Operation"
          },
          "requestId": {
            "description": "The request ID.",
            "type": "string"
          }
        }
      },
      "OperationResult": {
        "description": "The result half of an operation.",
        "type": "object",
        "required": [
          "status"
        ],
        "properties": {
          "exitCode": {
            "description": "The runner's exit code, when one was recovered.",
            "type": "integer",
            "format": "int32",
            "nullable": true
          },
          "exitReason": {
            "description": "The wire exit reason or terminal state.",
            "type": "string",
            "nullable": true
          },
          "lastPhaseCompleted": {
            "description": "The last restore phase slot completed.",
            "type": "integer",
            "format": "int32",
            "nullable": true
          },
          "outcome": {
            "description": "The restore scorecard outcome (`pass`, `fail-objective`, ...).",
            "type": "string",
            "nullable": true
          },
          "status": {
            "description": "The result class.",
            "$ref": "#/components/schemas/ResultStatus"
          }
        }
      },
      "OperationStage": {
        "description": "D3 §2.5's stage vocabulary, exactly six values.",
        "oneOf": [
          {
            "description": "Accepted; nothing has been scheduled.",
            "type": "string",
            "enum": [
              "admission"
            ]
          },
          {
            "description": "A Job exists and no pod is running yet.",
            "type": "string",
            "enum": [
              "queued"
            ]
          },
          {
            "description": "The pod exists and the runner container has not started.",
            "type": "string",
            "enum": [
              "preparing"
            ]
          },
          {
            "description": "The runner is doing the work.",
            "type": "string",
            "enum": [
              "running"
            ]
          },
          {
            "description": "The runner is checking, or the controller is verifying the evidence.",
            "type": "string",
            "enum": [
              "verifying"
            ]
          },
          {
            "description": "Nothing more will happen.",
            "type": "string",
            "enum": [
              "finished"
            ]
          }
        ]
      },
      "OperationState": {
        "description": "The product lifecycle state.",
        "oneOf": [
          {
            "description": "Accepted and waiting: not yet observed by the controller, or held for an approval.",
            "type": "string",
            "enum": [
              "pending"
            ]
          },
          {
            "description": "Waiting for execution capacity. Not produced by current resources.",
            "type": "string",
            "enum": [
              "queued"
            ]
          },
          {
            "description": "The controller is materializing execution inputs.",
            "type": "string",
            "enum": [
              "preparing"
            ]
          },
          {
            "description": "The runner Job exists and has not finished.",
            "type": "string",
            "enum": [
              "running"
            ]
          },
          {
            "description": "The run finished and its signed evidence has not been verified yet.",
            "type": "string",
            "enum": [
              "verifying"
            ]
          },
          {
            "description": "The run exited 0.",
            "type": "string",
            "enum": [
              "succeeded"
            ]
          },
          {
            "description": "The run finished without a pass, crashed, or failed operationally.",
            "type": "string",
            "enum": [
              "failed"
            ]
          },
          {
            "description": "A guard or the controller refused the run before anything executed.",
            "type": "string",
            "enum": [
              "refused"
            ]
          },
          {
            "description": "Cancelled. Not produced by current resources (no cancel exists).",
            "type": "string",
            "enum": [
              "cancelled"
            ]
          },
          {
            "description": "A phase this build does not recognise.",
            "type": "string",
            "enum": [
              "unknown"
            ]
          }
        ]
      },
      "OperationSummary": {
        "description": "The status summary carried by list items.",
        "type": "object",
        "required": [
          "state",
          "terminal",
          "verificationState",
          "verifiedSuccess"
        ],
        "properties": {
          "state": {
            "description": "The lifecycle state.",
            "$ref": "#/components/schemas/OperationState"
          },
          "stateReason": {
            "description": "The CamelCase reason.",
            "type": "string",
            "nullable": true
          },
          "terminal": {
            "description": "Whether the state is final.",
            "type": "boolean"
          },
          "verificationState": {
            "description": "The verification state.",
            "$ref": "#/components/schemas/VerificationState"
          },
          "verifiedSuccess": {
            "description": "Result pass AND verification `Valid`.",
            "type": "boolean"
          }
        }
      },
      "OperationTrust": {
        "description": "The trust half of the verdict, separate from the result and from the signature check.\n\n`basis` IS NEVER ABSENT HERE, AND THAT IS D3 §12. A status with no `trust` block has not had its signing time compared with anything, so the honest projection is `none` — \"not observed\" — and never a flattering default.",
        "type": "object",
        "required": [
          "basis",
          "state"
        ],
        "properties": {
          "basis": {
            "description": "`Current`, `Historical`, `RecordedBeforeRevocation`, `Unverified` or `None`, **in the CRD's own spelling**. `None` is what an absent `trust` block projects to, which is D3 §12's sentence word for word: \"`trust` absent → `basis: None`\". The first round lowercased these and invented a vocabulary no other surface prints.",
            "type": "string"
          },
          "keyState": {
            "description": "The signing key's state when the verdict was reached.",
            "type": "string",
            "nullable": true
          },
          "policy": {
            "description": "Which policy answered.",
            "$ref": "#/components/schemas/PolicyRefView",
            "nullable": true
          },
          "signedAt": {
            "description": "The signing time the verdict was reached AGAINST. Attacker-controlled for a compromised key, which is why it is recorded and not trusted.",
            "type": "string",
            "format": "date-time",
            "nullable": true
          },
          "signingTimeRead": {
            "description": "`absent` once a bounded re-read established that the DOCUMENT carries no signing time. It is the answer, recorded in place of the question.",
            "type": "string",
            "nullable": true
          },
          "state": {
            "description": "The combined verdict.",
            "$ref": "#/components/schemas/TrustState"
          }
        }
      },
      "OperationVerification": {
        "description": "The verification half of an operation.",
        "type": "object",
        "required": [
          "state"
        ],
        "properties": {
          "detail": {
            "description": "Why the verdict is what it is, at most 512 bytes.",
            "type": "string",
            "nullable": true
          },
          "matchedKeyId": {
            "description": "The TrustRoster signing key that verified the evidence.",
            "type": "string",
            "nullable": true
          },
          "payloadType": {
            "description": "The DSSE payload type verified.",
            "type": "string",
            "nullable": true
          },
          "state": {
            "description": "The verification state.",
            "$ref": "#/components/schemas/VerificationState"
          },
          "verifiedAt": {
            "description": "When the verdict was reached.",
            "type": "string",
            "format": "date-time",
            "nullable": true
          }
        }
      },
      "OperationView": {
        "description": "The normalized operation, D3 §2.5's shape: PLAT-17.1's frozen projection plus everything D3 adds.",
        "type": "object",
        "required": [
          "awaitingApproval",
          "conditions",
          "evidence",
          "kind",
          "name",
          "namespace",
          "readiness",
          "resourceVersion",
          "result",
          "stale",
          "state",
          "terminal",
          "trust",
          "uid",
          "verification",
          "verificationScope",
          "verifiedSuccess"
        ],
        "properties": {
          "awaitingApproval": {
            "description": "Whether the run is held for a human approval.",
            "type": "boolean"
          },
          "capture": {
            "description": "A Backup's capture window.",
            "$ref": "#/components/schemas/CaptureView",
            "nullable": true
          },
          "completion": {
            "description": "A Restore's completion panel.",
            "$ref": "#/components/schemas/CompletionView",
            "nullable": true
          },
          "conditions": {
            "description": "The status conditions, at most 16.",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/ConditionView"
            }
          },
          "createdAt": {
            "description": "When the object was created.",
            "type": "string",
            "format": "date-time",
            "nullable": true
          },
          "diagnostics": {
            "description": "The closed diagnosis list, newest `lastSeen` first, at most eight.",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/DiagnosticView"
            }
          },
          "evidence": {
            "description": "Evidence object references.",
            "$ref": "#/components/schemas/OperationEvidence"
          },
          "kind": {
            "description": "`backup` or `restore`.",
            "$ref": "#/components/schemas/OperationKind"
          },
          "lastUpdatedAt": {
            "description": "The most recent condition transition or verification instant.",
            "type": "string",
            "format": "date-time",
            "nullable": true
          },
          "message": {
            "description": "The message of the condition that carries the reason, bounded.",
            "type": "string",
            "nullable": true
          },
          "name": {
            "description": "The object name.",
            "type": "string"
          },
          "namespace": {
            "description": "The namespace.",
            "type": "string"
          },
          "progress": {
            "description": "The progress channel. Absent on anything an older controller wrote, and its absence is why `queued` and `preparing` are never inferred.",
            "$ref": "#/components/schemas/ProgressView",
            "nullable": true
          },
          "readiness": {
            "description": "Configuration readiness, which is a separate object.",
            "$ref": "#/components/schemas/ReadinessView"
          },
          "resourceVersion": {
            "description": "The resourceVersion this status was read at.",
            "type": "string"
          },
          "result": {
            "description": "The result, separate from verification.",
            "$ref": "#/components/schemas/OperationResult"
          },
          "stage": {
            "description": "The controller's stage. Absent means no stage was observed — an older controller, or a phase this build does not recognise.",
            "$ref": "#/components/schemas/OperationStage",
            "nullable": true
          },
          "stale": {
            "description": "Whether the status itself is too old to believe (D3 §2.5's last row).",
            "type": "boolean"
          },
          "state": {
            "description": "The lifecycle state.",
            "$ref": "#/components/schemas/OperationState"
          },
          "stateReason": {
            "description": "The CamelCase reason for the state.",
            "type": "string",
            "nullable": true
          },
          "targetMode": {
            "description": "`scratch` or `newTopic` — a Restore's target mode, absent on a Backup.\n\nAT THE TOP LEVEL, NOT ON THE COMPLETION PANEL. D3 §3.5 keys its two fixed guidance blocks on `spec.target.mode`, and that is a fact about the run from the moment it is created — a rehearsal is a rehearsal before its scorecard exists. The first round hid it inside `completion`, so a Restore that had not finished could not be labelled.",
            "type": "string",
            "nullable": true
          },
          "teardown": {
            "description": "A Restore's teardown outcome.",
            "$ref": "#/components/schemas/TeardownView",
            "nullable": true
          },
          "terminal": {
            "description": "Whether the state is final.",
            "type": "boolean"
          },
          "trust": {
            "description": "The evidence verdict in trust terms, beside — never instead of — the signature result in `verification`.",
            "$ref": "#/components/schemas/OperationTrust"
          },
          "uid": {
            "description": "The Kubernetes UID.",
            "type": "string"
          },
          "verification": {
            "description": "Evidence verification, separate from the result.",
            "$ref": "#/components/schemas/OperationVerification"
          },
          "verificationScope": {
            "description": "What the verification covered.",
            "$ref": "#/components/schemas/VerificationScopeView"
          },
          "verifiedSuccess": {
            "description": "True only when the result passed AND the evidence verified `Valid` (the controller's green-badge rule). `NotAttempted` is never verified success.",
            "type": "boolean"
          }
        }
      },
      "OperationViewResponse": {
        "description": "`GET .../operations/{kind}/{name}` for a backup or a restore.",
        "type": "object",
        "required": [
          "item",
          "requestId"
        ],
        "properties": {
          "item": {
            "description": "The normalized operation: PLAT-17.1's projection plus D3 §2.5's additions.",
            "$ref": "#/components/schemas/OperationView"
          },
          "requestId": {
            "description": "The request ID.",
            "type": "string"
          }
        }
      },
      "Page": {
        "description": "One page of a list.",
        "type": "object",
        "required": [
          "limit"
        ],
        "properties": {
          "limit": {
            "description": "The page size that was applied.",
            "type": "integer",
            "format": "uint32",
            "minimum": 0.0
          },
          "nextCursor": {
            "description": "An opaque cursor for the next page, valid for fifteen minutes for the same actor, route, namespace and filters; absent on the last page.",
            "type": "string",
            "nullable": true
          },
          "snapshot": {
            "description": "The Kubernetes list resourceVersion this page was read at.",
            "type": "string",
            "nullable": true
          }
        }
      },
      "PointLocationView": {
        "description": "One location a point was seen at.",
        "type": "object",
        "required": [
          "availability",
          "locationId"
        ],
        "properties": {
          "availability": {
            "description": "What was found THERE.",
            "type": "string"
          },
          "locationId": {
            "description": "`s3://<bucket>/<prefix>` — bucket and prefix only. No endpoint, no region, no credential.",
            "type": "string"
          }
        }
      },
      "PointPageResponse": {
        "description": "One page of the materialised point view.",
        "type": "object",
        "required": [
          "incomplete",
          "items",
          "page",
          "requestId",
          "truncated",
          "viewExpired"
        ],
        "properties": {
          "incomplete": {
            "description": "Whether a ConfigMap page named by this view disappeared while it was being read. Present only for the incomplete read so older clients keep their established `false` behavior when the member is absent.",
            "type": "boolean"
          },
          "items": {
            "description": "The points, newest first, in the order the view materialised them.",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/PointView"
            }
          },
          "page": {
            "description": "Paging. `snapshot` is the view generation this page came from.",
            "$ref": "#/components/schemas/Page"
          },
          "requestId": {
            "description": "The request ID.",
            "type": "string"
          },
          "truncated": {
            "description": "Whether the archive holds more points than the view carries.",
            "type": "boolean"
          },
          "viewExpired": {
            "description": "Whether the view has aged out. An empty `items` with this `true` means \"the window is gone\", never \"the archive is empty\".",
            "type": "boolean"
          },
          "viewExpiresAt": {
            "description": "When the view ages out.",
            "type": "string",
            "format": "date-time",
            "nullable": true
          }
        }
      },
      "PointSelectorView": {
        "description": "Which point a rehearsal takes.",
        "type": "object",
        "required": [
          "minAgeSeconds",
          "requireVerifiedEvidence",
          "selection"
        ],
        "properties": {
          "catalogRef": {
            "description": "The catalog candidates come from.",
            "$ref": "#/components/schemas/NameRef",
            "nullable": true
          },
          "minAgeSeconds": {
            "description": "How old a point must be before it qualifies.",
            "type": "integer",
            "format": "int32"
          },
          "requireVerifiedEvidence": {
            "description": "Whether unverified evidence disqualifies a point. CEL pins it `true` in v1: a rehearsal over unverified evidence proves the archive is readable and nothing about whether it is trustworthy.",
            "type": "boolean"
          },
          "scheduleRefs": {
            "description": "Candidate schedules.",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/NameRef"
            }
          },
          "selection": {
            "description": "`NewestAvailable` — the only strategy in v1.",
            "type": "string"
          },
          "topics": {
            "description": "The topics the rehearsal restores.",
            "type": "array",
            "items": {
              "type": "string"
            },
            "nullable": true
          }
        }
      },
      "PointView": {
        "description": "One recovery point, with its two verdicts kept apart.",
        "type": "object",
        "required": [
          "availability",
          "backupId",
          "pointId",
          "receiptKey",
          "receiptSha256",
          "runId",
          "selectable",
          "verification"
        ],
        "properties": {
          "availability": {
            "description": "`Available`, `Missing`, `Unreadable`, `Deleted`, `Conflict`, `UnsupportedFormat` or `Partial` — the BEST of its locations.",
            "type": "string"
          },
          "backupId": {
            "description": "The backup set it belongs to.",
            "type": "string"
          },
          "coveredFrom": {
            "description": "The start of the window it covers.",
            "type": "string",
            "format": "date-time",
            "nullable": true
          },
          "coveredTo": {
            "description": "The end of the window it covers. The restore window's end is inclusive, so a plan restores to `coveredTo` minus one millisecond.",
            "type": "string",
            "format": "date-time",
            "nullable": true
          },
          "formatVersion": {
            "description": "The record's format version.",
            "type": "string",
            "nullable": true
          },
          "locations": {
            "description": "Every location it was seen at, each with its own availability.",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/PointLocationView"
            }
          },
          "manifestKey": {
            "description": "The manifest's object key.",
            "type": "string",
            "nullable": true
          },
          "manifestSha256": {
            "description": "The manifest's recorded SHA-256.",
            "type": "string",
            "nullable": true
          },
          "pointId": {
            "description": "The content-derived point identity.",
            "type": "string"
          },
          "receiptKey": {
            "description": "The signed receipt's object key.\n\nKEYS AND DIGESTS ONLY, NEVER CONTENT — the rule `OperationEvidence` already follows. These four are what D3 §5.5 step 4 puts in a restore plan's `source.point {point_id, receipt_key, receipt_sha256, manifest_sha256}` binding, so a console that offers \"restore this point\" cannot build the plan without them.",
            "type": "string"
          },
          "receiptSha256": {
            "description": "Its recorded SHA-256.",
            "type": "string"
          },
          "recoveryPointAt": {
            "description": "Capture start, as an instant.",
            "type": "string",
            "format": "date-time",
            "nullable": true
          },
          "remedy": {
            "description": "What to do about a degraded verdict, from the catalog's own vocabulary.",
            "type": "string",
            "nullable": true
          },
          "runId": {
            "description": "The run that produced it.",
            "type": "string"
          },
          "selectable": {
            "description": "The conjunction of the two, materialised by the controller so no surface writes D3 §5.4's selection rule a second time.",
            "type": "boolean"
          },
          "signerKeyId": {
            "description": "The key that signed it, when one was identified.",
            "type": "string",
            "nullable": true
          },
          "verification": {
            "description": "`Verified`, `VerifiedHistorical`, `UntrustedSigner`, `Revoked`, `Invalid`, `NoEvidence` or `NotAttempted` — the WORST of its locations.",
            "type": "string"
          }
        }
      },
      "PolicyChangedDetail": {
        "description": "What a schedule's policy is NOW, on `policy_changed` (D1 §8.2).\n\nA PROBLEM EXTENSION, NOT A FIELD ERROR. `expectedGeneration` was not malformed — it named a revision that has been superseded — so the answer a console needs is the current revision, not a sentence about a field. RFC 9457 permits extension members; this is the only one this API defines.",
        "type": "object",
        "required": [
          "currentGeneration"
        ],
        "properties": {
          "currentGeneration": {
            "description": "The schedule's current `metadata.generation`.",
            "type": "integer",
            "format": "int64"
          },
          "currentRunPolicySha256": {
            "description": "Its current run-policy digest, when the controller has recorded one. ABSENT means the controller has not evaluated this revision yet, never that the policy is empty.",
            "type": "string",
            "nullable": true
          }
        }
      },
      "PolicyRefView": {
        "description": "Which trust policy answered, and the revision that answered.",
        "type": "object",
        "properties": {
          "generation": {
            "description": "Its `metadata.generation`.",
            "type": "integer",
            "format": "int64",
            "nullable": true
          },
          "name": {
            "description": "Its name.",
            "type": "string",
            "nullable": true
          },
          "uid": {
            "description": "Its UID — a same-named replacement is a different policy.",
            "type": "string",
            "nullable": true
          }
        }
      },
      "Preflight": {
        "description": "An operation-readiness result.",
        "type": "object",
        "required": [
          "applicable",
          "binding",
          "checks",
          "conditions",
          "detailsAvailable",
          "executionOnly",
          "id",
          "namespace",
          "operation",
          "resourceVersion",
          "stale",
          "staleBasis",
          "staleReasons",
          "state",
          "terminal",
          "uid",
          "warnings"
        ],
        "properties": {
          "applicable": {
            "description": "Whether the result still describes the caller's current inputs.\n\nRECOMPUTED PER READ. A result is applicable only when it completed, has not expired, and its binding still matches: edit the plan, the target, the destination or the recovery point and this goes false.",
            "type": "boolean"
          },
          "binding": {
            "description": "Exactly what the result is about.",
            "$ref": "#/components/schemas/PreflightBindingView"
          },
          "checks": {
            "description": "The blocking checks.",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/CheckEntryView"
            }
          },
          "conditions": {
            "description": "The status conditions, at most 16.",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/ConditionView"
            }
          },
          "createdAt": {
            "description": "When the object was created.",
            "type": "string",
            "format": "date-time",
            "nullable": true
          },
          "detailsAvailable": {
            "description": "Whether a details document exists for the details route.",
            "type": "boolean"
          },
          "executionOnly": {
            "description": "The checks that can only be answered while the run executes. They never affect the aggregate, and \"ready\" never means they passed.",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/ExecutionOnlyView"
            }
          },
          "expiresAt": {
            "description": "The minimum expiry over the non-skipped checks.",
            "type": "string",
            "format": "date-time",
            "nullable": true
          },
          "id": {
            "description": "The object name, which is the id every other route uses.",
            "type": "string"
          },
          "namespace": {
            "description": "The namespace.",
            "type": "string"
          },
          "observedAt": {
            "description": "When the facts were observed.",
            "type": "string",
            "format": "date-time",
            "nullable": true
          },
          "operation": {
            "description": "What it is about.",
            "$ref": "#/components/schemas/PreflightOperationDto"
          },
          "reason": {
            "description": "The CamelCase reason.",
            "type": "string",
            "nullable": true
          },
          "resourceVersion": {
            "description": "The resourceVersion this projection was read at.",
            "type": "string"
          },
          "stale": {
            "description": "Whether it is out of date.",
            "type": "boolean"
          },
          "staleBasis": {
            "description": "What the staleness comparison actually covered, in the order it ran.\n\nAN EMPTY `staleReasons` MEANS \"I COMPARED THESE AND THEY MATCH\", and this is the list of \"these\". Without it a console cannot tell a verdict that was re-checked against live objects from one where the check was skipped, and those two look identical in every other field.",
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "staleReasons": {
            "description": "Why, when it is. Closed over [`StaleReasonKind`].",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/StaleReasonView"
            }
          },
          "state": {
            "description": "The lifecycle, and — once complete — the aggregate.",
            "$ref": "#/components/schemas/PreflightState"
          },
          "terminal": {
            "description": "Whether the state is final.",
            "type": "boolean"
          },
          "uid": {
            "description": "The Kubernetes UID.",
            "type": "string"
          },
          "warnings": {
            "description": "The advisory checks that said no.",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/CheckEntryView"
            }
          }
        }
      },
      "PreflightBindingView": {
        "description": "Exactly what a result is a result ABOUT.",
        "type": "object",
        "required": [
          "referents"
        ],
        "properties": {
          "inputsDigest": {
            "description": "The digest over every input the verdict depended on.",
            "type": "string",
            "nullable": true
          },
          "planHash": {
            "description": "The plan hash the check was bound to.",
            "type": "string",
            "nullable": true
          },
          "referents": {
            "description": "The objects it resolved, at most 16.",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/ReferentView"
            }
          }
        }
      },
      "PreflightOperationDto": {
        "description": "What a preflight is about.",
        "oneOf": [
          {
            "description": "\"Back up now\" and schedule readiness.",
            "type": "string",
            "enum": [
              "backup"
            ]
          },
          {
            "description": "Restore readiness, bound to an exact plan hash.",
            "type": "string",
            "enum": [
              "restore"
            ]
          },
          {
            "description": "An explicit destination access test.",
            "type": "string",
            "enum": [
              "destinationAccess"
            ]
          },
          {
            "description": "A source connection on its own: does this `KafkaCluster` answer, as this principal, right now (D2-SOURCECHECK). It names no destination, no plan and no topic, which is what lets the console's \"Test connection\" start one with nothing but the cluster the reader is looking at.",
            "type": "string",
            "enum": [
              "sourceConnection"
            ]
          }
        ]
      },
      "PreflightResponse": {
        "description": "One `Preflight`.",
        "type": "object",
        "required": [
          "item",
          "requestId"
        ],
        "properties": {
          "item": {
            "description": "The item.",
            "$ref": "#/components/schemas/Preflight"
          },
          "replayed": {
            "description": "On a create: whether this replays an earlier identical request.",
            "type": "boolean",
            "nullable": true
          },
          "requestId": {
            "description": "The request ID.",
            "type": "string"
          }
        }
      },
      "PreflightState": {
        "description": "The aggregate a preflight reports.",
        "oneOf": [
          {
            "description": "Accepted; not observed yet.",
            "type": "string",
            "enum": [
              "pending"
            ]
          },
          {
            "description": "Waiting for a check slot.",
            "type": "string",
            "enum": [
              "queued"
            ]
          },
          {
            "description": "The check Job exists and has not finished.",
            "type": "string",
            "enum": [
              "running"
            ]
          },
          {
            "description": "Every blocking check passed.",
            "type": "string",
            "enum": [
              "ready"
            ]
          },
          {
            "description": "A blocking check said no.",
            "type": "string",
            "enum": [
              "notReady"
            ]
          },
          {
            "description": "A blocking check could not be decided, or was skipped.",
            "type": "string",
            "enum": [
              "unknown"
            ]
          },
          {
            "description": "No result could be produced at all.",
            "type": "string",
            "enum": [
              "failed"
            ]
          },
          {
            "description": "Cancelled before a result.",
            "type": "string",
            "enum": [
              "cancelled"
            ]
          }
        ]
      },
      "Problem": {
        "description": "The rendered problem document.",
        "type": "object",
        "required": [
          "code",
          "detail",
          "requestId",
          "retryable",
          "status",
          "title",
          "type"
        ],
        "properties": {
          "code": {
            "description": "The stable code.",
            "$ref": "#/components/schemas/ProblemCode"
          },
          "detail": {
            "description": "A sentence this service wrote. Never a Kubernetes message.",
            "type": "string"
          },
          "errors": {
            "description": "Field-level failures, when the code is `validation_failed`.",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/FieldError"
            }
          },
          "policy": {
            "description": "The schedule's current revision, when the code is `policy_changed`.",
            "$ref": "#/components/schemas/PolicyChangedDetail",
            "nullable": true
          },
          "requestId": {
            "description": "The request ID, also returned as `X-Request-ID`.",
            "type": "string"
          },
          "retryable": {
            "description": "Whether repeating the identical request later can succeed.",
            "type": "boolean"
          },
          "status": {
            "description": "The HTTP status, repeated.",
            "type": "integer",
            "format": "uint16",
            "minimum": 0.0
          },
          "title": {
            "description": "A fixed title for the code.",
            "type": "string"
          },
          "type": {
            "description": "`https://logweir.dev/problems/<code>`.",
            "type": "string"
          }
        }
      },
      "ProblemCode": {
        "description": "The stable problem codes. Clients branch on `code`, never on `detail`.\n\nThe decision's baseline set is here verbatim (`unauthenticated`, `session_expired`, `forbidden`, `namespace_forbidden`, `not_found`, `idempotency_conflict`, `state_conflict`, `approval_required`, `policy_mismatch`, `precondition_failed`, `validation_failed`, `rate_limited`, `kubernetes_unavailable`, `upstream_timeout`, plus the cursor pair). The remainder name transport-level refusals the baseline did not enumerate; each is documented in the OpenAPI document.",
        "oneOf": [
          {
            "description": "400 — the body is not JSON, a query string is malformed or carries an unknown or repeated parameter.",
            "type": "string",
            "enum": [
              "malformed_request"
            ]
          },
          {
            "description": "400 — a header this service refuses outright (`Impersonate-*`).",
            "type": "string",
            "enum": [
              "header_not_allowed"
            ]
          },
          {
            "description": "400 — a durable POST arrived without `Idempotency-Key`.",
            "type": "string",
            "enum": [
              "idempotency_key_required"
            ]
          },
          {
            "description": "400 — `Idempotency-Key` is not 8–128 visible ASCII characters, or is present on a route that does not accept it.",
            "type": "string",
            "enum": [
              "idempotency_key_invalid"
            ]
          },
          {
            "description": "400 — a list cursor failed authentication or names another scope.",
            "type": "string",
            "enum": [
              "cursor_invalid"
            ]
          },
          {
            "description": "401 — no authenticated actor (PLAT-17.2 authenticators).",
            "type": "string",
            "enum": [
              "unauthenticated"
            ]
          },
          {
            "description": "401 — the session is past its signed expiry (PLAT-17.2).",
            "type": "string",
            "enum": [
              "session_expired"
            ]
          },
          {
            "description": "403 — the actor is not permitted this action.",
            "type": "string",
            "enum": [
              "forbidden"
            ]
          },
          {
            "description": "403 — the namespace is not explicitly granted to the actor.",
            "type": "string",
            "enum": [
              "namespace_forbidden"
            ]
          },
          {
            "description": "403 — an unsafe method without an `Origin` exactly equal to the configured public origin.",
            "type": "string",
            "enum": [
              "origin_mismatch"
            ]
          },
          {
            "description": "404 — no such route or no such object.",
            "type": "string",
            "enum": [
              "not_found"
            ]
          },
          {
            "description": "404 — a legacy object carries no frozen execution and the installation publishes no legacy addressing, so no destination can be derived from facts (D2 §3.12 step 2c).",
            "type": "string",
            "enum": [
              "legacy_location_unknown"
            ]
          },
          {
            "description": "405 — the route exists and does not accept this method.",
            "type": "string",
            "enum": [
              "method_not_allowed"
            ]
          },
          {
            "description": "409 — the idempotency key was already used with a different request.",
            "type": "string",
            "enum": [
              "idempotency_conflict"
            ]
          },
          {
            "description": "409 — the deterministic object exists but was not created by this idempotency scope; it is never adopted.",
            "type": "string",
            "enum": [
              "state_conflict"
            ]
          },
          {
            "description": "409 — an approval is required first (PLAT-19.2).",
            "type": "string",
            "enum": [
              "approval_required"
            ]
          },
          {
            "description": "409 — `:update-access` tried to move a destination's location; `spec.storage` is immutable (D2 R1).",
            "type": "string",
            "enum": [
              "destination_location_immutable"
            ]
          },
          {
            "description": "409 — `:update-access` tried to change a destination's transport; `spec.transport.security` is immutable in BOTH directions (D2 R2).",
            "type": "string",
            "enum": [
              "transport_downgrade_forbidden"
            ]
          },
          {
            "description": "409 — a legacy adoption named a destination whose `locationDigest` differs from the one derived from the legacy object (D2 §3.12).",
            "type": "string",
            "enum": [
              "legacy_location_mismatch"
            ]
          },
          {
            "description": "409 — a stored result chunk failed its owner, immutability or digest check (D2 §5.6). The page is refused rather than served from bytes whose provenance did not hold.",
            "type": "string",
            "enum": [
              "result_integrity_failed"
            ]
          },
          {
            "description": "409 — the bound approval policy does not match (PLAT-19.2).",
            "type": "string",
            "enum": [
              "policy_mismatch"
            ]
          },
          {
            "description": "409 — a manual run named `expectedGeneration` and the schedule's policy has moved on since (D1 §8.2). The body carries the current generation and run-policy digest so the console can show what changed; confirming starts a NEW idempotency intent, because running a policy the person did not see is not what the button promised.",
            "type": "string",
            "enum": [
              "policy_changed"
            ]
          },
          {
            "description": "410 — the list cursor expired or Kubernetes compacted its continue token; restart the list without a cursor.",
            "type": "string",
            "enum": [
              "cursor_expired"
            ]
          },
          {
            "description": "412 — `expectedResourceVersion` is not the object's current version.",
            "type": "string",
            "enum": [
              "precondition_failed"
            ]
          },
          {
            "description": "413 — the request body exceeds its limit.",
            "type": "string",
            "enum": [
              "payload_too_large"
            ]
          },
          {
            "description": "415 — an unsafe method without `Content-Type: application/json`.",
            "type": "string",
            "enum": [
              "unsupported_media_type"
            ]
          },
          {
            "description": "421 — the `Host` header names an authority this listener does not serve (DNS-rebinding defence).",
            "type": "string",
            "enum": [
              "misdirected_request"
            ]
          },
          {
            "description": "422 — the request is well-formed JSON and fails validation.",
            "type": "string",
            "enum": [
              "validation_failed"
            ]
          },
          {
            "description": "422 — a destination request breaks one of D2 §3.2's rules. It carries the same field paths and rule ids the CRD's CEL and the controller use, so all three enforcement points name one mistake the same way.",
            "type": "string",
            "enum": [
              "destination_invalid"
            ]
          },
          {
            "description": "429 — Kubernetes throttled the request; see `Retry-After`.",
            "type": "string",
            "enum": [
              "rate_limited"
            ]
          },
          {
            "description": "500 — an invariant this service holds did not hold.",
            "type": "string",
            "enum": [
              "internal_error"
            ]
          },
          {
            "description": "503 — Kubernetes could not be reached or refused this service's own identity.",
            "type": "string",
            "enum": [
              "kubernetes_unavailable"
            ]
          },
          {
            "description": "504 — the Kubernetes call exceeded its 10-second deadline.",
            "type": "string",
            "enum": [
              "upstream_timeout"
            ]
          }
        ]
      },
      "ProgressView": {
        "description": "The progress channel, absent on anything an older controller reconciled.",
        "type": "object",
        "properties": {
          "lastObservedTime": {
            "description": "When the run was last observed, in active stages only.",
            "type": "string",
            "format": "date-time",
            "nullable": true
          },
          "lastTransitionTime": {
            "description": "When the stage or reason last CHANGED.",
            "type": "string",
            "format": "date-time",
            "nullable": true
          },
          "message": {
            "description": "The sanitized message, at most 1024 bytes.",
            "type": "string",
            "nullable": true
          },
          "reason": {
            "description": "The CamelCase reason.",
            "type": "string",
            "nullable": true
          },
          "runnerPhase": {
            "description": "The runner's own phase, from its `progress-phase=` lines.\n\nNAMED AS THE CRD NAMES IT. D3 §2.2 is \"verbatim from the CRD\" and the stored field is `status.progress.runnerPhase`; this view published it as `phase` in the first round and the console had already written `runnerPhase` against the decision. One spelling, and it is the one the object carries.",
            "$ref": "#/components/schemas/RunnerPhaseView",
            "nullable": true
          },
          "stage": {
            "description": "The stage the controller wrote, when this build recognises it.",
            "$ref": "#/components/schemas/OperationStage",
            "nullable": true
          }
        }
      },
      "ProtectedPointView": {
        "description": "One point the rules selected and something else protected.",
        "type": "object",
        "required": [
          "pointId",
          "reason"
        ],
        "properties": {
          "pointId": {
            "description": "The point.",
            "type": "string"
          },
          "reason": {
            "description": "`ActiveRestore`, `MinUsablePoints`, `LegalHold`, `SharedSegment`, `Hold` or `Unknown`.",
            "type": "string"
          }
        }
      },
      "ProtectedSubjectView": {
        "description": "What the policy protects, as references.",
        "type": "object",
        "required": [
          "sourceRef"
        ],
        "properties": {
          "catalogRef": {
            "description": "The catalog that answers \"is the point still there?\".",
            "$ref": "#/components/schemas/NameRef",
            "nullable": true
          },
          "destinationRef": {
            "description": "The saved destination, when there is one.",
            "$ref": "#/components/schemas/NameRef",
            "nullable": true
          },
          "legacyArchive": {
            "description": "An inline archive, for an installation with no saved destinations. The URL has any userinfo redacted and the credential is a Secret NAME — `ArchiveView`'s rule since PLAT-08.",
            "$ref": "#/components/schemas/ArchiveView",
            "nullable": true
          },
          "scheduleRefs": {
            "description": "The schedules whose runs count.",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/NameRef"
            }
          },
          "sourceRef": {
            "description": "The source connection.",
            "$ref": "#/components/schemas/NameRef"
          },
          "topics": {
            "description": "The topics the objective is about. Absent means \"the topics of the newest matching point\", which is the honest reading of a schedule that selects dynamically — never \"all of them\".",
            "type": "array",
            "items": {
              "type": "string"
            },
            "nullable": true
          }
        }
      },
      "ProtectionPolicyList": {
        "description": "A page of protection policies.",
        "type": "object",
        "required": [
          "items",
          "page",
          "requestId"
        ],
        "properties": {
          "items": {
            "description": "The items on this page.",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/ProtectionPolicyView"
            }
          },
          "page": {
            "description": "Paging.",
            "$ref": "#/components/schemas/Page"
          },
          "requestId": {
            "description": "The request ID.",
            "type": "string"
          }
        }
      },
      "ProtectionPolicyResponse": {
        "description": "One protection policy.",
        "type": "object",
        "required": [
          "item",
          "requestId"
        ],
        "properties": {
          "item": {
            "description": "The item.",
            "$ref": "#/components/schemas/ProtectionPolicyView"
          },
          "requestId": {
            "description": "The request ID.",
            "type": "string"
          }
        }
      },
      "ProtectionPolicyView": {
        "description": "A `ProtectionPolicy`, projected.",
        "type": "object",
        "required": [
          "evaluationIntervalSeconds",
          "name",
          "namespace",
          "objectives",
          "protects",
          "resourceVersion",
          "uid"
        ],
        "properties": {
          "alerts": {
            "description": "The deduplication ledger.",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/AlertView"
            }
          },
          "availabilityBasis": {
            "description": "`KubernetesStatus`, `Catalog` or `CatalogStale` — how availability was decided. \"The point exists\" and \"a Backup object says it succeeded\" are different claims.",
            "type": "string",
            "nullable": true
          },
          "conditions": {
            "description": "`Ready`, `Protected` and `NotificationsDelivered`.",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/ConditionView"
            }
          },
          "consecutiveFailedRuns": {
            "description": "How many runs have failed in a row.",
            "type": "integer",
            "format": "int64",
            "nullable": true
          },
          "createdAt": {
            "description": "When the object was created.",
            "type": "string",
            "format": "date-time",
            "nullable": true
          },
          "evaluatedAt": {
            "description": "When the verdict was reached.",
            "type": "string",
            "format": "date-time",
            "nullable": true
          },
          "evaluationIntervalSeconds": {
            "description": "How often it is evaluated.",
            "type": "integer",
            "format": "int32"
          },
          "generation": {
            "description": "`metadata.generation`.",
            "type": "integer",
            "format": "int64",
            "nullable": true
          },
          "health": {
            "description": "`Healthy`, `AtRisk`, `Stale`, `Unprotected` or `Unknown`. Absent means nothing has been evaluated yet, which is not `Healthy`.",
            "type": "string",
            "nullable": true
          },
          "lastAttempt": {
            "description": "The most recent run.",
            "$ref": "#/components/schemas/LastAttemptView",
            "nullable": true
          },
          "lastAvailablePoint": {
            "description": "The newest point that can actually be recovered from.",
            "$ref": "#/components/schemas/AvailablePointView",
            "nullable": true
          },
          "lastMissedSlot": {
            "description": "The last slot that did not run.",
            "type": "string",
            "nullable": true
          },
          "name": {
            "description": "The object name.",
            "type": "string"
          },
          "namespace": {
            "description": "The namespace.",
            "type": "string"
          },
          "notifications": {
            "description": "Where alerts go. Absent means the verdict is reported here and delivered nowhere — a real choice, not a broken configuration.",
            "$ref": "#/components/schemas/NotificationsView",
            "nullable": true
          },
          "objectives": {
            "description": "What \"protected\" means.",
            "$ref": "#/components/schemas/ObjectivesView"
          },
          "observedGeneration": {
            "description": "The generation the verdict was computed from. Different from `generation` means the verdict predates the current objective.",
            "type": "integer",
            "format": "int64",
            "nullable": true
          },
          "protects": {
            "description": "What it protects.",
            "$ref": "#/components/schemas/ProtectedSubjectView"
          },
          "rehearsalLastFailedAt": {
            "description": "When one last failed, and why.",
            "type": "string",
            "format": "date-time",
            "nullable": true
          },
          "rehearsalLastReason": {
            "description": "The reason the last rehearsal failed or was skipped.",
            "type": "string",
            "nullable": true
          },
          "rehearsalLastRestoreRef": {
            "description": "The `Restore` that rehearsal ran as, so the console can link it.",
            "$ref": "#/components/schemas/NameRef",
            "nullable": true
          },
          "rehearsalLastSucceededAt": {
            "description": "When a rehearsal last passed.",
            "type": "string",
            "format": "date-time",
            "nullable": true
          },
          "resourceVersion": {
            "description": "The resourceVersion this projection was read at.",
            "type": "string"
          },
          "schedules": {
            "description": "The schedules, with their own readiness.",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/ScheduleHealthView"
            }
          },
          "sinceLastFire": {
            "description": "Seconds since the last successful fire.",
            "type": "integer",
            "format": "int64",
            "nullable": true
          },
          "staleSince": {
            "description": "Since when the objective has been missed.",
            "type": "string",
            "format": "date-time",
            "nullable": true
          },
          "uid": {
            "description": "The Kubernetes UID.",
            "type": "string"
          }
        }
      },
      "ReachabilityState": {
        "description": "The controller's last reachability observation.",
        "oneOf": [
          {
            "description": "The last probe reached a broker.",
            "type": "string",
            "enum": [
              "reachable"
            ]
          },
          {
            "description": "The last probe did not.",
            "type": "string",
            "enum": [
              "unreachable"
            ]
          },
          {
            "description": "No completed probe.",
            "type": "string",
            "enum": [
              "unknown"
            ]
          }
        ]
      },
      "ReachabilityView": {
        "description": "Reachability, as the controller recorded it.",
        "type": "object",
        "required": [
          "state"
        ],
        "properties": {
          "clusterId": {
            "description": "The cluster ID read from the broker.",
            "type": "string",
            "nullable": true
          },
          "observedAt": {
            "description": "When the probe ran.",
            "type": "string",
            "format": "date-time",
            "nullable": true
          },
          "reason": {
            "description": "The controller's CamelCase reason.",
            "type": "string",
            "nullable": true
          },
          "state": {
            "description": "The state.",
            "$ref": "#/components/schemas/ReachabilityState"
          }
        }
      },
      "ReadinessAcknowledgementRequest": {
        "description": "\"Run anyway\", recorded and never authoritative (D1 §8.4).\n\nTHE API AND THE CONTROLLER NEVER GATE ON READINESS. Execution-time guards stay authoritative and the direct `kubectl create -f` path exists regardless; this records that a person was shown a `notReady` or `unknown` verdict and went ahead. It is an annotation on the run, not a permission.",
        "type": "object",
        "required": [
          "preflight",
          "state"
        ],
        "properties": {
          "preflight": {
            "description": "The `Preflight` whose verdict was shown, in this namespace.",
            "type": "string"
          },
          "state": {
            "description": "What it said.",
            "$ref": "#/components/schemas/AcknowledgedReadiness"
          }
        },
        "additionalProperties": false
      },
      "ReadinessRequest": {
        "description": "Explicit readiness settings.",
        "type": "object",
        "properties": {
          "writeProbe": {
            "description": "Whether a destination test may write a marker. Absent means `createOnlyMarker` HERE, while the CRD's own default is `disabled`: a form that discloses the choice may default to the useful answer, and an object created by `kubectl` with no opinion may not.",
            "default": null,
            "$ref": "#/components/schemas/WriteProbeDto",
            "nullable": true
          }
        },
        "additionalProperties": false
      },
      "ReadinessView": {
        "description": "Configuration readiness (PLAT-03.1), which NEVER overwrites `state`.",
        "type": "object",
        "required": [
          "basis",
          "state"
        ],
        "properties": {
          "basis": {
            "description": "Why: `notImplemented`.",
            "type": "string"
          },
          "state": {
            "description": "`unknown` until PLAT-03.1 lands.",
            "type": "string"
          }
        }
      },
      "RecoveryPointRequest": {
        "description": "The recovery point a restore preflight is about.",
        "type": "object",
        "required": [
          "backupName"
        ],
        "properties": {
          "backupName": {
            "description": "The `Backup` that produced it.",
            "type": "string"
          },
          "backupUid": {
            "description": "Its UID. A deleted and recreated point is a different point.",
            "default": null,
            "type": "string",
            "nullable": true
          }
        },
        "additionalProperties": false
      },
      "ReferentView": {
        "description": "One object a preflight's binding names.",
        "type": "object",
        "required": [
          "kind",
          "name"
        ],
        "properties": {
          "generation": {
            "description": "The generation at the time of the check.",
            "type": "integer",
            "format": "int64",
            "nullable": true
          },
          "kind": {
            "description": "The kind.",
            "type": "string"
          },
          "name": {
            "description": "The name.",
            "type": "string"
          },
          "uid": {
            "description": "The UID at the time of the check.",
            "type": "string",
            "nullable": true
          }
        }
      },
      "RehearsalBoundsView": {
        "description": "The bounds a rehearsal runs inside.",
        "type": "object",
        "required": [
          "concurrencyPolicy",
          "deadlineSeconds",
          "maxPartitions",
          "recordsPerPartition",
          "startingDeadlineSeconds"
        ],
        "properties": {
          "concurrencyPolicy": {
            "description": "`Forbid` — the only policy in v1.",
            "type": "string"
          },
          "deadlineSeconds": {
            "description": "The run deadline.",
            "type": "integer",
            "format": "int32"
          },
          "maxPartitions": {
            "description": "The partition ceiling, enforced when the catalog knows the count.",
            "type": "integer",
            "format": "int32"
          },
          "recordsPerPartition": {
            "description": "How many records per partition the canary writes.",
            "type": "integer",
            "format": "int32"
          },
          "startingDeadlineSeconds": {
            "description": "The missed-slot horizon.",
            "type": "integer",
            "format": "int32"
          }
        }
      },
      "RehearsalFailureView": {
        "description": "The last rehearsal that failed.",
        "type": "object",
        "properties": {
          "at": {
            "description": "When.",
            "type": "string",
            "format": "date-time",
            "nullable": true
          },
          "reason": {
            "description": "Why.",
            "type": "string",
            "nullable": true
          },
          "restoreRef": {
            "description": "The `Restore`.",
            "$ref": "#/components/schemas/NameRef",
            "nullable": true
          }
        }
      },
      "RehearsalScheduleList": {
        "description": "A page of rehearsal schedules.",
        "type": "object",
        "required": [
          "items",
          "page",
          "requestId"
        ],
        "properties": {
          "items": {
            "description": "The items on this page.",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/RehearsalScheduleView"
            }
          },
          "page": {
            "description": "Paging.",
            "$ref": "#/components/schemas/Page"
          },
          "requestId": {
            "description": "The request ID.",
            "type": "string"
          }
        }
      },
      "RehearsalScheduleResponse": {
        "description": "One rehearsal schedule.",
        "type": "object",
        "required": [
          "item",
          "requestId"
        ],
        "properties": {
          "item": {
            "description": "The item.",
            "$ref": "#/components/schemas/RehearsalScheduleView"
          },
          "requestId": {
            "description": "The request ID.",
            "type": "string"
          }
        }
      },
      "RehearsalScheduleView": {
        "description": "A `RehearsalSchedule`, projected.",
        "type": "object",
        "required": [
          "bounds",
          "name",
          "namespace",
          "point",
          "resourceVersion",
          "schedule",
          "standingApprovalRef",
          "suspend",
          "target",
          "uid"
        ],
        "properties": {
          "activeRestoreRef": {
            "description": "The rehearsal running now.",
            "$ref": "#/components/schemas/NameRef",
            "nullable": true
          },
          "approvalPolicyRef": {
            "description": "The approval policy, when one is named.",
            "$ref": "#/components/schemas/NameRef",
            "nullable": true
          },
          "bounds": {
            "description": "The bounds.",
            "$ref": "#/components/schemas/RehearsalBoundsView"
          },
          "cleanup": {
            "description": "Leftover topics, which block the next slot.",
            "$ref": "#/components/schemas/CleanupStateView",
            "nullable": true
          },
          "conditions": {
            "description": "`Ready`, `Authorized` and `RehearsalHealthy`.",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/ConditionView"
            }
          },
          "createdAt": {
            "description": "When the object was created.",
            "type": "string",
            "format": "date-time",
            "nullable": true
          },
          "generation": {
            "description": "`metadata.generation`.",
            "type": "integer",
            "format": "int64",
            "nullable": true
          },
          "lastFailed": {
            "description": "The last failure.",
            "$ref": "#/components/schemas/RehearsalFailureView",
            "nullable": true
          },
          "lastScheduledSlot": {
            "description": "The last slot the controller scheduled.",
            "type": "string",
            "nullable": true
          },
          "lastSkipped": {
            "description": "The last skip. **A skip is not a pass and is never rendered as one.**",
            "$ref": "#/components/schemas/SkippedSlotView",
            "nullable": true
          },
          "lastSucceeded": {
            "description": "The last pass.",
            "$ref": "#/components/schemas/RehearsalSuccessView",
            "nullable": true
          },
          "name": {
            "description": "The object name.",
            "type": "string"
          },
          "namespace": {
            "description": "The namespace.",
            "type": "string"
          },
          "nextFireTime": {
            "description": "When it fires next.",
            "type": "string",
            "format": "date-time",
            "nullable": true
          },
          "observedGeneration": {
            "description": "The generation the status was computed from.",
            "type": "integer",
            "format": "int64",
            "nullable": true
          },
          "pendingRestoreRef": {
            "description": "The reservation a slot took before it created its child.",
            "$ref": "#/components/schemas/NameRef",
            "nullable": true
          },
          "point": {
            "description": "Which point.",
            "$ref": "#/components/schemas/PointSelectorView"
          },
          "protectionPolicyRef": {
            "description": "The protection policy the results surface on.",
            "$ref": "#/components/schemas/NameRef",
            "nullable": true
          },
          "resourceVersion": {
            "description": "The resourceVersion this projection was read at.",
            "type": "string"
          },
          "rtoSeconds": {
            "description": "The requested recovery time objective.",
            "type": "integer",
            "format": "int64",
            "nullable": true
          },
          "schedule": {
            "description": "The five-field UTC cron expression.",
            "type": "string"
          },
          "standingApprovalRef": {
            "description": "The `Approval` carrying the signed standing authorization. A NAME: the document and its signatures are reachable only through the approval packet route.",
            "$ref": "#/components/schemas/NameRef"
          },
          "suspend": {
            "description": "Whether it is suspended — the only mutable field of a sealed spec.",
            "type": "boolean"
          },
          "target": {
            "description": "Where it writes.",
            "$ref": "#/components/schemas/RehearsalTargetView"
          },
          "uid": {
            "description": "The Kubernetes UID.",
            "type": "string"
          }
        }
      },
      "RehearsalSuccessView": {
        "description": "The last rehearsal that passed.",
        "type": "object",
        "properties": {
          "at": {
            "description": "When.",
            "type": "string",
            "format": "date-time",
            "nullable": true
          },
          "evidence": {
            "description": "The verification verdict the run recorded.",
            "type": "string",
            "nullable": true
          },
          "pointId": {
            "description": "Which point.",
            "type": "string",
            "nullable": true
          },
          "restoreRef": {
            "description": "The `Restore` it ran as.",
            "$ref": "#/components/schemas/NameRef",
            "nullable": true
          },
          "rtoSeconds": {
            "description": "The measured recovery time.",
            "type": "integer",
            "format": "int64",
            "nullable": true
          }
        }
      },
      "RehearsalTargetView": {
        "description": "Where a rehearsal writes.",
        "type": "object",
        "required": [
          "clusterRef",
          "markerTopic",
          "replicationFactor",
          "topicPrefix"
        ],
        "properties": {
          "clusterRef": {
            "description": "The target connection.",
            "$ref": "#/components/schemas/NameRef"
          },
          "markerTopic": {
            "description": "The marker topic phase 0 checks.",
            "type": "string"
          },
          "replicationFactor": {
            "description": "The replication factor for created topics.",
            "type": "integer",
            "format": "int32"
          },
          "topicPrefix": {
            "description": "The prefix as WRITTEN in the spec. The controller renders `<prefix><uid-first-8>-` per schedule, which is what teardown's deletion guard matches.",
            "type": "string"
          }
        }
      },
      "RemovableSetView": {
        "description": "One set retention would remove. It is still in the archive.",
        "type": "object",
        "required": [
          "backupId",
          "newestRecordAt",
          "reason"
        ],
        "properties": {
          "backupId": {
            "description": "The backup set ID.",
            "type": "string"
          },
          "days": {
            "description": "The configured keepDays, for `OlderThanKeepDays`.",
            "type": "integer",
            "format": "int64",
            "nullable": true
          },
          "newestRecordAt": {
            "description": "The newest record instant in the set.",
            "type": "string",
            "format": "date-time"
          },
          "rank": {
            "description": "The newest-first rank, for `BeyondKeepLast`.",
            "type": "integer",
            "format": "int64",
            "nullable": true
          },
          "reason": {
            "description": "`OlderThanKeepDays` or `BeyondKeepLast`.",
            "type": "string"
          }
        }
      },
      "Restore": {
        "description": "A restore run.",
        "type": "object",
        "required": [
          "approvalRef",
          "backupSetRef",
          "deadlineSeconds",
          "name",
          "namespace",
          "newTopics",
          "operation",
          "planBytesLength",
          "planHash",
          "pointInTime",
          "resourceVersion",
          "sourceArchive",
          "target",
          "uid"
        ],
        "properties": {
          "approvalRef": {
            "description": "The Approval name this restore waits for.",
            "$ref": "#/components/schemas/NameRef"
          },
          "backupSetRef": {
            "description": "The backup set ID.",
            "type": "string"
          },
          "createdAt": {
            "description": "When the object was created.",
            "type": "string",
            "format": "date-time",
            "nullable": true
          },
          "deadlineSeconds": {
            "description": "The Job deadline.",
            "type": "integer",
            "format": "int64"
          },
          "evidenceDestinationRef": {
            "description": "The saved destination evidence was written to. Absent on legacy inline-archive restores.",
            "$ref": "#/components/schemas/NameRef",
            "nullable": true
          },
          "name": {
            "description": "The object name.",
            "type": "string"
          },
          "namespace": {
            "description": "The namespace.",
            "type": "string"
          },
          "newTopics": {
            "description": "The topics this run created.",
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "operation": {
            "description": "The normalized status summary.",
            "$ref": "#/components/schemas/OperationSummary"
          },
          "planBytes": {
            "description": "The stored plan bytes, unchanged. Present on the single-object read only; lists omit it.",
            "type": "string",
            "nullable": true
          },
          "planBytesLength": {
            "description": "The stored plan bytes' length in bytes.",
            "type": "integer",
            "format": "uint",
            "minimum": 0.0
          },
          "planHash": {
            "description": "`sha256:<hex>` of the stored plan bytes, computed by this service.",
            "type": "string"
          },
          "pointInTime": {
            "description": "The point in time.",
            "type": "string",
            "format": "date-time"
          },
          "resourceVersion": {
            "description": "The resourceVersion.",
            "type": "string"
          },
          "sourceArchive": {
            "description": "The archive restored from.",
            "$ref": "#/components/schemas/ArchiveView"
          },
          "sourceDestinationRef": {
            "description": "The saved destination the archive was read from. Absent on legacy inline-archive restores.",
            "$ref": "#/components/schemas/NameRef",
            "nullable": true
          },
          "target": {
            "description": "Where it writes.",
            "$ref": "#/components/schemas/RestoreTargetView"
          },
          "uid": {
            "description": "The Kubernetes UID.",
            "type": "string"
          }
        }
      },
      "RestoreList": {
        "type": "object",
        "required": [
          "items",
          "page",
          "requestId"
        ],
        "properties": {
          "items": {
            "description": "The items on this page.",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/Restore"
            }
          },
          "page": {
            "description": "Paging.",
            "$ref": "#/components/schemas/Page"
          },
          "requestId": {
            "description": "The request ID.",
            "type": "string"
          }
        }
      },
      "RestoreMode": {
        "description": "`scratch` (a drill) or `newTopic`.",
        "oneOf": [
          {
            "description": "A drill into a proven scratch cluster, torn down afterwards.",
            "type": "string",
            "enum": [
              "scratch"
            ]
          },
          {
            "description": "A real restore into new topics.",
            "type": "string",
            "enum": [
              "newTopic"
            ]
          }
        ]
      },
      "RestorePreflightRequest": {
        "description": "The restore half of a preflight request.",
        "type": "object",
        "properties": {
          "evidenceDestination": {
            "description": "The destination evidence is read from. Set together with `sourceDestination`.",
            "default": null,
            "type": "string",
            "nullable": true
          },
          "legacySourceArchive": {
            "description": "Or a legacy inline source archive.",
            "default": null,
            "$ref": "#/components/schemas/ArchiveRequest",
            "nullable": true
          },
          "planBytes": {
            "description": "The exact draft plan bytes, at most 256 KiB. FORWARDED VERBATIM: the service checks `planHash` against the SHA-256 of exactly these bytes and stores them unchanged. It never parses or re-emits the plan.",
            "default": null,
            "type": "string",
            "nullable": true
          },
          "planHash": {
            "description": "`sha256:<lowercase hex>` of `planBytes`.",
            "default": null,
            "type": "string",
            "nullable": true
          },
          "recoveryPoint": {
            "description": "The recovery point.",
            "default": null,
            "$ref": "#/components/schemas/RecoveryPointRequest",
            "nullable": true
          },
          "restoreName": {
            "description": "Or an existing `Restore` to check.",
            "default": null,
            "type": "string",
            "nullable": true
          },
          "sourceDestination": {
            "description": "The destination the archive is read from.",
            "default": null,
            "type": "string",
            "nullable": true
          },
          "target": {
            "description": "The target connection; required with `planBytes`.",
            "default": null,
            "type": "string",
            "nullable": true
          }
        },
        "additionalProperties": false
      },
      "RestoreResponse": {
        "type": "object",
        "required": [
          "item",
          "requestId"
        ],
        "properties": {
          "item": {
            "description": "The item.",
            "$ref": "#/components/schemas/Restore"
          },
          "replayed": {
            "description": "On a durable create: whether this response replays an earlier identical request (HTTP 200) rather than creating (HTTP 201).",
            "type": "boolean",
            "nullable": true
          },
          "requestId": {
            "description": "The request ID.",
            "type": "string"
          }
        }
      },
      "RestoreTargetRequest": {
        "description": "Where a restore writes.",
        "type": "object",
        "required": [
          "clusterRef",
          "mode",
          "topicNaming"
        ],
        "properties": {
          "clusterRef": {
            "description": "The target connection, in this namespace.",
            "$ref": "#/components/schemas/NameRef"
          },
          "mode": {
            "description": "`scratch` or `newTopic`.",
            "$ref": "#/components/schemas/RestoreMode"
          },
          "topicNaming": {
            "description": "Topic naming.",
            "$ref": "#/components/schemas/TopicNamingRequest"
          }
        },
        "additionalProperties": false
      },
      "RestoreTargetView": {
        "description": "A restore target, as stored.",
        "type": "object",
        "required": [
          "clusterRef",
          "mode",
          "topicPrefix"
        ],
        "properties": {
          "clusterRef": {
            "description": "The target connection.",
            "$ref": "#/components/schemas/NameRef"
          },
          "mode": {
            "description": "`scratch` or `newTopic`.",
            "$ref": "#/components/schemas/RestoreMode"
          },
          "topicPrefix": {
            "description": "The topic prefix.",
            "type": "string"
          }
        }
      },
      "ResultStatus": {
        "description": "The operation's result, separate from evidence verification.",
        "oneOf": [
          {
            "description": "No result yet.",
            "type": "string",
            "enum": [
              "pending"
            ]
          },
          {
            "description": "Exit 0 (and, for a restore, outcome `pass` when recorded).",
            "type": "string",
            "enum": [
              "pass"
            ]
          },
          {
            "description": "Exit 2: a signed result that is not a pass.",
            "type": "string",
            "enum": [
              "notPass"
            ]
          },
          {
            "description": "Exit 3, or a controller refusal before execution.",
            "type": "string",
            "enum": [
              "refused"
            ]
          },
          {
            "description": "Exit 1 or 4, a crash, or a terminal failure without a code.",
            "type": "string",
            "enum": [
              "error"
            ]
          },
          {
            "description": "A combination this build does not recognise.",
            "type": "string",
            "enum": [
              "unknown"
            ]
          }
        ]
      },
      "RetentionEvaluationView": {
        "description": "What the last evaluation found. **Nothing here was deleted.**\n\nTHE NAME CARRIES ITS DOMAIN BECAUSE THE SCHEMA NAME IS GLOBAL. `schemars` keys `components/schemas` by the type's SHORT name, so two `EvaluationView` types in two route modules silently become one schema and one of the two `$ref`s points at the wrong shape — with no error anywhere. `trust.rs` has its own evaluation view, and `no_two_published_types_share_a_schema_name` in `tests/contract.rs` is what keeps the pair honest.",
        "type": "object",
        "required": [
          "truncated"
        ],
        "properties": {
          "at": {
            "description": "When.",
            "type": "string",
            "format": "date-time",
            "nullable": true
          },
          "candidateCount": {
            "description": "How many candidates there are in total, which may exceed the rows below.",
            "type": "integer",
            "format": "int64",
            "nullable": true
          },
          "candidates": {
            "description": "The points the rules would remove.",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/CandidateView"
            }
          },
          "kept": {
            "description": "The points the rules keep.",
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "planExpiresAt": {
            "description": "When the plan ages out.",
            "type": "string",
            "format": "date-time",
            "nullable": true
          },
          "planRef": {
            "description": "The plan `ConfigMap`.",
            "$ref": "#/components/schemas/NameRef",
            "nullable": true
          },
          "planSha256": {
            "description": "The digest an administrator approves. A digest, not a credential.",
            "type": "string",
            "nullable": true
          },
          "pointsEvaluated": {
            "description": "How many points were considered.",
            "type": "integer",
            "format": "int64",
            "nullable": true
          },
          "protected": {
            "description": "The points something else protected.",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/ProtectedPointView"
            }
          },
          "skipped": {
            "description": "The points the evaluation could not classify.",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/SkippedEntryView"
            }
          },
          "truncated": {
            "description": "Whether any of the four lists above was cut short by this route's own row bound.",
            "type": "boolean"
          }
        }
      },
      "RetentionPolicyList": {
        "description": "A page of retention policies.",
        "type": "object",
        "required": [
          "items",
          "page",
          "requestId"
        ],
        "properties": {
          "items": {
            "description": "The items on this page.",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/RetentionPolicyView"
            }
          },
          "page": {
            "description": "Paging.",
            "$ref": "#/components/schemas/Page"
          },
          "requestId": {
            "description": "The request ID.",
            "type": "string"
          }
        }
      },
      "RetentionPolicyResponse": {
        "description": "One retention policy.",
        "type": "object",
        "required": [
          "item",
          "requestId"
        ],
        "properties": {
          "item": {
            "description": "The item.",
            "$ref": "#/components/schemas/RetentionPolicyView"
          },
          "requestId": {
            "description": "The request ID.",
            "type": "string"
          }
        }
      },
      "RetentionPolicyView": {
        "description": "A `RetentionPolicy`, projected.",
        "type": "object",
        "required": [
          "approvedPlanState",
          "catalogRef",
          "destinationRef",
          "enforcementDegraded",
          "holds",
          "leasedPoints",
          "minUsablePoints",
          "mode",
          "name",
          "namespace",
          "resourceVersion",
          "scopePrefix",
          "uid"
        ],
        "properties": {
          "approvedPlanState": {
            "description": "Where the approved-plan gate stands.",
            "$ref": "#/components/schemas/ApprovedPlanState"
          },
          "catalogRef": {
            "description": "The catalog it evaluates.",
            "$ref": "#/components/schemas/NameRef"
          },
          "conditions": {
            "description": "`Ready`, `Evaluated`, `Enforced`, `ExternalLifecycleConflict` and `EnforcementDegraded`.",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/ConditionView"
            }
          },
          "consecutiveRunFailures": {
            "description": "How many runs have failed in a row.",
            "type": "integer",
            "format": "int64",
            "nullable": true
          },
          "createdAt": {
            "description": "When the object was created.",
            "type": "string",
            "format": "date-time",
            "nullable": true
          },
          "destinationRef": {
            "description": "The destination it governs.",
            "$ref": "#/components/schemas/NameRef"
          },
          "enforcement": {
            "description": "**What is actually enforcing**: `RecommendationOnly`, `LogweirWorker` or `ExternalLifecycleDeclared`.",
            "type": "string",
            "nullable": true
          },
          "enforcementDegraded": {
            "description": "Whether `EnforcementDegraded` is `True`: three consecutive failed runs stop scheduling until the spec changes.",
            "type": "boolean"
          },
          "enforcementSettings": {
            "description": "The enforcement settings, when the mode has them.",
            "$ref": "#/components/schemas/EnforcementSettingsView",
            "nullable": true
          },
          "externalLifecycle": {
            "description": "The declared bucket lifecycle rule, when the mode declares one.",
            "$ref": "#/components/schemas/ExternalLifecycleView",
            "nullable": true
          },
          "generation": {
            "description": "`metadata.generation`.",
            "type": "integer",
            "format": "int64",
            "nullable": true
          },
          "guarantees": {
            "description": "Which guarantees are in force, and by whom.",
            "$ref": "#/components/schemas/GuaranteesView",
            "nullable": true
          },
          "holds": {
            "description": "How many holds are in force.",
            "type": "integer",
            "format": "int64"
          },
          "keepDays": {
            "description": "How many days they keep by age.",
            "type": "integer",
            "format": "int32",
            "nullable": true
          },
          "keepLast": {
            "description": "How many points the rules keep by rank.",
            "type": "integer",
            "format": "int32",
            "nullable": true
          },
          "lastEnforcement": {
            "description": "The last enforcement run.",
            "$ref": "#/components/schemas/EnforcementRunView",
            "nullable": true
          },
          "lastEvaluation": {
            "description": "The last evaluation.",
            "$ref": "#/components/schemas/RetentionEvaluationView",
            "nullable": true
          },
          "leasedPoints": {
            "description": "How many points a run has claimed right now. A non-zero lease is why a restore of one of them holds with `PointRetentionInProgress`.",
            "type": "integer",
            "format": "int64"
          },
          "minUsablePoints": {
            "description": "The floor, which the rules can never go under.",
            "type": "integer",
            "format": "int32"
          },
          "mode": {
            "description": "`Report`, `Enforce` or `ExternalLifecycle`, as configured.",
            "type": "string"
          },
          "name": {
            "description": "The object name.",
            "type": "string"
          },
          "namespace": {
            "description": "The namespace.",
            "type": "string"
          },
          "observedGeneration": {
            "description": "The generation the status was computed from.",
            "type": "integer",
            "format": "int64",
            "nullable": true
          },
          "resourceVersion": {
            "description": "The resourceVersion this projection was read at.",
            "type": "string"
          },
          "scopePrefix": {
            "description": "The only prefix deletion may ever touch.",
            "type": "string"
          },
          "uid": {
            "description": "The Kubernetes UID.",
            "type": "string"
          }
        }
      },
      "RetentionReportView": {
        "description": "The controller's retention report, bounded to 100 entries per list.",
        "type": "object",
        "required": [
          "mcRemovalCommands",
          "removalCommands",
          "setsKept",
          "setsThatWouldBeRemoved",
          "skippedManifests",
          "truncated"
        ],
        "properties": {
          "evaluatedAt": {
            "description": "When retention was last evaluated.",
            "type": "string",
            "format": "date-time",
            "nullable": true
          },
          "keepDays": {
            "description": "keepDays as applied.",
            "type": "integer",
            "format": "int64",
            "nullable": true
          },
          "keepLast": {
            "description": "keepLast as applied.",
            "type": "integer",
            "format": "int64",
            "nullable": true
          },
          "mcRemovalCommands": {
            "description": "The same commands in `mc` spelling.",
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "note": {
            "description": "Why nothing would be removed, when that is not \"nothing to remove\".",
            "type": "string",
            "nullable": true
          },
          "removalCommands": {
            "description": "Removal commands in the archive scheme's own CLI. Reported, never run.",
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "setsKept": {
            "description": "Kept set IDs, newest first.",
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "setsThatWouldBeRemoved": {
            "description": "Sets that would be removed, newest first. Nothing was deleted.",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/RemovableSetView"
            }
          },
          "skippedManifests": {
            "description": "How many manifests could not be read.",
            "type": "integer",
            "format": "uint",
            "minimum": 0.0
          },
          "truncated": {
            "description": "Whether any list above was cut at 100 entries.",
            "type": "boolean"
          }
        }
      },
      "RetentionRequest": {
        "description": "Retention reporting settings. Logweir deletes nothing.",
        "type": "object",
        "properties": {
          "keepDays": {
            "description": "Keep sets newer than N days.",
            "default": null,
            "type": "integer",
            "format": "int64",
            "nullable": true
          },
          "keepLast": {
            "description": "Keep the newest N sets.",
            "default": null,
            "type": "integer",
            "format": "int64",
            "nullable": true
          }
        },
        "additionalProperties": false
      },
      "RetentionView": {
        "description": "Retention settings of a stored schedule.",
        "type": "object",
        "properties": {
          "keepDays": {
            "description": "Keep sets newer than N days.",
            "type": "integer",
            "format": "int64",
            "nullable": true
          },
          "keepLast": {
            "description": "Keep the newest N sets.",
            "type": "integer",
            "format": "int64",
            "nullable": true
          }
        }
      },
      "RetryPolicy": {
        "description": "Retries for a failed slot.",
        "type": "object",
        "required": [
          "maxRetries"
        ],
        "properties": {
          "delaySeconds": {
            "description": "Seconds between a failed attempt finishing and its retry becoming admissible, 60 to 21600. Absent means 300.",
            "default": null,
            "type": "integer",
            "format": "int64",
            "nullable": true
          },
          "maxRetries": {
            "description": "0 to 3. Each retry is a NEW `Backup` with a new execution id: a failed attempt's partial archive is never appended to.",
            "type": "integer",
            "format": "int32"
          }
        },
        "additionalProperties": false
      },
      "Role": {
        "description": "The four product roles. They are LOGWEIR roles: they are not Kubernetes roles, they are not granted by cluster RBAC, and holding one says nothing about what the console ServiceAccount may do.",
        "oneOf": [
          {
            "description": "Reads, never mutates.",
            "type": "string",
            "enum": [
              "viewer"
            ]
          },
          {
            "description": "Creates and runs operations. Never submits a governed approval and never changes trust or policy.",
            "type": "string",
            "enum": [
              "operator"
            ]
          },
          {
            "description": "Submits governed approvals in approver-bound namespaces. Cannot create an execution.",
            "type": "string",
            "enum": [
              "approver"
            ]
          },
          {
            "description": "Administers bound namespaces. Is STILL the requester when it operates, and is not an approver unless separately bound as one.",
            "type": "string",
            "enum": [
              "administrator"
            ]
          }
        ]
      },
      "RunnerPhaseView": {
        "description": "The runner's own phase, from its `progress-phase=` lines.",
        "type": "object",
        "properties": {
          "name": {
            "description": "The step or phase name, at most 32 bytes.",
            "type": "string",
            "nullable": true
          },
          "number": {
            "description": "`-1`..`9`. A backup reports `-1` with a step name.",
            "type": "integer",
            "format": "int32",
            "nullable": true
          }
        }
      },
      "SampleWindowView": {
        "description": "The window an integrity sample covered.",
        "type": "object",
        "properties": {
          "end": {
            "description": "Its end.",
            "type": "string",
            "format": "date-time",
            "nullable": true
          },
          "start": {
            "description": "Its start.",
            "type": "string",
            "format": "date-time",
            "nullable": true
          }
        }
      },
      "ScanView": {
        "description": "How much of the stored result one page actually looked at.",
        "type": "object",
        "required": [
          "chunksScanned",
          "complete"
        ],
        "properties": {
          "chunksScanned": {
            "description": "How many chunks this request read, at most 8.",
            "type": "integer",
            "format": "uint32",
            "minimum": 0.0
          },
          "complete": {
            "description": "Whether the scan reached the end of the result. A sparse `q` returns a short page with `complete: false` and a cursor rather than reading every chunk in one request.",
            "type": "boolean"
          }
        }
      },
      "Schedule": {
        "description": "A saved schedule.",
        "type": "object",
        "required": [
          "archive",
          "concurrencyPolicy",
          "name",
          "namespace",
          "resourceVersion",
          "schedule",
          "sourceRef",
          "status",
          "suspended",
          "topics",
          "uid"
        ],
        "properties": {
          "activeDeadlineSeconds": {
            "description": "The run's `activeDeadlineSeconds`. ABSENT means 3600.",
            "type": "integer",
            "format": "int64",
            "nullable": true
          },
          "allUserTopics": {
            "description": "Dynamic selection, when the schedule uses it.",
            "$ref": "#/components/schemas/AllUserTopics",
            "nullable": true
          },
          "archive": {
            "description": "Where backups are written. With `destinationRef` this is the CRD's sentinel URL and the console should render the destination instead.",
            "$ref": "#/components/schemas/ArchiveView"
          },
          "catchUpPolicy": {
            "description": "ABSENT means `None`.",
            "$ref": "#/components/schemas/CatchUpPolicy",
            "nullable": true
          },
          "concurrencyPolicy": {
            "description": "`Forbid` or `Allow`.",
            "$ref": "#/components/schemas/ConcurrencyPolicy"
          },
          "createdAt": {
            "description": "When the object was created.",
            "type": "string",
            "format": "date-time",
            "nullable": true
          },
          "destinationRef": {
            "description": "The saved destination, when the schedule names one.",
            "$ref": "#/components/schemas/NameRef",
            "nullable": true
          },
          "generation": {
            "description": "The `metadata.generation`; send it back as `expectedGeneration` on `PUT .../schedules/{name}` and on a manual run taken from this schedule. It increments on EVERY spec change, suspension included.\n\nOPTIONAL IN THE SCHEMA, ALWAYS EMITTED BY THIS BUILD. The API server sets `metadata.generation` on every object, so this projection always carries it; it is declared optional ONLY because `ui/contract.js`'s decoder drift test compares the schema's `required` set with a frozen list on the console side, and moving a field into that set is a contract change that must land in the same commit as `ui/contract.js` and the console fixtures (D1 W7's ownership). A console that somehow sees it absent must ask for a reload rather than guess a value for `expectedGeneration`.",
            "type": "integer",
            "format": "int64",
            "nullable": true
          },
          "name": {
            "description": "The object name.",
            "type": "string"
          },
          "namespace": {
            "description": "The namespace.",
            "type": "string"
          },
          "preset": {
            "description": "The preset this expression IS, or absent for \"Advanced cron\".",
            "$ref": "#/components/schemas/CadencePreset",
            "nullable": true
          },
          "resourceVersion": {
            "description": "The resourceVersion; send it back as `expectedResourceVersion` on `:set-suspension`.",
            "type": "string"
          },
          "retention": {
            "description": "Retention reporting settings.",
            "$ref": "#/components/schemas/RetentionView",
            "nullable": true
          },
          "retry": {
            "description": "ABSENT means no retries.",
            "$ref": "#/components/schemas/RetryPolicy",
            "nullable": true
          },
          "schedule": {
            "description": "The cron expression.",
            "type": "string"
          },
          "sourceRef": {
            "description": "The source connection. Immutable: a different cluster is a different schedule.",
            "$ref": "#/components/schemas/NameRef"
          },
          "startingDeadlineSeconds": {
            "description": "How long after its instant a slot may still start. ABSENT means 3600.",
            "type": "integer",
            "format": "int64",
            "nullable": true
          },
          "status": {
            "description": "What the controller recorded.",
            "$ref": "#/components/schemas/ScheduleStatusView"
          },
          "suspended": {
            "description": "Whether the schedule is suspended.",
            "type": "boolean"
          },
          "timeZone": {
            "description": "The IANA zone the expression is evaluated in. ABSENT means UTC, which is what every pre-PLAT-04.2 schedule does.",
            "type": "string",
            "nullable": true
          },
          "topics": {
            "description": "Named topics. Empty with `allUserTopics`.",
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "uid": {
            "description": "The Kubernetes UID.",
            "type": "string"
          }
        }
      },
      "ScheduleContextView": {
        "description": "The schedule a manual run was taken from, as it stood when the run was created.",
        "type": "object",
        "required": [
          "activeRuns",
          "generation",
          "name",
          "suspended",
          "uid"
        ],
        "properties": {
          "activeRuns": {
            "description": "The schedule-created runs that were not terminal. A manual run is neither counted by nor blocked by `concurrencyPolicy`; this is a non-blocking notice.",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/ActiveRunView"
            }
          },
          "generation": {
            "description": "The generation whose policy this run copied.",
            "type": "integer",
            "format": "int64"
          },
          "name": {
            "description": "The schedule's name.",
            "type": "string"
          },
          "runPolicySha256": {
            "description": "The digest of that policy.",
            "type": "string",
            "nullable": true
          },
          "suspended": {
            "description": "Whether the schedule is suspended. A manual run is ALLOWED while it is, and does not resume it; the console says so rather than hiding it.",
            "type": "boolean"
          },
          "uid": {
            "description": "Its UID.",
            "type": "string"
          }
        }
      },
      "ScheduleHealthView": {
        "description": "A schedule as this policy sees it — rendered BESIDE protection health and never folded into it.",
        "type": "object",
        "required": [
          "name"
        ],
        "properties": {
          "lastMissedSlot": {
            "description": "The last slot it missed.",
            "type": "string",
            "nullable": true
          },
          "name": {
            "description": "The schedule's name.",
            "type": "string"
          },
          "nextFireTime": {
            "description": "When it fires next.",
            "type": "string",
            "format": "date-time",
            "nullable": true
          },
          "ready": {
            "description": "Its own `Ready` status.",
            "type": "string",
            "nullable": true
          },
          "suspended": {
            "description": "Whether it is suspended — the commonest cause of staleness.",
            "type": "boolean",
            "nullable": true
          }
        }
      },
      "ScheduleList": {
        "type": "object",
        "required": [
          "items",
          "page",
          "requestId"
        ],
        "properties": {
          "items": {
            "description": "The items on this page.",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/Schedule"
            }
          },
          "page": {
            "description": "Paging.",
            "$ref": "#/components/schemas/Page"
          },
          "requestId": {
            "description": "The request ID.",
            "type": "string"
          }
        }
      },
      "SchedulePolicyView": {
        "description": "The revision a schedule's controller last evaluated (D1 §4.8).",
        "type": "object",
        "required": [
          "effectiveSince",
          "evaluatedAt",
          "generation",
          "runPolicySha256",
          "timeZone",
          "tzdb"
        ],
        "properties": {
          "effectiveSince": {
            "description": "When this revision was first observed.",
            "type": "string",
            "format": "date-time"
          },
          "evaluatedAt": {
            "description": "When this status last MOVED — **not** a liveness probe.\n\nThe controller re-examines every schedule every 30 s and writes nothing when the computed status equals the stored one, so this instant standing still means \"nothing has changed\", not \"nobody is watching\". A console must not compare it with the requeue interval. The staleness signal is `nextRuns[0].at` in the past (D1 §4.9 as amended).",
            "type": "string",
            "format": "date-time"
          },
          "generation": {
            "description": "The `metadata.generation` this block was computed from.",
            "type": "integer",
            "format": "int64"
          },
          "runPolicySha256": {
            "description": "`sha256:<lowercase hex>` over the run policy of that generation.",
            "type": "string"
          },
          "timeZone": {
            "description": "The EFFECTIVE zone, `UTC` when `spec.timeZone` is absent.",
            "type": "string"
          },
          "tzdb": {
            "description": "Which time-zone database resolved it.",
            "type": "string"
          }
        }
      },
      "ScheduleRefView": {
        "description": "The schedule revision a run recorded.",
        "type": "object",
        "required": [
          "name"
        ],
        "properties": {
          "generation": {
            "description": "The revision this run copied.",
            "type": "integer",
            "format": "int64",
            "nullable": true
          },
          "name": {
            "description": "The schedule's name, in this namespace.",
            "type": "string"
          },
          "runPolicySha256": {
            "description": "The digest of the policy it copied.",
            "type": "string",
            "nullable": true
          },
          "uid": {
            "description": "Its UID. A schedule deleted and recreated under the same name is a different schedule; a run of the old one is not a run of the new one.",
            "type": "string",
            "nullable": true
          }
        }
      },
      "ScheduleResponse": {
        "type": "object",
        "required": [
          "item",
          "requestId"
        ],
        "properties": {
          "item": {
            "description": "The item.",
            "$ref": "#/components/schemas/Schedule"
          },
          "replayed": {
            "description": "On a durable create: whether this response replays an earlier identical request (HTTP 200) rather than creating (HTTP 201).",
            "type": "boolean",
            "nullable": true
          },
          "requestId": {
            "description": "The request ID.",
            "type": "string"
          }
        }
      },
      "ScheduleStatusView": {
        "description": "What the schedule controller last recorded.",
        "type": "object",
        "properties": {
          "activeBackup": {
            "description": "The running Backup's name.",
            "type": "string",
            "nullable": true
          },
          "activeRuns": {
            "description": "The schedule-created runs that are not terminal. ABSENT means \"not yet computed\", never \"none are running\".",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/ActiveRunView"
            },
            "nullable": true
          },
          "lastFireTime": {
            "description": "When the schedule last created a Backup.",
            "type": "string",
            "format": "date-time",
            "nullable": true
          },
          "lastMissedSlot": {
            "description": "The most recent skipped slot.",
            "type": "string",
            "nullable": true
          },
          "nextFireTime": {
            "description": "When it will next create one.",
            "type": "string",
            "format": "date-time",
            "nullable": true
          },
          "nextRuns": {
            "description": "Up to five upcoming firings for the current generation; empty when the schedule is suspended or invalid. **This is the staleness signal**: a live controller rewrites these once `nextRuns[0].at` has passed, so a first entry in the past means nobody is evaluating this schedule. ABSENT means \"not yet computed\", never \"it never fires\".",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/NextRunView"
            },
            "nullable": true
          },
          "observedGeneration": {
            "description": "The `metadata.generation` the controller has evaluated. A `generation` ahead of this one is an edit the controller has not seen yet; ABSENT means it has not been computed, never that it is zero.",
            "type": "integer",
            "format": "int64",
            "nullable": true
          },
          "pendingBackup": {
            "description": "A reserved Backup name whose creation is not yet confirmed.",
            "type": "string",
            "nullable": true
          },
          "policy": {
            "description": "The revision the controller last evaluated, with the effective zone, the tz database that resolved it and the run-policy digest.",
            "$ref": "#/components/schemas/SchedulePolicyView",
            "nullable": true
          },
          "ready": {
            "description": "The `Ready` condition.",
            "$ref": "#/components/schemas/ConditionView",
            "nullable": true
          },
          "retentionReport": {
            "description": "The retention report.",
            "$ref": "#/components/schemas/RetentionReportView",
            "nullable": true
          }
        }
      },
      "SecretSourceRequest": {
        "description": "Exactly one of an existing Secret reference or a new write-only value.",
        "type": "object",
        "properties": {
          "existing": {
            "description": "Name an existing Secret.",
            "default": null,
            "$ref": "#/components/schemas/ExistingSecretRequest",
            "nullable": true
          },
          "new": {
            "description": "Or type the credential once, here.",
            "default": null,
            "$ref": "#/components/schemas/NewCredentialRequest",
            "nullable": true
          }
        },
        "additionalProperties": false
      },
      "SessionResponse": {
        "description": "`GET /api/v1/session`.",
        "type": "object",
        "required": [
          "actor",
          "authenticationMode",
          "bindingRevision",
          "capabilities",
          "namespaces",
          "requestId"
        ],
        "properties": {
          "actor": {
            "description": "The actor.",
            "$ref": "#/components/schemas/ActorView"
          },
          "authenticationMode": {
            "description": "How the actor was authenticated.",
            "$ref": "#/components/schemas/AuthenticationMode"
          },
          "bindingRevision": {
            "description": "The revision of the administrator's role-binding table that produced these grants. It is recorded in every audit line, so a decision can be tied to the configuration that made it. Empty in localAdmin mode.",
            "type": "string"
          },
          "capabilities": {
            "description": "The union of the grants' capabilities.",
            "$ref": "#/components/schemas/Capabilities"
          },
          "csrfToken": {
            "description": "The CSRF token for unsafe requests; `null` in localAdmin mode, which relies on the loopback listener and the exact Origin check.",
            "type": "string",
            "nullable": true
          },
          "expiresAt": {
            "description": "When the session expires; `null` in localAdmin mode, which has no session.",
            "type": "string",
            "format": "date-time",
            "nullable": true
          },
          "namespaces": {
            "description": "The explicitly granted namespaces.",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/NamespaceGrant"
            }
          },
          "requestId": {
            "description": "The request ID.",
            "type": "string"
          }
        }
      },
      "SetSuspensionRequest": {
        "description": "`POST /api/v1/namespaces/{ns}/schedules/{name}:set-suspension`.",
        "type": "object",
        "required": [
          "expectedResourceVersion",
          "suspended"
        ],
        "properties": {
          "expectedResourceVersion": {
            "description": "The resourceVersion the client last read. A stale value is `precondition_failed`.",
            "type": "string"
          },
          "suspended": {
            "description": "The desired value of the schedule's one mutable field.",
            "type": "boolean"
          }
        },
        "additionalProperties": false
      },
      "SignerPageResponse": {
        "description": "The signer panel.",
        "type": "object",
        "required": [
          "fingerprintCommand",
          "items",
          "requestId"
        ],
        "properties": {
          "fingerprintCommand": {
            "description": "The out-of-band comparison an administrator runs before adding a key. A command, not an action this service offers.",
            "type": "string"
          },
          "items": {
            "description": "The signers.",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/SignerView"
            }
          },
          "requestId": {
            "description": "The request ID.",
            "type": "string"
          },
          "untrustedPoints": {
            "description": "How many points carry a signature the bound policy does not accept.",
            "type": "integer",
            "format": "int64",
            "nullable": true
          }
        }
      },
      "SignerView": {
        "description": "Who signed the points in this catalog — the untrusted-signer panel's data.",
        "type": "object",
        "required": [
          "keyId"
        ],
        "properties": {
          "keyId": {
            "description": "The key id: the SHA-256 of the DER SPKI, lowercase hex. Compare it out of band; there is no one-click trust.",
            "type": "string"
          },
          "points": {
            "description": "How many points it signed.",
            "type": "integer",
            "format": "int64",
            "nullable": true
          },
          "principalHint": {
            "description": "A hint at the principal, taken from the record. **Never authority.**",
            "type": "string",
            "nullable": true
          },
          "trusted": {
            "description": "Whether the bound `TrustPolicy` accepts it. Absent means the catalog could not tell, which is not `false` and is certainly not `true`.",
            "type": "boolean",
            "nullable": true
          }
        }
      },
      "SkippedEntryView": {
        "description": "One point or key the evaluation could not classify. **Unknown is retained**: nothing here is ever a deletion candidate.",
        "type": "object",
        "required": [
          "reason"
        ],
        "properties": {
          "key": {
            "description": "The object key, when the point was not.",
            "type": "string",
            "nullable": true
          },
          "pointId": {
            "description": "The point, when one was identified.",
            "type": "string",
            "nullable": true
          },
          "reason": {
            "description": "`Unreadable`, `UnsupportedFormat` or `Conflict`.",
            "type": "string"
          }
        }
      },
      "SkippedSlotView": {
        "description": "A slot that did not run, and why.",
        "type": "object",
        "properties": {
          "reason": {
            "description": "The reason, from the closed list.",
            "type": "string",
            "nullable": true
          },
          "slot": {
            "description": "The slot.",
            "type": "string",
            "nullable": true
          }
        }
      },
      "SourceConnectionPreflightRequest": {
        "description": "The source-connection half of a preflight request.",
        "type": "object",
        "required": [
          "connectionRef"
        ],
        "properties": {
          "connectionRef": {
            "description": "The `KafkaCluster` to dial, in this namespace. REQUIRED: a connectivity check with no connection is not a smaller check, it is no check, and the absence is refused as `422 connectionRef required` rather than defaulted to anything.",
            "type": "string"
          }
        },
        "additionalProperties": false
      },
      "StaleReasonKind": {
        "description": "Why a stored readiness verdict no longer describes the caller's inputs.\n\nCLOSED OVER CORE'S VOCABULARY, PLUS ONE. Six of these are the spellings `logweir_core::check_contract::StaleReason` renders, and `crates/logweir-api/tests/preflights.rs` pins the two lists against each other so neither side can grow a reason the other cannot carry. [`StaleReasonKind::Unverifiable`] is the seventh and is this service's own: core has no spelling for \"I could not compare this\", and without one the only honest answers left were to invent a reason or to stay silent — and staying silent is `applicable: true` for a verdict nobody checked.\n\nTHESE ARE COMPARISONS, NOT MESSAGES. The API recomputes staleness on every read from `status.binding` against the objects as they are now — which is the division of labour `weirkeeper`'s preflight reconciler documents and `check_contract::inputs_digest` restates. An earlier cut of this module recovered the reasons from the controller's prose instead; that prose is redacted and capped at 512 characters, so a long referent list lost its closing bracket and the parse returned NOTHING — reporting a downgraded verdict as applicable. It failed open, which is the one direction a staleness check may never fail.\n\n`cancelRequested` is DELIBERATELY ABSENT, and was in the first cut of this enum. The controller never emits it: a cancelled check is `state: cancelled` and carries no result at all, so its verdict is not stale — it is ABSENT, which is a different thing and one `state` and `terminal` already say. Reporting it here invited a console to render \"your readiness result is out of date\" for a check that never produced one.",
        "oneOf": [
          {
            "description": "The verdict is past `expiresAt`, or recorded no expiry at all.",
            "type": "string",
            "enum": [
              "expired"
            ]
          },
          {
            "description": "The plan the caller is looking at is not the plan the check was bound to.",
            "type": "string",
            "enum": [
              "planHashChanged"
            ]
          },
          {
            "description": "A named object's UID or generation moved, or it appeared or vanished, since the verdict was computed — a recreated destination, an edited access block, a re-created recovery point, a `TrustRoster` edit, or an `Approval` whose resourceVersion moved when verification landed. [`StaleReasonView::kind`] and [`StaleReasonView::name`] say which.",
            "type": "string",
            "enum": [
              "referentChanged"
            ]
          },
          {
            "description": "A destination's CA bundle `ConfigMap` now digests differently, so the trust material the check exercised is not the trust material a run would use.\n\n**RESERVED: nothing emits this yet.** It is part of core's vocabulary, so it is published here, but the recorded `status.binding` does not carry the CA bundle list and neither this service nor the controller can therefore compare it. CA-bundle drift surfaces, if at all, through the destination's own `referentChanged` (its generation moves when its `caBundle` reference is edited). It becomes reachable when `status.binding` records `caBundles[]`; until then a console that branches on it is branching on a value it will never receive, and `docs/api.md` says so.",
            "type": "string",
            "enum": [
              "caBundleChanged"
            ]
          },
          {
            "description": "The installation policy `ConfigMap` digests differently, which can change the concurrency ceilings, the engine CA rule, the `ControllerIdentity` allowlist and the visibility attestations the verdict was computed under.",
            "type": "string",
            "enum": [
              "policyChanged"
            ]
          },
          {
            "description": "The recomputed inputs digest differs and none of the named reasons explains it. THE CATCH-ALL EXISTS SO \"stale\" IS NEVER REPORTED WITHOUT A REASON: a field the five named comparisons do not cover (a backup readiness request's topic set, for instance) still surfaces.\n\n**RESERVED on the same grounds as [`StaleReasonKind::CaBundleChanged`]:** the recorded digest was taken over a wider document than this service can rebuild, so comparing the two would report a difference that is an artefact of the narrower recomputation rather than a change in the world. A gap this service cannot close is [`StaleReasonKind::Unverifiable`], not a false positive.",
            "type": "string",
            "enum": [
              "inputsDigestChanged"
            ]
          },
          {
            "description": "**This service could not compare something, so it refuses to call the verdict applicable.** [`StaleReasonView::basis`] says what.\n\nTHE ONE VARIANT THAT IS NOT CORE'S. Every other reason answers \"this changed\"; this one answers \"I do not know whether it changed\", which is a different claim and the only honest one when a referent cannot be read, when the recorded binding is missing, or when an input lives somewhere this service has no verb for. Silence in those cases is `applicable: true` for a verdict nobody checked — and a green readiness badge that was never re-evaluated is exactly the defect PLAT-03.2 is about.",
            "type": "string",
            "enum": [
              "unverifiable"
            ]
          }
        ]
      },
      "StaleReasonView": {
        "description": "One reason, with its subject when it has one.\n\nSTRUCTURED, NOT A PARSED STRING. The controller renders `referentChanged` as `referentChanged:<Kind>/<name>`; splitting that once, here, is the difference between a console that can say \"the destination `primary` was replaced\" and one that shows a colon-separated token to an operator.",
        "type": "object",
        "required": [
          "reason"
        ],
        "properties": {
          "basis": {
            "description": "Why this service could not compare something. `unverifiable` only, and always present there: a refusal to answer that does not say what it could not check is not an answer.",
            "type": "string",
            "nullable": true
          },
          "kind": {
            "description": "The kind of the object that moved. `referentChanged` and `unverifiable` only.",
            "type": "string",
            "nullable": true
          },
          "name": {
            "description": "That object's name — or, for the `TrustRoster` and `Approval` a binding names, its UID, because those two are identified in the binding by UID and a name would add nothing. `referentChanged` and `unverifiable` only.",
            "type": "string",
            "nullable": true
          },
          "reason": {
            "description": "Which reason this is.",
            "$ref": "#/components/schemas/StaleReasonKind"
          }
        }
      },
      "StorageProviderDto": {
        "description": "The object-store provider a destination names.",
        "oneOf": [
          {
            "description": "S3 and S3-compatible endpoints.",
            "type": "string",
            "enum": [
              "s3"
            ]
          }
        ]
      },
      "StorageRequest": {
        "description": "Where the archive root is. Immutable once the destination exists.",
        "type": "object",
        "required": [
          "addressing",
          "bucket",
          "provider"
        ],
        "properties": {
          "addressing": {
            "description": "How a request names the bucket.",
            "$ref": "#/components/schemas/AddressingDto"
          },
          "bucket": {
            "description": "The bucket.",
            "type": "string"
          },
          "endpoint": {
            "description": "An http(s) origin — scheme, host and optional port. Absent means AWS S3.",
            "default": null,
            "type": "string",
            "nullable": true
          },
          "prefix": {
            "description": "The key prefix inside the bucket, relative; absent is the bucket root. Never `logweir` or anything under it.",
            "default": null,
            "type": "string",
            "nullable": true
          },
          "provider": {
            "description": "The object-store provider.",
            "$ref": "#/components/schemas/StorageProviderDto"
          },
          "region": {
            "description": "The region, when the provider needs one.",
            "default": null,
            "type": "string",
            "nullable": true
          }
        },
        "additionalProperties": false
      },
      "StorageView": {
        "description": "A destination's location, as stored.",
        "type": "object",
        "required": [
          "addressing",
          "bucket",
          "prefix",
          "provider"
        ],
        "properties": {
          "addressing": {
            "description": "How a request names the bucket.",
            "$ref": "#/components/schemas/AddressingDto"
          },
          "bucket": {
            "description": "The bucket.",
            "type": "string"
          },
          "endpoint": {
            "description": "The endpoint origin; absent means AWS S3.",
            "type": "string",
            "nullable": true
          },
          "prefix": {
            "description": "The prefix, `\"\"` for the bucket root.",
            "type": "string"
          },
          "provider": {
            "description": "The provider.",
            "$ref": "#/components/schemas/StorageProviderDto"
          },
          "region": {
            "description": "The region.",
            "type": "string",
            "nullable": true
          }
        }
      },
      "SubjectRefView": {
        "description": "What an approval is about.",
        "type": "object",
        "required": [
          "kind",
          "name"
        ],
        "properties": {
          "kind": {
            "description": "`Restore` or `Backup`.",
            "type": "string"
          },
          "name": {
            "description": "The subject name.",
            "type": "string"
          }
        }
      },
      "SyncCursorView": {
        "description": "Where the last walk got to.",
        "type": "object",
        "properties": {
          "complete": {
            "description": "Whether the walk finished. `false` is a budgeted walk that will continue, not a failure.",
            "type": "boolean",
            "nullable": true
          },
          "indexShard": {
            "description": "The day shard the next `Index` sync resumes from.",
            "type": "string",
            "nullable": true
          }
        }
      },
      "TeardownFailureView": {
        "description": "One topic teardown could not remove.",
        "type": "object",
        "required": [
          "error",
          "topic"
        ],
        "properties": {
          "error": {
            "description": "The error, bounded.",
            "type": "string"
          },
          "topic": {
            "description": "The topic.",
            "type": "string"
          }
        }
      },
      "TeardownView": {
        "description": "What phase 9 removed, from the SIGNED teardown attestation.",
        "type": "object",
        "required": [
          "deletedTruncated"
        ],
        "properties": {
          "attestationKey": {
            "description": "Where the attestation is.",
            "type": "string",
            "nullable": true
          },
          "deleted": {
            "description": "The topics phase 9 removed, by name.\n\nA LIST AND NOT A COUNT. D3 §2.2 declares `deleted: [string]`, and the incident question is \"which topics went\", not \"how many\": a count cannot be reconciled against the names the run created. The first round published the length.",
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "deletedTruncated": {
            "description": "Whether `deleted` was cut short by this projection's row bound.",
            "type": "boolean"
          },
          "failed": {
            "description": "The ones that could not be removed. A non-empty list is what makes the next rehearsal slot SKIP rather than adopt topics it did not create.",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/TeardownFailureView"
            }
          }
        }
      },
      "TestDestinationRequest": {
        "description": "`POST .../destinations/{name}:test`.",
        "type": "object",
        "properties": {
          "roles": {
            "description": "The roles to exercise, 1 to 4. Absent means every configured role.",
            "default": null,
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/DestinationRoleDto"
            },
            "nullable": true
          }
        },
        "additionalProperties": false
      },
      "TopicDiscovery": {
        "description": "A bounded topic inventory.",
        "type": "object",
        "required": [
          "chunkCount",
          "conditions",
          "connection",
          "id",
          "namespace",
          "resourceVersion",
          "stale",
          "staleReasons",
          "state",
          "terminal",
          "truncated",
          "uid"
        ],
        "properties": {
          "chunkCount": {
            "description": "How many stored chunks the result has.",
            "type": "integer",
            "format": "uint",
            "minimum": 0.0
          },
          "clusterId": {
            "description": "The cluster id the check read.",
            "type": "string",
            "nullable": true
          },
          "conditions": {
            "description": "The status conditions, at most 16.",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/ConditionView"
            }
          },
          "connection": {
            "description": "The connection it was bound to.",
            "$ref": "#/components/schemas/DiscoveryConnectionView"
          },
          "counts": {
            "description": "The counts.",
            "$ref": "#/components/schemas/DiscoveryCountsView",
            "nullable": true
          },
          "createdAt": {
            "description": "When the object was created.",
            "type": "string",
            "format": "date-time",
            "nullable": true
          },
          "error": {
            "description": "The failure, when there is one.",
            "$ref": "#/components/schemas/CheckErrorView",
            "nullable": true
          },
          "expected": {
            "description": "What became of the expected topics.",
            "$ref": "#/components/schemas/ExpectedTopicsView",
            "nullable": true
          },
          "freshUntil": {
            "description": "Until when the result counts as fresh.",
            "type": "string",
            "format": "date-time",
            "nullable": true
          },
          "id": {
            "description": "The object name, which is the id every other route uses.",
            "type": "string"
          },
          "namespace": {
            "description": "The namespace.",
            "type": "string"
          },
          "observedAt": {
            "description": "When the runner container finished, which is when the facts were true.",
            "type": "string",
            "format": "date-time",
            "nullable": true
          },
          "reason": {
            "description": "The CamelCase reason.",
            "type": "string",
            "nullable": true
          },
          "resourceVersion": {
            "description": "The resourceVersion this projection was read at.",
            "type": "string"
          },
          "stale": {
            "description": "Whether the result is past its freshness or its binding changed.",
            "type": "boolean"
          },
          "staleReasons": {
            "description": "Why, when it is: `expired`, `connectionReplaced`, `connectionChanged` or `principalChanged`.\n\nA DIFFERENT VOCABULARY FROM A DIFFERENT PRODUCER, and deliberately still strings. D2 §5.7's four reasons are computed by THIS service, by comparing the recorded binding with the connection as it is now; no controller emits them, so there is no second list for a closed enum to be pinned against the way [`StaleReasonKind`] is pinned against `check_contract::StaleReason`. Closing it would be a contract change with nothing on the other side asking for it.",
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "state": {
            "description": "The lifecycle state.",
            "$ref": "#/components/schemas/CheckLifecycle"
          },
          "terminal": {
            "description": "Whether the state is final.",
            "type": "boolean"
          },
          "topicsSha256": {
            "description": "`sha256:<hex>` over the canonical TSV of the stored entries. The topics page binds its cursor to it, so a page cannot straddle two results.",
            "type": "string",
            "nullable": true
          },
          "truncated": {
            "description": "Whether the inventory was cut.",
            "type": "boolean"
          },
          "truncationReason": {
            "description": "Why it was cut.",
            "type": "string",
            "nullable": true
          },
          "uid": {
            "description": "The Kubernetes UID.",
            "type": "string"
          },
          "visibility": {
            "description": "Completeness, never better than `unknown` without an attestation.",
            "$ref": "#/components/schemas/VisibilityView",
            "nullable": true
          }
        }
      },
      "TopicDiscoveryList": {
        "description": "`GET .../connections/{name}/topic-discoveries` — one page.",
        "type": "object",
        "required": [
          "items",
          "page",
          "requestId"
        ],
        "properties": {
          "items": {
            "description": "The discoveries on this page.",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/TopicDiscovery"
            }
          },
          "page": {
            "description": "Paging.",
            "$ref": "#/components/schemas/Page"
          },
          "requestId": {
            "description": "The request ID.",
            "type": "string"
          }
        }
      },
      "TopicDiscoveryResponse": {
        "description": "One `TopicDiscovery`.",
        "type": "object",
        "required": [
          "item",
          "requestId"
        ],
        "properties": {
          "item": {
            "description": "The item.",
            "$ref": "#/components/schemas/TopicDiscovery"
          },
          "replayed": {
            "description": "On a create: whether this replays an earlier identical request.",
            "type": "boolean",
            "nullable": true
          },
          "requestId": {
            "description": "The request ID.",
            "type": "string"
          },
          "reused": {
            "description": "On a create: whether a fresh identical result was returned instead of starting another check.",
            "type": "boolean",
            "nullable": true
          }
        }
      },
      "TopicEntryView": {
        "description": "One row of a topic page.",
        "type": "object",
        "required": [
          "expected",
          "internal",
          "name",
          "partitions"
        ],
        "properties": {
          "errorCode": {
            "description": "The CamelCase code, when its metadata could not be read.",
            "type": "string",
            "nullable": true
          },
          "expected": {
            "description": "Whether it was one of the expected names.",
            "type": "boolean"
          },
          "internal": {
            "description": "Whether it is a Kafka internal topic (`__`-prefixed, and only that).",
            "type": "boolean"
          },
          "name": {
            "description": "The topic name.",
            "type": "string"
          },
          "partitions": {
            "description": "Its partition count.",
            "type": "integer",
            "format": "uint32",
            "minimum": 0.0
          }
        }
      },
      "TopicExclusions": {
        "description": "Topics to leave out of a dynamic selection. Literal names and literal prefixes; never a pattern language.",
        "type": "object",
        "properties": {
          "prefixes": {
            "description": "Literal prefixes, at most 32. `orders-` excludes `orders-eu`; `orders*` is not writable at all.",
            "default": null,
            "type": "array",
            "items": {
              "type": "string"
            },
            "nullable": true
          },
          "topics": {
            "description": "Exact names, at most 1000.",
            "default": null,
            "type": "array",
            "items": {
              "type": "string"
            },
            "nullable": true
          }
        },
        "additionalProperties": false
      },
      "TopicMappingRow": {
        "description": "One row of the topic mapping the caller previewed: a source topic in the recovery point, and the target topic the restore would create for it.\n\nTHIS IS A DECLARATION, NOT A STORED FIELD. `Restore.spec` has no topic list — the subset lives in the opaque plan bytes (`logweir_core::spec::SourceSpec::topics`) — so nothing here is persisted. What it buys is a rail this service CAN check without parsing the plan: `target.topicNaming.prefix` IS stored, the mapping rule is prefix concatenation and nothing else (`logweir_core::spec::target_topic_prefix`), so every row must be exactly `prefix + source`. A console that previewed one mapping and submitted another is refused here by name instead of discovering it in phase 0 after an approver has signed.",
        "type": "object",
        "required": [
          "source",
          "target"
        ],
        "properties": {
          "source": {
            "description": "The source topic, as the recovery point froze it.",
            "type": "string"
          },
          "target": {
            "description": "The target topic name this restore would create for it.",
            "type": "string"
          }
        },
        "additionalProperties": false
      },
      "TopicNamingRequest": {
        "description": "How restored topics are named.",
        "type": "object",
        "required": [
          "prefix"
        ],
        "properties": {
          "prefix": {
            "description": "Prepended to each source topic name.",
            "type": "string"
          }
        },
        "additionalProperties": false
      },
      "TopicPageResponse": {
        "description": "`GET .../topic-discoveries/{id}/topics`.",
        "type": "object",
        "required": [
          "items",
          "page",
          "requestId",
          "scan"
        ],
        "properties": {
          "items": {
            "description": "The rows on this page, in stored (bytewise name) order.",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/TopicEntryView"
            }
          },
          "page": {
            "description": "Paging. `snapshot` is `<uid>@<topicsSha256>`.",
            "$ref": "#/components/schemas/Page"
          },
          "requestId": {
            "description": "The request ID.",
            "type": "string"
          },
          "scan": {
            "description": "How much of the result was read.",
            "$ref": "#/components/schemas/ScanView"
          }
        }
      },
      "TopicSelectionRequest": {
        "description": "How a run picks its topics: a named allowlist, or dynamic resolution.\n\nTHE THIRD SHAPE IS NOT REFUSED HERE, AND THAT IS DELIBERATE. Sending a non-empty `topics` together with `allUserTopics` is refused by the CRD's own CEL (D1 §5.2 R2 / the `Backup` rule), and this API lets the API server say so rather than keeping a second copy of the rule that can drift from it. An EMPTY selection — neither a name nor a block — is refused here, before any write, because no rule catches it and a run with nothing to do is not a run.",
        "type": "object",
        "properties": {
          "allUserTopics": {
            "description": "Dynamic selection.",
            "default": null,
            "$ref": "#/components/schemas/AllUserTopics",
            "nullable": true
          },
          "topics": {
            "description": "Named topics; patterns are refused. Empty for dynamic selection.",
            "default": [],
            "type": "array",
            "items": {
              "type": "string"
            }
          }
        },
        "additionalProperties": false
      },
      "TransportRequest": {
        "description": "Transport security and its trust material.",
        "type": "object",
        "required": [
          "security"
        ],
        "properties": {
          "caBundle": {
            "description": "A private CA, for `tls` only.",
            "default": null,
            "$ref": "#/components/schemas/CaBundleRequest",
            "nullable": true
          },
          "security": {
            "description": "`tls` or `insecureHttp`. It must agree with the endpoint scheme, and `addressing` never changes it.",
            "$ref": "#/components/schemas/TransportSecurityDto"
          }
        },
        "additionalProperties": false
      },
      "TransportSecurityDto": {
        "description": "Transport security. Immutable once the destination exists.",
        "oneOf": [
          {
            "description": "TLS, with an `https://` endpoint or none.",
            "type": "string",
            "enum": [
              "tls"
            ]
          },
          {
            "description": "Plaintext HTTP, and only with an explicit `http://` endpoint.",
            "type": "string",
            "enum": [
              "insecureHttp"
            ]
          }
        ]
      },
      "TransportView": {
        "description": "Transport security, as stored.",
        "type": "object",
        "required": [
          "security"
        ],
        "properties": {
          "caBundle": {
            "description": "The CA reference.",
            "$ref": "#/components/schemas/CaBundleView",
            "nullable": true
          },
          "security": {
            "description": "`tls` or `insecureHttp`.",
            "$ref": "#/components/schemas/TransportSecurityDto"
          }
        }
      },
      "TriggerKind": {
        "description": "Which kind of run a `Backup` is (D1 §3.1).",
        "oneOf": [
          {
            "description": "A slot that fired at its own instant.",
            "type": "string",
            "enum": [
              "Scheduled"
            ]
          },
          {
            "description": "The same slot, started late because the controller was not running.",
            "type": "string",
            "enum": [
              "CatchUp"
            ]
          },
          {
            "description": "Attempt k of a slot, 1 to 3, with a NEW execution id.",
            "type": "string",
            "enum": [
              "Retry"
            ]
          },
          {
            "description": "Created by a person, this API or the console. No slot; the execution id is the object's own UID.",
            "type": "string",
            "enum": [
              "Manual"
            ]
          }
        ]
      },
      "TriggerView": {
        "description": "What caused a run.",
        "type": "object",
        "required": [
          "attempt",
          "kind"
        ],
        "properties": {
          "attempt": {
            "description": "0 for `Scheduled`, `CatchUp` and `Manual`; 1 to 3 for `Retry`.",
            "type": "integer",
            "format": "int32"
          },
          "kind": {
            "description": "The kind.",
            "$ref": "#/components/schemas/TriggerKind"
          },
          "retryOf": {
            "description": "The attempt this one retries.",
            "$ref": "#/components/schemas/NameRef",
            "nullable": true
          },
          "timeZone": {
            "description": "The zone the slot was computed in. INFORMATIONAL: the slot itself is UTC, and this is what keeps a history row's local time readable after somebody edits the schedule's `timeZone`.",
            "type": "string",
            "nullable": true
          }
        }
      },
      "TrustEvaluationView": {
        "description": "How fresh the controller's verdict is, and what it was computed from.\n\nNAMED FOR ITS DOMAIN; see `retention.rs`'s `RetentionEvaluationView` for why a short type name is a global schema name.",
        "type": "object",
        "required": [
          "freshWithinSeconds",
          "reason",
          "serverTime",
          "state"
        ],
        "properties": {
          "ageSeconds": {
            "description": "How old that is, by this server's clock.",
            "type": "integer",
            "format": "int64",
            "nullable": true
          },
          "evaluatedAt": {
            "description": "When the controller evaluated.",
            "type": "string",
            "format": "date-time",
            "nullable": true
          },
          "freshWithinSeconds": {
            "description": "The freshness window.",
            "type": "integer",
            "format": "int64"
          },
          "reason": {
            "description": "Which of the four ways it is not fresh, or `evaluated`.",
            "$ref": "#/components/schemas/EvaluationReason"
          },
          "serverTime": {
            "description": "The clock the comparison used. Published so a reader can check the arithmetic rather than redo it against the browser's.",
            "type": "string",
            "format": "date-time"
          },
          "state": {
            "description": "`fresh` or `unknown`.",
            "$ref": "#/components/schemas/EvaluationState"
          }
        }
      },
      "TrustKeyView": {
        "description": "One key, as the policy declares it and as the controller evaluated it.",
        "type": "object",
        "required": [
          "algorithm",
          "effectiveState",
          "keyId",
          "notAfter",
          "notBefore",
          "principal",
          "state",
          "usages"
        ],
        "properties": {
          "algorithm": {
            "description": "`p256` or `ed25519`.",
            "type": "string"
          },
          "effectiveState": {
            "description": "The EVALUATED state — `Active`, `NotYetValid`, `Expired`, `Retired`, `Revoked`, `Unparseable` — or `unknown` when the evaluation is not fresh. **`valid` and `expired` are rendered only for a fresh evaluation.**",
            "type": "string"
          },
          "keyId": {
            "description": "The key ID: the SHA-256 of the DER SPKI, lowercase hex — the number `openssl` prints, and what an out-of-band comparison compares.",
            "type": "string"
          },
          "notAfter": {
            "description": "The end of it. It may only ever move earlier.",
            "type": "string",
            "format": "date-time"
          },
          "notBefore": {
            "description": "The start of its validity window.",
            "type": "string",
            "format": "date-time"
          },
          "principal": {
            "description": "Who holds it.",
            "$ref": "#/components/schemas/KeyPrincipalView"
          },
          "retiredAt": {
            "description": "When it was retired.",
            "type": "string",
            "format": "date-time",
            "nullable": true
          },
          "revocationEffectiveFrom": {
            "description": "From when the revocation bites.",
            "type": "string",
            "format": "date-time",
            "nullable": true
          },
          "revocationReason": {
            "description": "`KeyCompromise`, `Superseded` or `Unspecified`. The distinction decides what happens to evidence signed before the revocation.",
            "type": "string",
            "nullable": true
          },
          "revokedAt": {
            "description": "When it was revoked.",
            "type": "string",
            "format": "date-time",
            "nullable": true
          },
          "state": {
            "description": "The DECLARED lifecycle state: `Active`, `Retired` or `Revoked`.",
            "type": "string"
          },
          "usableForNewSignatures": {
            "description": "Whether it may sign new material. Absent when the evaluation is not fresh: an unevaluated key is not a usable one, and it is not an unusable one either.",
            "type": "boolean",
            "nullable": true
          },
          "usableForVerification": {
            "description": "`Full`, `Historical` or `None`. Absent when the evaluation is not fresh.",
            "type": "string",
            "nullable": true
          },
          "usages": {
            "description": "Exactly one of `EvidenceSigning`, `GovernedApproval` or `ConsoleConfirmation`. One key, one usage.",
            "type": "array",
            "items": {
              "type": "string"
            }
          }
        }
      },
      "TrustPolicyList": {
        "description": "A page of trust policies.",
        "type": "object",
        "required": [
          "items",
          "page",
          "requestId"
        ],
        "properties": {
          "items": {
            "description": "The items on this page.",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/TrustPolicyView"
            }
          },
          "page": {
            "description": "Paging.",
            "$ref": "#/components/schemas/Page"
          },
          "requestId": {
            "description": "The request ID.",
            "type": "string"
          }
        }
      },
      "TrustPolicyResponse": {
        "description": "One trust policy.",
        "type": "object",
        "required": [
          "item",
          "requestId"
        ],
        "properties": {
          "item": {
            "description": "The item.",
            "$ref": "#/components/schemas/TrustPolicyView"
          },
          "requestId": {
            "description": "The request ID.",
            "type": "string"
          }
        }
      },
      "TrustPolicyView": {
        "description": "A `TrustPolicy`, projected.",
        "type": "object",
        "required": [
          "allowedTargetClusterIds",
          "default",
          "evaluation",
          "keyCount",
          "keysTruncated",
          "name",
          "namespacesFiltered",
          "namespacesTruncated",
          "resourceVersion",
          "uid"
        ],
        "properties": {
          "allowedTargetClusterIds": {
            "description": "How many target cluster ids a rehearsal or restore may write to.",
            "type": "integer",
            "format": "int64"
          },
          "boundNamespaces": {
            "description": "The namespaces the controller resolved to this policy, filtered the same way as [`TrustPolicyView::namespaces`].",
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "conditions": {
            "description": "`Loaded`, `Bound` and `ExpiringSoon`.",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/ConditionView"
            }
          },
          "conflicts": {
            "description": "Namespaces two policies claim, filtered the same way. A conflict is actionable only by somebody who administers the namespace it is about.",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/NamespaceConflictView"
            }
          },
          "createdAt": {
            "description": "When the object was created.",
            "type": "string",
            "format": "date-time",
            "nullable": true
          },
          "default": {
            "description": "Whether this is the installation default.",
            "type": "boolean"
          },
          "evaluation": {
            "description": "How fresh the verdicts above are.",
            "$ref": "#/components/schemas/TrustEvaluationView"
          },
          "generation": {
            "description": "`metadata.generation`.",
            "type": "integer",
            "format": "int64",
            "nullable": true
          },
          "keyCount": {
            "description": "How many keys the policy carries in total.",
            "type": "integer",
            "format": "int64"
          },
          "keys": {
            "description": "The keys, at most [`MAX_ROWS`].",
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/TrustKeyView"
            }
          },
          "keysTruncated": {
            "description": "Whether the key list was cut short.",
            "type": "boolean"
          },
          "loaded": {
            "description": "Whether the controller could load the policy at all.",
            "type": "boolean",
            "nullable": true
          },
          "name": {
            "description": "The object name. Cluster-scoped: there is no namespace.",
            "type": "string"
          },
          "namespaces": {
            "description": "The namespaces it governs by name, **filtered to the ones the reader administers**.",
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "namespacesFiltered": {
            "description": "Whether the namespace lists on this object are a FILTERED view.\n\nA `TrustPolicy` names the namespaces it governs, so publishing the list whole would let an administrator of one namespace enumerate every other namespace in the installation — the enumeration the shared-mode 404 rule exists to prevent, on the one object with no 404 to hide behind. The reader is told the list is partial rather than shown a short list that looks complete.",
            "type": "boolean"
          },
          "namespacesTruncated": {
            "description": "Whether the namespace list was cut short by the row bound.",
            "type": "boolean"
          },
          "observedGeneration": {
            "description": "The generation the status was computed from.",
            "type": "integer",
            "format": "int64",
            "nullable": true
          },
          "resourceVersion": {
            "description": "The resourceVersion this projection was read at.",
            "type": "string"
          },
          "uid": {
            "description": "The Kubernetes UID.",
            "type": "string"
          }
        }
      },
      "TrustState": {
        "description": "D3 §2.5's evidence verdict, which is the RESULT and the TRUST BASIS together.",
        "oneOf": [
          {
            "description": "Not finished, or finished with evidence and no verdict yet.",
            "type": "string",
            "enum": [
              "pending"
            ]
          },
          {
            "description": "Verified under a key the policy accepts now.",
            "type": "string",
            "enum": [
              "verified"
            ]
          },
          {
            "description": "Verified under a key that was valid when it signed and has since been retired. **A pass, not a downgrade** — it is what rotation looks like.",
            "type": "string",
            "enum": [
              "verifiedHistorical"
            ]
          },
          {
            "description": "The signature verifies and the key is one this installation will not accept: unknown, revoked, or holding the wrong usage.",
            "type": "string",
            "enum": [
              "untrusted"
            ]
          },
          {
            "description": "The bytes do not match the signature.",
            "type": "string",
            "enum": [
              "invalid"
            ]
          },
          {
            "description": "No verdict was reached. NOT a verified result.",
            "type": "string",
            "enum": [
              "notAttempted"
            ]
          },
          {
            "description": "The run wrote no artifact to verify (exits 1, 3 and 4, refusals, crashes).",
            "type": "string",
            "enum": [
              "notApplicable"
            ]
          }
        ]
      },
      "UpdateDestinationAccessRequest": {
        "description": "`POST .../destinations/{name}:update-access`.",
        "type": "object",
        "required": [
          "access",
          "expectedGeneration"
        ],
        "properties": {
          "access": {
            "description": "The complete four-grant object. A grant omitted here is REMOVED.",
            "$ref": "#/components/schemas/AccessRequest"
          },
          "expectedGeneration": {
            "description": "The `metadata.generation` the client last read. A stale value is `precondition_failed`: a rotation must not land on a destination that changed under the operator's feet.",
            "type": "integer",
            "format": "int64"
          },
          "transport": {
            "description": "The CA reference. Absent leaves it alone; present with a `null` `caBundle` clears it.",
            "default": null,
            "$ref": "#/components/schemas/UpdateTransportRequest",
            "nullable": true
          }
        },
        "additionalProperties": false
      },
      "UpdateSchedulePolicyRequest": {
        "description": "`PUT /api/v1/namespaces/{ns}/schedules/{name}` — the editable future policy (D1 §5.1, §5.6).\n\nTHE WHOLE POLICY, NOT A DIFF. Every mutable field is sent; a field omitted is REMOVED, which is what makes \"absent means the documented default\" reachable from a form. `spec.sourceRef` is not on this DTO at all: the identity of the protected cluster is immutable, and a request naming it is `422 validation_failed` with `sourceRef: field_immutable` before anything is read or written.\n\nIT CHANGES THE FUTURE AND NOTHING ELSE. A `Backup` already created keeps its copied policy and its frozen inputs; an edit during a run does not reach the run (D1 §5.5).",
        "type": "object",
        "required": [
          "expectedGeneration",
          "schedule",
          "suspended",
          "topicSelection"
        ],
        "properties": {
          "activeDeadlineSeconds": {
            "description": "The run's `activeDeadlineSeconds`, 60 to 86400. Absent means 3600.",
            "default": null,
            "type": "integer",
            "format": "int64",
            "nullable": true
          },
          "archive": {
            "description": "Where backups are written. Exactly one of `archive` or `destinationRef`.",
            "default": null,
            "$ref": "#/components/schemas/ArchiveRequest",
            "nullable": true
          },
          "catchUpPolicy": {
            "description": "Absent means `None`.",
            "default": null,
            "$ref": "#/components/schemas/CatchUpPolicy",
            "nullable": true
          },
          "concurrencyPolicy": {
            "description": "`Forbid` (default) or `Allow`.",
            "default": null,
            "$ref": "#/components/schemas/ConcurrencyPolicy",
            "nullable": true
          },
          "destinationRef": {
            "description": "A saved `BackupDestination` in this namespace. Exactly one of `archive` or `destinationRef`; the sentinel `archive.url` the CRD requires is built here and is never accepted from a body.",
            "default": null,
            "$ref": "#/components/schemas/NameRef",
            "nullable": true
          },
          "expectedGeneration": {
            "description": "The `metadata.generation` the client last read. A different current generation is `412 precondition_failed`: somebody else edited the policy between the read and this write.",
            "type": "integer",
            "format": "int64"
          },
          "retention": {
            "description": "Retention reporting. Logweir deletes nothing.",
            "default": null,
            "$ref": "#/components/schemas/RetentionRequest",
            "nullable": true
          },
          "retry": {
            "description": "Absent means no retries.",
            "default": null,
            "$ref": "#/components/schemas/RetryPolicy",
            "nullable": true
          },
          "schedule": {
            "description": "A five-field cron expression or `@hourly`/`@daily`/`@weekly`, parsed by the controller's own parser.",
            "type": "string"
          },
          "sourceRef": {
            "description": "PRESENT ONLY TO BE REFUSED, and present so that the refusal is a FIELD ERROR rather than \"unknown field\". A console that reads a schedule and writes it back sends every field it read; `sourceRef` is one of them, and `sourceRef: field_immutable` tells the person what to do (create a new schedule) where `malformed_request` would not.",
            "default": null,
            "$ref": "#/components/schemas/NameRef",
            "nullable": true
          },
          "startingDeadlineSeconds": {
            "description": "How long after its instant a slot may still start, 60 to 604800. Absent means 3600.",
            "default": null,
            "type": "integer",
            "format": "int64",
            "nullable": true
          },
          "suspended": {
            "description": "Whether the schedule is suspended. Suspension is part of the policy here as well as its own command route; flipping it changes `metadata.generation` and leaves `runPolicySha256` alone.",
            "type": "boolean"
          },
          "timeZone": {
            "description": "An IANA zone name. Absent means UTC and reproduces today's slots exactly.",
            "default": null,
            "type": "string",
            "nullable": true
          },
          "topicSelection": {
            "description": "The topic selection.",
            "$ref": "#/components/schemas/TopicSelectionRequest"
          }
        },
        "additionalProperties": false
      },
      "UpdateTransportRequest": {
        "description": "The transport half of an access rotation: the CA reference, and nothing else. `security` is absent from this type ON PURPOSE — it is immutable.",
        "type": "object",
        "properties": {
          "caBundle": {
            "description": "The new CA reference, or `null` to clear it.",
            "default": null,
            "$ref": "#/components/schemas/CaBundleRequest",
            "nullable": true
          }
        },
        "additionalProperties": false
      },
      "VerificationScopeLevel": {
        "description": "How thorough the record check was. `complete` does not exist in v1.",
        "oneOf": [
          {
            "description": "`byte-fingerprint`: a sample was compared byte for byte.",
            "type": "string",
            "enum": [
              "sampled"
            ]
          },
          {
            "description": "`consume-only`: records were read back and not compared.",
            "type": "string",
            "enum": [
              "degraded"
            ]
          },
          {
            "description": "No record check ran, or the artifact attests counts and a window rather than a restore.",
            "type": "string",
            "enum": [
              "none"
            ]
          }
        ]
      },
      "VerificationScopeView": {
        "description": "What the verification actually covered, in numbers the console renders a fixed sentence around.",
        "type": "object",
        "required": [
          "level"
        ],
        "properties": {
          "level": {
            "description": "The level.",
            "$ref": "#/components/schemas/VerificationScopeLevel"
          },
          "recordsExpected": {
            "description": "The canary size the plan asked for.",
            "type": "integer",
            "format": "int64",
            "nullable": true
          },
          "recordsSampled": {
            "description": "How many records were sampled.",
            "type": "integer",
            "format": "int64",
            "nullable": true
          },
          "recordsSampledMatching": {
            "description": "How many of them matched byte for byte.",
            "type": "integer",
            "format": "int64",
            "nullable": true
          }
        }
      },
      "VerificationState": {
        "description": "Whether the controller verified the signed evidence.",
        "oneOf": [
          {
            "description": "Not finished, or evidence exists and no verdict is recorded yet.",
            "type": "string",
            "enum": [
              "pending"
            ]
          },
          {
            "description": "The controller verified the signature and digest.",
            "type": "string",
            "enum": [
              "valid"
            ]
          },
          {
            "description": "The controller verified and the answer was no.",
            "type": "string",
            "enum": [
              "invalid"
            ]
          },
          {
            "description": "The controller could not attempt verification. NOT a verified result.",
            "type": "string",
            "enum": [
              "notAttempted"
            ]
          },
          {
            "description": "The finished run recorded no signed evidence to verify.",
            "type": "string",
            "enum": [
              "noEvidence"
            ]
          },
          {
            "description": "A verdict string this build does not recognise.",
            "type": "string",
            "enum": [
              "unknown"
            ]
          }
        ]
      },
      "VerifiedSubjectView": {
        "description": "The exact object whose bytes the controller verified.",
        "type": "object",
        "required": [
          "kind",
          "name",
          "namespace",
          "uid"
        ],
        "properties": {
          "kind": {
            "description": "The subject kind.",
            "type": "string"
          },
          "name": {
            "description": "The subject name.",
            "type": "string"
          },
          "namespace": {
            "description": "The subject namespace.",
            "type": "string"
          },
          "uid": {
            "description": "The subject UID.",
            "type": "string"
          }
        }
      },
      "VisibilityState": {
        "description": "How complete a topic inventory's own author believes it is.",
        "oneOf": [
          {
            "description": "A successful Kafka list ALONE. It is never called complete: an ACL can hide a topic from `DESCRIBE` with no error anywhere.",
            "type": "string",
            "enum": [
              "unknown"
            ]
          },
          {
            "description": "An authorization omission was observed.",
            "type": "string",
            "enum": [
              "limited"
            ]
          },
          {
            "description": "An administrator-governed attestation says the principal sees everything.",
            "type": "string",
            "enum": [
              "attestedComplete"
            ]
          }
        ]
      },
      "VisibilityView": {
        "description": "What a discovery could see, and why.",
        "type": "object",
        "required": [
          "basis",
          "state"
        ],
        "properties": {
          "attestation": {
            "description": "The attestation that justified `attestedComplete`.",
            "type": "string",
            "nullable": true
          },
          "basis": {
            "description": "The observations behind it.",
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "state": {
            "description": "The state.",
            "$ref": "#/components/schemas/VisibilityState"
          }
        }
      },
      "WindowCoveredView": {
        "description": "The covered window.",
        "type": "object",
        "required": [
          "fromMs",
          "toMs"
        ],
        "properties": {
          "fromMs": {
            "description": "Inclusive start, epoch milliseconds.",
            "type": "integer",
            "format": "int64"
          },
          "toMs": {
            "description": "Exclusive end, epoch milliseconds.",
            "type": "integer",
            "format": "int64"
          }
        }
      },
      "WorkloadIdentityRequest": {
        "description": "The ServiceAccount a workload-identity grant runs as.",
        "type": "object",
        "properties": {
          "serviceAccountName": {
            "description": "The ServiceAccount name. Absent means `logweir-runner`.",
            "default": null,
            "type": "string",
            "nullable": true
          }
        },
        "additionalProperties": false
      },
      "WriteProbeDto": {
        "description": "Whether an explicit readiness test may write a marker object.",
        "oneOf": [
          {
            "description": "No object is ever written by a readiness test.",
            "type": "string",
            "enum": [
              "disabled"
            ]
          },
          {
            "description": "A destination test may create ONE marker object under the destination's own prefix. It is never deleted.",
            "type": "string",
            "enum": [
              "createOnlyMarker"
            ]
          }
        ]
      }
    }
  },
  "x-logweir-reserved-problem-codes": [
    "approval_required",
    "policy_mismatch"
  ]
}
