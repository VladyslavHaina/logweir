// Stub so the workspace manifest resolves from Task 1 onward.
// Task 8c overwrites both this file and xtask/Cargo.toml with the real xtask.
fn main() {
    eprintln!("xtask: no subcommand implemented yet (see Task 8c)");
    std::process::exit(2);
}
