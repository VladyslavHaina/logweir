//! Vendored (ported, not linked) serde shapes for OSO's on-bucket artifacts.
//! See each submodule's `SOURCE:` header for the exact upstream file and tag
//! each shape was ported from.

pub mod consumer_groups;
pub mod manifest;
pub mod preflight;
