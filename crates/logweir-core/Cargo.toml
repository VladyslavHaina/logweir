[package]
name = "logweir-core"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[dependencies]
base64.workspace = true
chrono.workspace = true
hex.workspace = true
schemars.workspace = true
serde.workspace = true
serde_json.workspace = true
serde_yaml.workspace = true
sha2.workspace = true
thiserror.workspace = true
ulid = "1"        # pinned deliberately: crates.io is at 3.0.0, but `Ulid::new()`
                  # and `to_string()` are unchanged across 1.x-3.x and v0.1 uses
                  # nothing else; revisit only if a third API is needed.

[features]
default = []
