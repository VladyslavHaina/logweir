[
  {"message": "objectives.pass_rate is not finite (NaN or +/-Inf)", "why": "numeric-domain arm, UNCORPUSABLE BY CONSTRUCTION — no successor is owed, because there is nothing to defer. Measured at Task 5 fix round 1, not argued: `json.loads` accepts the non-standard `NaN`/`Infinity` tokens by default (see `_finite`'s docstring in docs/verify_scorecard.py), so the PYTHON arm IS reachable from a hand-written document; serde_json refuses the bare token at PARSE time, so Rust never reaches `validate_invariants` at all — `logweir drill show` on such a document exits 1 with \"not a scorecard: expected value at line 64 column 18\". The walker requires both readers to produce the SAME invariant refusal text, which those two behaviours can never do. Covered instead by logweir-core's objectives_pass_rate_must_be_finite"},
  {"message": "integrity.pass_rate_measured is not finite (NaN or +/-Inf)", "why": "numeric-domain arm, UNCORPUSABLE BY CONSTRUCTION — no successor is owed, because there is nothing to defer. Measured at Task 5 fix round 1, not argued: `json.loads` accepts the non-standard `NaN`/`Infinity` tokens by default (see `_finite`'s docstring in docs/verify_scorecard.py), so the PYTHON arm IS reachable from a hand-written document; serde_json refuses the bare token at PARSE time, so Rust never reaches `validate_invariants` at all — `logweir drill show` on such a document exits 1 with \"not a scorecard: expected value at line 64 column 18\". The walker requires both readers to produce the SAME invariant refusal text, which those two behaviours can never do. Covered instead by logweir-core's integrity_pass_rate_measured_must_be_finite"},
  {"message": "source.captured_by_logweir is true but last_phase_completed is below -1", "why": "GC18(a) source arm; --from-cluster is Task 24"},
  {"message": "source.captured_by_logweir is true but rpo_source_relative_seconds is null", "why": "GC18(a) source arm; --from-cluster is Task 24"},
  {"message": "source.captured_by_logweir is true but an unmeasured reason is present", "why": "GC18(a) source arm; --from-cluster is Task 24"},
  {"message": "rpo_source_relative_seconds is set but the source was never contacted", "why": "GC18(a) source arm; --from-cluster is Task 24"},
  {"message": "source.captured_by_logweir is false but rpo_source_relative_unmeasured_reason is null", "why": "GC18(a) source arm; --from-cluster is Task 24"},
  {"message": "objectives.met must be null when pass_rate is not measurable", "why": "pre-existing arm, corpusable. Carried by a unit test in BOTH readers: logweir-core's invariants_refuse_a_met_objective_at_a_reduced_integrity_level (written in Task 5 fix round 1 — before it this entry claimed two-reader unit coverage that did not exist) and docs/test_verify_scorecard.py::test_met_true_is_refused_when_the_pass_rate_was_not_measurable. It still has no CORPUS case: Task 5's brief (step 5) enumerates the six NEW outcome arms only. SUCCESSOR for the corpus case: Task 5b."},
  {"message": "{name} is negative ({v}); a recovery-point gap of less than zero", "why": "the fourteenth base statement (addendum A2's placeholder): the only format!-interpolated base arm, so its message never surfaced on a grep for a bare literal"},
  {"message": "records_sampled_matching exceeds records_sampled", "why": "pre-existing arm, corpusable. Task 5 added the CONVERSE (matching != sampled under a `pass`) and gave that one a corpus case; this base direction is carried by a unit test in BOTH readers: logweir-core's invariants_refuse_more_matching_records_than_were_sampled (written in Task 5 fix round 1 — before it this entry claimed two-reader unit coverage that did not exist) and docs/test_verify_scorecard.py::test_more_matching_records_than_sampled_is_refused. SUCCESSOR for the corpus case: Task 5b."},
  {"message": "engine.matrix_verdict is 'fail' but matrix_verdict_reason is null", "why": "pre-existing arm, corpusable. Task 5's matrix work is the 'pass' direction (ruling R-F) and its three cases, not this one. Carried by docs/test_verify_scorecard.py::test_a_matrix_fail_without_a_reason_is_refused ONLY — there is no Rust unit test asserting this message, the same gap Task 5 fix round 1 closed for the two arms above. SUCCESSOR for the corpus case AND the missing Rust unit test: Task 5b."},
  {"message": "integrity.pass_rate_measured is set but the level is not byte-fingerprint", "why": "pre-existing arm, corpusable. Carried by docs/test_verify_scorecard.py::test_a_pass_rate_measured_without_byte_fingerprint_is_refused ONLY, which Task 5 had to move off a matrix_verdict 'pass' to keep isolated; there is no Rust unit test asserting this message. SUCCESSOR for the corpus case AND the missing Rust unit test: Task 5b."},
  {"message": "last_phase_completed outside -1..=9", "why": "GR4 Part A domain arm, corpusable TODAY — it needs no `--from-cluster`, unlike the GC18(a) source arms above, so Task 24 is not its owner. Carried by docs/test_verify_scorecard.py::test_a_last_phase_completed_outside_the_domain_is_refused ONLY; there is no Rust unit test asserting this message. SUCCESSOR for the corpus case AND the missing Rust unit test: Task 5b."},
  {"message": "evidence.version_id is set but the four post-put fields are zeroed before signing", "why": "Task 2's arm; Task 2 covers it in Rust and pytest, not in this corpus"},
  {"message": "evidence.retain_until is set but the four post-put fields are zeroed before signing", "why": "Task 2's arm; covered by Task 2's own tests"},
  {"message": "evidence.immutable is true but the four post-put fields are zeroed before signing", "why": "Task 2's arm; covered by Task 2's own tests"},
  {"message": "evidence.create_only_enforced is true but the four post-put fields are zeroed before signing", "why": "Task 2's arm; covered by Task 2's own tests"},
  {"message": "READER ASYMMETRY (not an arm): a document with no `evidence` block", "occurrences": 0, "why": "NOT a `validate_invariants` statement — `occurrences: 0` keeps it out of the accounting; it is recorded here because this file is where reader-coverage gaps are kept. Addendum A1's deferred finding, probed and recorded by Task 5: `evidence` is an entry in verify_scorecard.py's block-presence loop, so PYTHON refuses a missing `evidence` block inside check_invariants with an invariant-shaped message, while RUST refuses it at DESERIALISATION time (`pub evidence: EvidenceInfo` in scorecard.rs carries no #[serde(default)]) — a different code path and a different message. A1 forbade Task 5 from adding or removing that loop entry. Pinned by nothing in either direction. SUCCESSOR: Task 5b."},
  {"message": "READER ASYMMETRY (not an arm): a document with no `sample` block, or with `sample.records_expected` absent, null, or a string", "occurrences": 0, "why": "NOT a `validate_invariants` statement — `occurrences: 0` keeps it out of the accounting. The twin of the `evidence` entry above and homed at the same successor. MEASURED in Task 5's review and again at da6a959, so pre-existing and not Task 5's to widen or fix: `drill verify` exits 1 from deserialisation (`sample: SampleInfo` is not an Option) while verify_scorecard.py prints VALID and exits 0, because the coverage arm — the first Python arm to read the block — guards with isinstance and skips silently. Task 5 §10 and addendum A1 expressly forbid adding `sample` to the block-presence loop, which would create a Python arm with no Rust counterpart. Pinned by nothing in either direction. SUCCESSOR: Task 5b."}
]
