//! End-to-end test package. The tests live in `e2e/tests/` and are gated behind
//! the `e2e` feature (Tasks 7b and 21c); this library target exists only so the
//! package has a target before those files are added.
